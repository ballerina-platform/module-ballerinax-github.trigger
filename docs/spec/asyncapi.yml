asyncapi: 3.0.0

info:
  title: GitHub Webhooks API
  version: 1.0.0
  description: |
    AsyncAPI specification for GitHub Webhook events. GitHub delivers webhook
    payloads via HTTP POST to a configured endpoint URL. Each event is identified
    by the `X-GitHub-Event` request header.

    Payloads are capped at 25 MB. Common payload parameters (sender, repository,
    organization, installation, enterprise) are included on every applicable event.

    Reference: https://docs.github.com/en/webhooks/webhook-events-and-payloads
  license:
    name: GitHub Docs License
    url: https://docs.github.com/en/site-policy/github-terms/github-terms-for-additional-products-and-features

x-ballerina-event-identifier:
  type: "composite"
  name: "X-GitHub-Event"
  path: "action"

x-ballerina-auth:
  header: "X-Hub-Signature-256"
  signature:
    algorithm: "sha256"
    encoding: "hex"
    headerFormat: "sha256=$signature"
    input: "$body"

# ---------------------------------------------------------------------------
# Channels — one per event type; all share the same webhook endpoint address.
# The X-GitHub-Event header on each message discriminates the event type.
# ---------------------------------------------------------------------------
channels:

  ping:
    address: /
    description: |
      Fired when a new webhook is created. Confirms the webhook was configured
      correctly. Available on repository, organization, app, business, and
      marketplace webhooks.
    messages:
      pingMessage:
        $ref: '#/components/messages/PingMessage'
    bindings:
      http:
        method: POST

  push:
    address: /
    description: |
      Fired when a commit, branch, or tag is pushed to a repository. Also fires
      when a repository is created from a template. Available on repository,
      organization, and app webhooks.
    messages:
      pushMessage:
        $ref: '#/components/messages/PushMessage'
    bindings:
      http:
        method: POST

  pullRequest:
    address: /
    description: |
      Fired when there is activity on a pull request. Actions: assigned,
      auto_merge_disabled, auto_merge_enabled, closed, converted_to_draft,
      demilestoned, dequeued, edited, enqueued, labeled, locked, milestoned,
      opened, ready_for_review, reopened, review_request_removed,
      review_requested, synchronize, unassigned, unlabeled, unlocked.
      Available on repository, organization, and app webhooks.
    messages:
      pullRequestAssigned:
        $ref: '#/components/messages/PullRequestAssignedMessage'
      pullRequestAutoMergeDisabled:
        $ref: '#/components/messages/PullRequestAutoMergeDisabledMessage'
      pullRequestAutoMergeEnabled:
        $ref: '#/components/messages/PullRequestAutoMergeEnabledMessage'
      pullRequestClosed:
        $ref: '#/components/messages/PullRequestClosedMessage'
      pullRequestConvertedToDraft:
        $ref: '#/components/messages/PullRequestConvertedToDraftMessage'
      pullRequestDemilestoned:
        $ref: '#/components/messages/PullRequestDemilestonedMessage'
      pullRequestDequeued:
        $ref: '#/components/messages/PullRequestDequeuedMessage'
      pullRequestEdited:
        $ref: '#/components/messages/PullRequestEditedMessage'
      pullRequestEnqueued:
        $ref: '#/components/messages/PullRequestEnqueuedMessage'
      pullRequestLabeled:
        $ref: '#/components/messages/PullRequestLabeledMessage'
      pullRequestLocked:
        $ref: '#/components/messages/PullRequestLockedMessage'
      pullRequestMilestoned:
        $ref: '#/components/messages/PullRequestMilestonedMessage'
      pullRequestOpened:
        $ref: '#/components/messages/PullRequestOpenedMessage'
      pullRequestReadyForReview:
        $ref: '#/components/messages/PullRequestReadyForReviewMessage'
      pullRequestReopened:
        $ref: '#/components/messages/PullRequestReopenedMessage'
      pullRequestReviewRequestRemoved:
        $ref: '#/components/messages/PullRequestReviewRequestRemovedMessage'
      pullRequestReviewRequested:
        $ref: '#/components/messages/PullRequestReviewRequestedMessage'
      pullRequestSynchronize:
        $ref: '#/components/messages/PullRequestSynchronizeMessage'
      pullRequestUnassigned:
        $ref: '#/components/messages/PullRequestUnassignedMessage'
      pullRequestUnlabeled:
        $ref: '#/components/messages/PullRequestUnlabeledMessage'
      pullRequestUnlocked:
        $ref: '#/components/messages/PullRequestUnlockedMessage'
    bindings:
      http:
        method: POST

  issues:
    address: /
    description: |
      Fired when there is activity on an issue. Actions: assigned, closed,
      deleted, demilestoned, edited, labeled, locked, milestoned, opened,
      pinned, reopened, transferred, typed, unassigned, unlabeled, unlocked,
      unpinned, untyped. Available on repository, organization, and app webhooks.
    messages:
      issuesAssigned:
        $ref: '#/components/messages/IssuesAssignedMessage'
      issuesClosed:
        $ref: '#/components/messages/IssuesClosedMessage'
      issuesDeleted:
        $ref: '#/components/messages/IssuesDeletedMessage'
      issuesDemilestoned:
        $ref: '#/components/messages/IssuesDemilestonedMessage'
      issuesEdited:
        $ref: '#/components/messages/IssuesEditedMessage'
      issuesLabeled:
        $ref: '#/components/messages/IssuesLabeledMessage'
      issuesLocked:
        $ref: '#/components/messages/IssuesLockedMessage'
      issuesMilestoned:
        $ref: '#/components/messages/IssuesMilestonedMessage'
      issuesOpened:
        $ref: '#/components/messages/IssuesOpenedMessage'
      issuesPinned:
        $ref: '#/components/messages/IssuesPinnedMessage'
      issuesReopened:
        $ref: '#/components/messages/IssuesReopenedMessage'
      issuesTransferred:
        $ref: '#/components/messages/IssuesTransferredMessage'
      issuesTyped:
        $ref: '#/components/messages/IssuesTypedMessage'
      issuesUnassigned:
        $ref: '#/components/messages/IssuesUnassignedMessage'
      issuesUnlabeled:
        $ref: '#/components/messages/IssuesUnlabeledMessage'
      issuesUnlocked:
        $ref: '#/components/messages/IssuesUnlockedMessage'
      issuesUnpinned:
        $ref: '#/components/messages/IssuesUnpinnedMessage'
      issuesUntyped:
        $ref: '#/components/messages/IssuesUntypedMessage'
    bindings:
      http:
        method: POST

  release:
    address: /
    description: |
      Fired when there is activity on a release. Actions: created, deleted,
      edited, prereleased, published, released, unpublished.
      Available on repository, organization, and app webhooks.
    messages:
      releaseCreated:
        $ref: '#/components/messages/ReleaseCreatedMessage'
      releaseDeleted:
        $ref: '#/components/messages/ReleaseDeletedMessage'
      releaseEdited:
        $ref: '#/components/messages/ReleaseEditedMessage'
      releasePrereleased:
        $ref: '#/components/messages/ReleasePrereleasedMessage'
      releasePublished:
        $ref: '#/components/messages/ReleasePublishedMessage'
      releaseReleased:
        $ref: '#/components/messages/ReleaseReleasedMessage'
      releaseUnpublished:
        $ref: '#/components/messages/ReleaseUnpublishedMessage'
    bindings:
      http:
        method: POST

  issueComment:
    address: /
    description: |
      Fired when a comment on an issue or pull request is created, deleted,
      edited, pinned, or unpinned. Available on repository, organization,
      and app webhooks.
    messages:
      issueCommentCreated:
        $ref: '#/components/messages/IssueCommentCreatedMessage'
      issueCommentDeleted:
        $ref: '#/components/messages/IssueCommentDeletedMessage'
      issueCommentEdited:
        $ref: '#/components/messages/IssueCommentEditedMessage'
      issueCommentPinned:
        $ref: '#/components/messages/IssueCommentPinnedMessage'
      issueCommentUnpinned:
        $ref: '#/components/messages/IssueCommentUnpinnedMessage'
    bindings:
      http:
        method: POST

  pullRequestReview:
    address: /
    description: |
      Fired when a pull request review is dismissed, edited, or submitted.
      Available on repository, organization, and app webhooks.
    messages:
      pullRequestReviewDismissed:
        $ref: '#/components/messages/PullRequestReviewDismissedMessage'
      pullRequestReviewEdited:
        $ref: '#/components/messages/PullRequestReviewEditedMessage'
      pullRequestReviewSubmitted:
        $ref: '#/components/messages/PullRequestReviewSubmittedMessage'
    bindings:
      http:
        method: POST

  pullRequestReviewComment:
    address: /
    description: |
      Fired when a pull request diff comment is created, deleted, or edited.
      Available on repository, organization, and app webhooks.
    messages:
      pullRequestReviewCommentCreated:
        $ref: '#/components/messages/PullRequestReviewCommentCreatedMessage'
      pullRequestReviewCommentDeleted:
        $ref: '#/components/messages/PullRequestReviewCommentDeletedMessage'
      pullRequestReviewCommentEdited:
        $ref: '#/components/messages/PullRequestReviewCommentEditedMessage'
    bindings:
      http:
        method: POST

  create:
    address: /
    description: |
      Fired when a Git branch or tag is created (up to three tags at once).
      Available on repository, organization, and app webhooks.
    messages:
      createMessage:
        $ref: '#/components/messages/CreateMessage'
    bindings:
      http:
        method: POST

  delete:
    address: /
    description: |
      Fired when a Git branch or tag is deleted (up to three tags at once).
      Available on repository, organization, and app webhooks.
    messages:
      deleteMessage:
        $ref: '#/components/messages/DeleteMessage'
    bindings:
      http:
        method: POST

  fork:
    address: /
    description: |
      Fired when a repository is forked. Available on business, repository,
      organization, and app webhooks.
    messages:
      forkMessage:
        $ref: '#/components/messages/ForkMessage'
    bindings:
      http:
        method: POST

  star:
    address: /
    description: |
      Fired when a repository is starred or unstarred. Actions: created, deleted.
      Available on repository, organization, and app webhooks.
    messages:
      starCreated:
        $ref: '#/components/messages/StarCreatedMessage'
      starDeleted:
        $ref: '#/components/messages/StarDeletedMessage'
    bindings:
      http:
        method: POST

  watch:
    address: /
    description: |
      Fired when someone starts watching (subscribing to) a repository.
      Available on repository, organization, and app webhooks.
    messages:
      watchMessage:
        $ref: '#/components/messages/WatchMessage'
    bindings:
      http:
        method: POST

  checkRun:
    address: /
    description: |
      Fired when there is activity on a check run. Actions: completed, created,
      requested_action, rerequested. Repository and organization webhooks only
      receive created and completed actions. Available on repository,
      organization, and app webhooks.
    messages:
      checkRunCompleted:
        $ref: '#/components/messages/CheckRunCompletedMessage'
      checkRunCreated:
        $ref: '#/components/messages/CheckRunCreatedMessage'
      checkRunRequestedAction:
        $ref: '#/components/messages/CheckRunRequestedActionMessage'
      checkRunRerequested:
        $ref: '#/components/messages/CheckRunRerequestedMessage'
    bindings:
      http:
        method: POST

  checkSuite:
    address: /
    description: |
      Fired when there is activity on a check suite. Actions: completed,
      requested, rerequested. Repository and organization webhooks only receive
      the completed action. Available on repository, organization, and app webhooks.
    messages:
      checkSuiteCompleted:
        $ref: '#/components/messages/CheckSuiteCompletedMessage'
      checkSuiteRequested:
        $ref: '#/components/messages/CheckSuiteRequestedMessage'
      checkSuiteRerequested:
        $ref: '#/components/messages/CheckSuiteRerequestedMessage'
    bindings:
      http:
        method: POST

  deployment:
    address: /
    description: |
      Fired when a deployment is created. Available on repository, organization,
      and app webhooks.
    messages:
      deploymentMessage:
        $ref: '#/components/messages/DeploymentMessage'
    bindings:
      http:
        method: POST

  deploymentStatus:
    address: /
    description: |
      Fired when a new deployment status is created. Not fired for deployments
      with an inactive state. Available on repository, organization, and app
      webhooks.
    messages:
      deploymentStatusMessage:
        $ref: '#/components/messages/DeploymentStatusMessage'
    bindings:
      http:
        method: POST

  workflowRun:
    address: /
    description: |
      Fired when a GitHub Actions workflow run is completed, in_progress, or
      requested. Available on business, repository, organization, and app webhooks.
    messages:
      workflowRunCompleted:
        $ref: '#/components/messages/WorkflowRunCompletedMessage'
      workflowRunInProgress:
        $ref: '#/components/messages/WorkflowRunInProgressMessage'
      workflowRunRequested:
        $ref: '#/components/messages/WorkflowRunRequestedMessage'
    bindings:
      http:
        method: POST

  workflowJob:
    address: /
    description: |
      Fired when there is activity on a GitHub Actions workflow job. Actions:
      completed, in_progress, queued, waiting. Available on business, repository,
      organization, and app webhooks.
    messages:
      workflowJobCompleted:
        $ref: '#/components/messages/WorkflowJobCompletedMessage'
      workflowJobInProgress:
        $ref: '#/components/messages/WorkflowJobInProgressMessage'
      workflowJobQueued:
        $ref: '#/components/messages/WorkflowJobQueuedMessage'
      workflowJobWaiting:
        $ref: '#/components/messages/WorkflowJobWaitingMessage'
    bindings:
      http:
        method: POST

  repository:
    address: /
    description: |
      Fired when there is activity on a repository. Actions: archived, created,
      deleted, edited, privatized, publicized, renamed, transferred, unarchived.
      Available on business, repository, organization, and app webhooks.
    messages:
      repositoryArchived:
        $ref: '#/components/messages/RepositoryArchivedMessage'
      repositoryCreated:
        $ref: '#/components/messages/RepositoryCreatedMessage'
      repositoryDeleted:
        $ref: '#/components/messages/RepositoryDeletedMessage'
      repositoryEdited:
        $ref: '#/components/messages/RepositoryEditedMessage'
      repositoryPrivatized:
        $ref: '#/components/messages/RepositoryPrivatizedMessage'
      repositoryPublicized:
        $ref: '#/components/messages/RepositoryPublicizedMessage'
      repositoryRenamed:
        $ref: '#/components/messages/RepositoryRenamedMessage'
      repositoryTransferred:
        $ref: '#/components/messages/RepositoryTransferredMessage'
      repositoryUnarchived:
        $ref: '#/components/messages/RepositoryUnarchivedMessage'
    bindings:
      http:
        method: POST

  member:
    address: /
    description: |
      Fired when there is activity relating to repository collaborators.
      Actions: added, edited, removed. Available on business, repository,
      organization, and app webhooks.
    messages:
      memberAdded:
        $ref: '#/components/messages/MemberAddedMessage'
      memberEdited:
        $ref: '#/components/messages/MemberEditedMessage'
      memberRemoved:
        $ref: '#/components/messages/MemberRemovedMessage'
    bindings:
      http:
        method: POST

  discussion:
    address: /
    description: |
      Fired when there is activity relating to a discussion. Actions: answered,
      category_changed, closed, created, deleted, edited, labeled, locked,
      pinned, reopened, transferred, unanswered, unlabeled, unlocked, unpinned.
      Available on repository, organization, and app webhooks.
      Note: GitHub Discussions webhooks are in public preview.
    messages:
      discussionAnswered:
        $ref: '#/components/messages/DiscussionAnsweredMessage'
      discussionCategoryChanged:
        $ref: '#/components/messages/DiscussionCategoryChangedMessage'
      discussionClosed:
        $ref: '#/components/messages/DiscussionClosedMessage'
      discussionCreated:
        $ref: '#/components/messages/DiscussionCreatedMessage'
      discussionDeleted:
        $ref: '#/components/messages/DiscussionDeletedMessage'
      discussionEdited:
        $ref: '#/components/messages/DiscussionEditedMessage'
      discussionLabeled:
        $ref: '#/components/messages/DiscussionLabeledMessage'
      discussionLocked:
        $ref: '#/components/messages/DiscussionLockedMessage'
      discussionPinned:
        $ref: '#/components/messages/DiscussionPinnedMessage'
      discussionReopened:
        $ref: '#/components/messages/DiscussionReopenedMessage'
      discussionTransferred:
        $ref: '#/components/messages/DiscussionTransferredMessage'
      discussionUnanswered:
        $ref: '#/components/messages/DiscussionUnansweredMessage'
      discussionUnlabeled:
        $ref: '#/components/messages/DiscussionUnlabeledMessage'
      discussionUnlocked:
        $ref: '#/components/messages/DiscussionUnlockedMessage'
      discussionUnpinned:
        $ref: '#/components/messages/DiscussionUnpinnedMessage'
    bindings:
      http:
        method: POST

  discussionComment:
    address: /
    description: |
      Fired when there is activity relating to a comment on a discussion.
      Actions: created, deleted, edited. Available on repository, organization,
      and app webhooks. Note: GitHub Discussions webhooks are in public preview.
    messages:
      discussionCommentCreated:
        $ref: '#/components/messages/DiscussionCommentCreatedMessage'
      discussionCommentDeleted:
        $ref: '#/components/messages/DiscussionCommentDeletedMessage'
      discussionCommentEdited:
        $ref: '#/components/messages/DiscussionCommentEditedMessage'
    bindings:
      http:
        method: POST

  label:
    address: /
    description: |
      Fired when there is activity relating to labels. Actions: created,
      deleted, edited. Available on repository, organization, and app webhooks.
    messages:
      labelCreated:
        $ref: '#/components/messages/LabelCreatedMessage'
      labelDeleted:
        $ref: '#/components/messages/LabelDeletedMessage'
      labelEdited:
        $ref: '#/components/messages/LabelEditedMessage'
    bindings:
      http:
        method: POST

  milestone:
    address: /
    description: |
      Fired when there is activity relating to milestones. Actions: closed,
      created, deleted, edited, opened. Available on repository, organization,
      and app webhooks.
    messages:
      milestoneClosed:
        $ref: '#/components/messages/MilestoneClosedMessage'
      milestoneCreated:
        $ref: '#/components/messages/MilestoneCreatedMessage'
      milestoneDeleted:
        $ref: '#/components/messages/MilestoneDeletedMessage'
      milestoneEdited:
        $ref: '#/components/messages/MilestoneEditedMessage'
      milestoneOpened:
        $ref: '#/components/messages/MilestoneOpenedMessage'
    bindings:
      http:
        method: POST

  status:
    address: /
    description: |
      Fired when the status of a Git commit changes (error, failure, pending,
      success). No action field — the state property carries the status.
      Available on repository, organization, and app webhooks.
    messages:
      statusMessage:
        $ref: '#/components/messages/StatusMessage'
    bindings:
      http:
        method: POST

  workflowDispatch:
    address: /
    description: |
      Fired when a GitHub Actions workflow is manually triggered via the
      workflow_dispatch event. Available on app webhooks.
    messages:
      workflowDispatchMessage:
        $ref: '#/components/messages/WorkflowDispatchMessage'
    bindings:
      http:
        method: POST

  pullRequestReviewThread:
    address: /
    description: |
      Fired when a comment thread on a pull request is marked as resolved or
      unresolved. Actions: resolved, unresolved. Available on repository,
      organization, and app webhooks.
    messages:
      pullRequestReviewThreadResolved:
        $ref: '#/components/messages/PullRequestReviewThreadResolvedMessage'
      pullRequestReviewThreadUnresolved:
        $ref: '#/components/messages/PullRequestReviewThreadUnresolvedMessage'
    bindings:
      http:
        method: POST

  team:
    address: /
    description: |
      Fired when there is activity relating to teams in an organization.
      Actions: added_to_repository, created, deleted, edited,
      removed_from_repository. Available on organization, business, and app
      webhooks.
    messages:
      teamAddedToRepository:
        $ref: '#/components/messages/TeamAddedToRepositoryMessage'
      teamCreated:
        $ref: '#/components/messages/TeamCreatedMessage'
      teamDeleted:
        $ref: '#/components/messages/TeamDeletedMessage'
      teamEdited:
        $ref: '#/components/messages/TeamEditedMessage'
      teamRemovedFromRepository:
        $ref: '#/components/messages/TeamRemovedFromRepositoryMessage'
    bindings:
      http:
        method: POST

  organization:
    address: /
    description: |
      Fired when there is activity relating to an organization and its members.
      Actions: deleted, member_added, member_invited, member_removed, renamed.
      Available on organization, business, and app webhooks.
    messages:
      organizationDeleted:
        $ref: '#/components/messages/OrganizationDeletedMessage'
      organizationMemberAdded:
        $ref: '#/components/messages/OrganizationMemberAddedMessage'
      organizationMemberInvited:
        $ref: '#/components/messages/OrganizationMemberInvitedMessage'
      organizationMemberRemoved:
        $ref: '#/components/messages/OrganizationMemberRemovedMessage'
      organizationRenamed:
        $ref: '#/components/messages/OrganizationRenamedMessage'
    bindings:
      http:
        method: POST

  deploymentProtectionRule:
    address: /
    description: |
      Fired when a deployment protection rule is requested for an environment.
      Available on app webhooks only.
    messages:
      deploymentProtectionRuleMessage:
        $ref: '#/components/messages/DeploymentProtectionRuleMessage'
    bindings:
      http:
        method: POST

  deploymentReview:
    address: /
    description: |
      Fired when a deployment review is requested, approved, or rejected.
      Actions: approved, rejected, requested. Available on organization and
      app webhooks.
    messages:
      deploymentReviewApproved:
        $ref: '#/components/messages/DeploymentReviewApprovedMessage'
      deploymentReviewRejected:
        $ref: '#/components/messages/DeploymentReviewRejectedMessage'
      deploymentReviewRequested:
        $ref: '#/components/messages/DeploymentReviewRequestedMessage'
    bindings:
      http:
        method: POST

  installationTarget:
    address: /
    description: |
      Fired when there is activity on the account where a GitHub App is
      installed. Action: renamed. Available on app webhooks only.
    messages:
      installationTargetMessage:
        $ref: '#/components/messages/InstallationTargetMessage'
    bindings:
      http:
        method: POST

  issueDependencies:
    address: /
    description: |
      Fired when there is activity relating to issue dependencies. Actions:
      blocked_by_added, blocked_by_removed, blocking_added, blocking_removed.
      Available on repository, organization, and app webhooks.
    messages:
      issueDependenciesBlockedByAdded:
        $ref: '#/components/messages/IssueDependenciesBlockedByAddedMessage'
      issueDependenciesBlockedByRemoved:
        $ref: '#/components/messages/IssueDependenciesBlockedByRemovedMessage'
      issueDependenciesBlockingAdded:
        $ref: '#/components/messages/IssueDependenciesBlockingAddedMessage'
      issueDependenciesBlockingRemoved:
        $ref: '#/components/messages/IssueDependenciesBlockingRemovedMessage'
    bindings:
      http:
        method: POST

  marketplacePurchase:
    address: /
    description: |
      Fired when there is activity on a GitHub Marketplace purchase.
      Actions: cancelled, changed, pending_change, pending_change_cancelled,
      purchased. Available on app webhooks only (GitHub Marketplace).
    messages:
      marketplacePurchaseCancelled:
        $ref: '#/components/messages/MarketplacePurchaseCancelledMessage'
      marketplacePurchaseChanged:
        $ref: '#/components/messages/MarketplacePurchaseChangedMessage'
      marketplacePurchasePendingChange:
        $ref: '#/components/messages/MarketplacePurchasePendingChangeMessage'
      marketplacePurchasePendingChangeCancelled:
        $ref: '#/components/messages/MarketplacePurchasePendingChangeCancelledMessage'
      marketplacePurchasePurchased:
        $ref: '#/components/messages/MarketplacePurchasePurchasedMessage'
    bindings:
      http:
        method: POST

  personalAccessTokenRequest:
    address: /
    description: |
      Fired when a fine-grained personal access token request is created,
      approved, denied, or cancelled. Actions: approved, cancelled, created,
      denied. Available on organization webhooks only.
    messages:
      personalAccessTokenRequestApproved:
        $ref: '#/components/messages/PersonalAccessTokenRequestApprovedMessage'
      personalAccessTokenRequestCancelled:
        $ref: '#/components/messages/PersonalAccessTokenRequestCancelledMessage'
      personalAccessTokenRequestCreated:
        $ref: '#/components/messages/PersonalAccessTokenRequestCreatedMessage'
      personalAccessTokenRequestDenied:
        $ref: '#/components/messages/PersonalAccessTokenRequestDeniedMessage'
    bindings:
      http:
        method: POST

  project:
    address: /
    description: |
      Fired when there is activity on a classic project board. Actions:
      closed, created, deleted, edited, reopened. Available on repository,
      organization, and app webhooks. Note: classic Projects are deprecated.
    messages:
      projectClosed:
        $ref: '#/components/messages/ProjectClosedMessage'
      projectCreated:
        $ref: '#/components/messages/ProjectCreatedMessage'
      projectDeleted:
        $ref: '#/components/messages/ProjectDeletedMessage'
      projectEdited:
        $ref: '#/components/messages/ProjectEditedMessage'
      projectReopened:
        $ref: '#/components/messages/ProjectReopenedMessage'
    bindings:
      http:
        method: POST

  projectCard:
    address: /
    description: |
      Fired when there is activity on a classic project card. Actions:
      converted, created, deleted, edited, moved. Available on repository,
      organization, and app webhooks. Note: classic Projects are deprecated.
    messages:
      projectCardConverted:
        $ref: '#/components/messages/ProjectCardConvertedMessage'
      projectCardCreated:
        $ref: '#/components/messages/ProjectCardCreatedMessage'
      projectCardDeleted:
        $ref: '#/components/messages/ProjectCardDeletedMessage'
      projectCardEdited:
        $ref: '#/components/messages/ProjectCardEditedMessage'
      projectCardMoved:
        $ref: '#/components/messages/ProjectCardMovedMessage'
    bindings:
      http:
        method: POST

  projectColumn:
    address: /
    description: |
      Fired when there is activity on a classic project column. Actions:
      created, deleted, edited, moved. Available on repository, organization,
      and app webhooks. Note: classic Projects are deprecated.
    messages:
      projectColumnCreated:
        $ref: '#/components/messages/ProjectColumnCreatedMessage'
      projectColumnDeleted:
        $ref: '#/components/messages/ProjectColumnDeletedMessage'
      projectColumnEdited:
        $ref: '#/components/messages/ProjectColumnEditedMessage'
      projectColumnMoved:
        $ref: '#/components/messages/ProjectColumnMovedMessage'
    bindings:
      http:
        method: POST

  repositoryImport:
    address: /
    description: |
      Fired when a repository import finishes on GitHub.com. No action field.
      Available on repository and organization webhooks.
    messages:
      repositoryImportMessage:
        $ref: '#/components/messages/RepositoryImportMessage'
    bindings:
      http:
        method: POST

  subIssues:
    address: /
    description: |
      Fired when there is activity relating to sub-issues. Actions:
      parent_issue_added, parent_issue_removed, sub_issue_added,
      sub_issue_removed. Available on repository, organization, and app
      webhooks.
    messages:
      subIssuesParentIssueAdded:
        $ref: '#/components/messages/SubIssuesParentIssueAddedMessage'
      subIssuesParentIssueRemoved:
        $ref: '#/components/messages/SubIssuesParentIssueRemovedMessage'
      subIssuesSubIssueAdded:
        $ref: '#/components/messages/SubIssuesSubIssueAddedMessage'
      subIssuesSubIssueRemoved:
        $ref: '#/components/messages/SubIssuesSubIssueRemovedMessage'
    bindings:
      http:
        method: POST

  pageBuild:
    address: /
    description: |
      Fired when a GitHub Pages site is built, whether the build is successful
      or not. No action field. Available on repository, organization, and app
      webhooks.
    messages:
      pageBuildMessage:
        $ref: '#/components/messages/PageBuildMessage'
    bindings:
      http:
        method: POST

  teamAdd:
    address: /
    description: |
      Fired when a repository is added to a team. No action field.
      Available on repository, organization, and app webhooks.
    messages:
      teamAddMessage:
        $ref: '#/components/messages/TeamAddMessage'
    bindings:
      http:
        method: POST

  sponsorship:
    address: /
    description: |
      Fired when there is activity relating to a GitHub Sponsors sponsorship.
      Actions: cancelled, created, edited, pending_cancellation,
      pending_tier_change, tier_changed. Only available on organization
      webhooks (for the sponsored account).
    messages:
      sponsorshipCancelled:
        $ref: '#/components/messages/SponsorshipCancelledMessage'
      sponsorshipCreated:
        $ref: '#/components/messages/SponsorshipCreatedMessage'
      sponsorshipEdited:
        $ref: '#/components/messages/SponsorshipEditedMessage'
      sponsorshipPendingCancellation:
        $ref: '#/components/messages/SponsorshipPendingCancellationMessage'
      sponsorshipPendingTierChange:
        $ref: '#/components/messages/SponsorshipPendingTierChangeMessage'
      sponsorshipTierChanged:
        $ref: '#/components/messages/SponsorshipTierChangedMessage'
    bindings:
      http:
        method: POST

  securityAndAnalysis:
    address: /
    description: |
      Fired when code security and analysis features are enabled or disabled
      for a repository. No action field. Available on repository, organization,
      and app webhooks.
    messages:
      securityAndAnalysisMessage:
        $ref: '#/components/messages/SecurityAndAnalysisMessage'
    bindings:
      http:
        method: POST

  branchProtectionConfiguration:
    address: /
    description: |
      Fired when branch protection configurations are enabled or disabled for
      a repository. Actions: disabled, enabled. Available on repository,
      organization, and app webhooks.
    messages:
      branchProtectionConfigurationDisabled:
        $ref: '#/components/messages/BranchProtectionConfigurationDisabledMessage'
      branchProtectionConfigurationEnabled:
        $ref: '#/components/messages/BranchProtectionConfigurationEnabledMessage'
    bindings:
      http:
        method: POST

  customProperty:
    address: /
    description: |
      Fired when there is activity relating to an organization custom property.
      Actions: created, deleted, promote_to_enterprise, updated. Available on
      business, organization, and app webhooks.
    messages:
      customPropertyCreated:
        $ref: '#/components/messages/CustomPropertyCreatedMessage'
      customPropertyDeleted:
        $ref: '#/components/messages/CustomPropertyDeletedMessage'
      customPropertyPromoteToEnterprise:
        $ref: '#/components/messages/CustomPropertyPromoteToEnterpriseMessage'
      customPropertyUpdated:
        $ref: '#/components/messages/CustomPropertyUpdatedMessage'
    bindings:
      http:
        method: POST

  customPropertyValues:
    address: /
    description: |
      Fired when the custom property values of a repository are updated.
      Action: updated. Available on repository, organization, and app webhooks.
    messages:
      customPropertyValuesMessage:
        $ref: '#/components/messages/CustomPropertyValuesMessage'
    bindings:
      http:
        method: POST

  projectsV2Item:
    address: /
    description: |
      Fired when there is activity relating to an item on an organization-level
      project (Projects v2). Actions: archived, converted, created, deleted,
      edited, reordered, restored. Available on organization webhooks.
      Note: Projects webhooks are in public preview.
    messages:
      projectsV2ItemArchived:
        $ref: '#/components/messages/ProjectsV2ItemArchivedMessage'
      projectsV2ItemConverted:
        $ref: '#/components/messages/ProjectsV2ItemConvertedMessage'
      projectsV2ItemCreated:
        $ref: '#/components/messages/ProjectsV2ItemCreatedMessage'
      projectsV2ItemDeleted:
        $ref: '#/components/messages/ProjectsV2ItemDeletedMessage'
      projectsV2ItemEdited:
        $ref: '#/components/messages/ProjectsV2ItemEditedMessage'
      projectsV2ItemReordered:
        $ref: '#/components/messages/ProjectsV2ItemReorderedMessage'
      projectsV2ItemRestored:
        $ref: '#/components/messages/ProjectsV2ItemRestoredMessage'
    bindings:
      http:
        method: POST

  projectsV2StatusUpdate:
    address: /
    description: |
      Fired when there is activity relating to a status update on an
      organization-level project. Actions: created, deleted, edited.
      Available on organization webhooks.
    messages:
      projectsV2StatusUpdateCreated:
        $ref: '#/components/messages/ProjectsV2StatusUpdateCreatedMessage'
      projectsV2StatusUpdateDeleted:
        $ref: '#/components/messages/ProjectsV2StatusUpdateDeletedMessage'
      projectsV2StatusUpdateEdited:
        $ref: '#/components/messages/ProjectsV2StatusUpdateEditedMessage'
    bindings:
      http:
        method: POST

  registryPackage:
    address: /
    description: |
      Fired when there is activity relating to GitHub Packages (legacy event).
      Actions: published, updated. GitHub recommends using the package event
      instead. Available on repository, organization, and app webhooks.
    messages:
      registryPackagePublished:
        $ref: '#/components/messages/RegistryPackagePublishedMessage'
      registryPackageUpdated:
        $ref: '#/components/messages/RegistryPackageUpdatedMessage'
    bindings:
      http:
        method: POST

  repositoryRuleset:
    address: /
    description: |
      Fired when there is activity relating to repository rulesets.
      Actions: created, deleted, edited. Available on repository,
      organization, and app webhooks.
    messages:
      repositoryRulesetCreated:
        $ref: '#/components/messages/RepositoryRulesetCreatedMessage'
      repositoryRulesetDeleted:
        $ref: '#/components/messages/RepositoryRulesetDeletedMessage'
      repositoryRulesetEdited:
        $ref: '#/components/messages/RepositoryRulesetEditedMessage'
    bindings:
      http:
        method: POST

  repositoryVulnerabilityAlert:
    address: /
    description: |
      Fired when there is activity relating to a security vulnerability alert
      in a repository. Actions: create, dismiss, reopen, resolve.
      Closing down — use dependabot_alert instead. Available on repository
      and organization webhooks.
    messages:
      repositoryVulnerabilityAlertCreate:
        $ref: '#/components/messages/RepositoryVulnerabilityAlertCreateMessage'
      repositoryVulnerabilityAlertDismiss:
        $ref: '#/components/messages/RepositoryVulnerabilityAlertDismissMessage'
      repositoryVulnerabilityAlertReopen:
        $ref: '#/components/messages/RepositoryVulnerabilityAlertReopenMessage'
      repositoryVulnerabilityAlertResolve:
        $ref: '#/components/messages/RepositoryVulnerabilityAlertResolveMessage'
    bindings:
      http:
        method: POST

  secretScanningAlertLocation:
    address: /
    description: |
      Fired when a new instance of a previously detected secret is found in a
      repository and its location is added to an existing alert. No action field.
      Available on repository, organization, and app webhooks.
    messages:
      secretScanningAlertLocationMessage:
        $ref: '#/components/messages/SecretScanningAlertLocationMessage'
    bindings:
      http:
        method: POST

  secretScanningScan:
    address: /
    description: |
      Fired when secret scanning completes a scan on a repository. No action
      field. Scans can be triggered by pushes, custom pattern updates, or
      partner pattern updates. Available on repository, organization, and app
      webhooks.
    messages:
      secretScanningScanMessage:
        $ref: '#/components/messages/SecretScanningScanMessage'
    bindings:
      http:
        method: POST

  branchProtectionRule:
    address: /
    description: |
      Fired when there is activity relating to branch protection rules.
      Actions: created, deleted, edited. Available on repository, organization,
      and app webhooks.
    messages:
      branchProtectionRuleCreated:
        $ref: '#/components/messages/BranchProtectionRuleCreatedMessage'
      branchProtectionRuleDeleted:
        $ref: '#/components/messages/BranchProtectionRuleDeletedMessage'
      branchProtectionRuleEdited:
        $ref: '#/components/messages/BranchProtectionRuleEditedMessage'
    bindings:
      http:
        method: POST

  codeScanningAlert:
    address: /
    description: |
      Fired when there is activity relating to code scanning alerts. Actions:
      appeared_in_branch, closed_by_user, created, fixed, reopened,
      reopened_by_user, updated_assignment. Available on repository,
      organization, and app webhooks.
    messages:
      codeScanningAlertAppearedInBranch:
        $ref: '#/components/messages/CodeScanningAlertAppearedInBranchMessage'
      codeScanningAlertClosedByUser:
        $ref: '#/components/messages/CodeScanningAlertClosedByUserMessage'
      codeScanningAlertCreated:
        $ref: '#/components/messages/CodeScanningAlertCreatedMessage'
      codeScanningAlertFixed:
        $ref: '#/components/messages/CodeScanningAlertFixedMessage'
      codeScanningAlertReopened:
        $ref: '#/components/messages/CodeScanningAlertReopenedMessage'
      codeScanningAlertReopenedByUser:
        $ref: '#/components/messages/CodeScanningAlertReopenedByUserMessage'
      codeScanningAlertUpdatedAssignment:
        $ref: '#/components/messages/CodeScanningAlertUpdatedAssignmentMessage'
    bindings:
      http:
        method: POST

  deployKey:
    address: /
    description: |
      Fired when a deploy key is created or deleted. Actions: created, deleted.
      Available on repository, organization, and app webhooks.
    messages:
      deployKeyCreated:
        $ref: '#/components/messages/DeployKeyCreatedMessage'
      deployKeyDeleted:
        $ref: '#/components/messages/DeployKeyDeletedMessage'
    bindings:
      http:
        method: POST

  githubAppAuthorization:
    address: /
    description: |
      Fired when a user revokes their authorization of a GitHub App.
      Action: revoked. All GitHub Apps receive this event by default.
      Available on app webhooks.
    messages:
      githubAppAuthorizationMessage:
        $ref: '#/components/messages/GithubAppAuthorizationMessage'
    bindings:
      http:
        method: POST

  meta:
    address: /
    description: |
      Fired when a webhook is deleted. Action: deleted. Available on
      marketplace, business, repository, organization, and app webhooks.
    messages:
      metaMessage:
        $ref: '#/components/messages/MetaMessage'
    bindings:
      http:
        method: POST

  orgBlock:
    address: /
    description: |
      Fired when a non-member is blocked or unblocked from collaborating on
      an organization's repositories. Actions: blocked, unblocked. Available
      on organization, business, and app webhooks.
    messages:
      orgBlockBlocked:
        $ref: '#/components/messages/OrgBlockBlockedMessage'
      orgBlockUnblocked:
        $ref: '#/components/messages/OrgBlockUnblockedMessage'
    bindings:
      http:
        method: POST

  package:
    address: /
    description: |
      Fired when there is activity relating to GitHub Packages. Actions:
      published, updated. Available on repository and organization webhooks.
    messages:
      packagePublished:
        $ref: '#/components/messages/PackagePublishedMessage'
      packageUpdated:
        $ref: '#/components/messages/PackageUpdatedMessage'
    bindings:
      http:
        method: POST

  projectsV2:
    address: /
    description: |
      Fired when there is activity relating to an organization-level project
      (Projects v2). Actions: closed, created, deleted, edited, reopened.
      Available on organization webhooks.
    messages:
      projectsV2Closed:
        $ref: '#/components/messages/ProjectsV2ClosedMessage'
      projectsV2Created:
        $ref: '#/components/messages/ProjectsV2CreatedMessage'
      projectsV2Deleted:
        $ref: '#/components/messages/ProjectsV2DeletedMessage'
      projectsV2Edited:
        $ref: '#/components/messages/ProjectsV2EditedMessage'
      projectsV2Reopened:
        $ref: '#/components/messages/ProjectsV2ReopenedMessage'
    bindings:
      http:
        method: POST

  repositoryAdvisory:
    address: /
    description: |
      Fired when there is activity relating to a repository security advisory.
      Actions: published, reported. Available on repository, organization,
      and app webhooks.
    messages:
      repositoryAdvisoryPublished:
        $ref: '#/components/messages/RepositoryAdvisoryPublishedMessage'
      repositoryAdvisoryReported:
        $ref: '#/components/messages/RepositoryAdvisoryReportedMessage'
    bindings:
      http:
        method: POST

  securityAdvisory:
    address: /
    description: |
      Fired when there is activity relating to a GitHub-reviewed global security
      advisory. Actions: published, updated, withdrawn. Available on app webhooks.
    messages:
      securityAdvisoryPublished:
        $ref: '#/components/messages/SecurityAdvisoryPublishedMessage'
      securityAdvisoryUpdated:
        $ref: '#/components/messages/SecurityAdvisoryUpdatedMessage'
      securityAdvisoryWithdrawn:
        $ref: '#/components/messages/SecurityAdvisoryWithdrawnMessage'
    bindings:
      http:
        method: POST

  commitComment:
    address: /
    description: |
      Fired when someone comments on a commit. Action: created.
      Available on repository, organization, and app webhooks.
    messages:
      commitCommentMessage:
        $ref: '#/components/messages/CommitCommentMessage'
    bindings:
      http:
        method: POST

  dependabotAlert:
    address: /
    description: |
      Fired when there is activity relating to Dependabot alerts. Actions:
      assignees_changed, auto_dismissed, auto_reopened, created, dismissed,
      fixed, reintroduced, reopened. Available on repository, organization,
      and app webhooks.
    messages:
      dependabotAlertAssigneesChanged:
        $ref: '#/components/messages/DependabotAlertAssigneesChangedMessage'
      dependabotAlertAutoDismissed:
        $ref: '#/components/messages/DependabotAlertAutoDismissedMessage'
      dependabotAlertAutoReopened:
        $ref: '#/components/messages/DependabotAlertAutoReopenedMessage'
      dependabotAlertCreated:
        $ref: '#/components/messages/DependabotAlertCreatedMessage'
      dependabotAlertDismissed:
        $ref: '#/components/messages/DependabotAlertDismissedMessage'
      dependabotAlertFixed:
        $ref: '#/components/messages/DependabotAlertFixedMessage'
      dependabotAlertReintroduced:
        $ref: '#/components/messages/DependabotAlertReintroducedMessage'
      dependabotAlertReopened:
        $ref: '#/components/messages/DependabotAlertReopenedMessage'
    bindings:
      http:
        method: POST

  gollum:
    address: /
    description: |
      Fired when someone creates or updates a wiki page. No action field.
      Available on repository, organization, and app webhooks.
    messages:
      gollumMessage:
        $ref: '#/components/messages/GollumMessage'
    bindings:
      http:
        method: POST

  installation:
    address: /
    description: |
      Fired when there is activity relating to a GitHub App installation.
      Actions: created, deleted, new_permissions_accepted, suspend, unsuspend.
      All GitHub Apps receive this event by default. Available on app webhooks.
    messages:
      installationCreated:
        $ref: '#/components/messages/InstallationCreatedMessage'
      installationDeleted:
        $ref: '#/components/messages/InstallationDeletedMessage'
      installationNewPermissionsAccepted:
        $ref: '#/components/messages/InstallationNewPermissionsAcceptedMessage'
      installationSuspend:
        $ref: '#/components/messages/InstallationSuspendMessage'
      installationUnsuspend:
        $ref: '#/components/messages/InstallationUnsuspendMessage'
    bindings:
      http:
        method: POST

  installationRepositories:
    address: /
    description: |
      Fired when the set of repositories a GitHub App installation can access
      changes. Actions: added, removed. Available on app webhooks.
    messages:
      installationRepositoriesAdded:
        $ref: '#/components/messages/InstallationRepositoriesAddedMessage'
      installationRepositoriesRemoved:
        $ref: '#/components/messages/InstallationRepositoriesRemovedMessage'
    bindings:
      http:
        method: POST

  membership:
    address: /
    description: |
      Fired when a user is added or removed from a team. Actions: added, removed.
      Available on organization, business, and app webhooks.
    messages:
      membershipAdded:
        $ref: '#/components/messages/MembershipAddedMessage'
      membershipRemoved:
        $ref: '#/components/messages/MembershipRemovedMessage'
    bindings:
      http:
        method: POST

  mergeGroup:
    address: /
    description: |
      Fired when there is activity relating to a merge group in a merge queue.
      Actions: checks_requested, destroyed. Available on app webhooks.
    messages:
      mergeGroupChecksRequested:
        $ref: '#/components/messages/MergeGroupChecksRequestedMessage'
      mergeGroupDestroyed:
        $ref: '#/components/messages/MergeGroupDestroyedMessage'
    bindings:
      http:
        method: POST

  public:
    address: /
    description: |
      Fired when a repository is made public (visibility changes from private
      to public). No action field. Available on repository, organization,
      and app webhooks.
    messages:
      publicMessage:
        $ref: '#/components/messages/PublicMessage'
    bindings:
      http:
        method: POST

  repositoryDispatch:
    address: /
    description: |
      Fired when a GitHub App sends a POST request to the repository dispatches
      endpoint. The action matches the event_type in the request body.
      Available on app webhooks.
    messages:
      repositoryDispatchMessage:
        $ref: '#/components/messages/RepositoryDispatchMessage'
    bindings:
      http:
        method: POST

  secretScanningAlert:
    address: /
    description: |
      Fired when there is activity relating to a secret scanning alert. Actions:
      assigned, created, publicly_leaked, reopened, resolved, unassigned,
      validated. Available on repository, organization, and app webhooks.
    messages:
      secretScanningAlertAssigned:
        $ref: '#/components/messages/SecretScanningAlertAssignedMessage'
      secretScanningAlertCreated:
        $ref: '#/components/messages/SecretScanningAlertCreatedMessage'
      secretScanningAlertPubliclyLeaked:
        $ref: '#/components/messages/SecretScanningAlertPubliclyLeakedMessage'
      secretScanningAlertReopened:
        $ref: '#/components/messages/SecretScanningAlertReopenedMessage'
      secretScanningAlertResolved:
        $ref: '#/components/messages/SecretScanningAlertResolvedMessage'
      secretScanningAlertUnassigned:
        $ref: '#/components/messages/SecretScanningAlertUnassignedMessage'
      secretScanningAlertValidated:
        $ref: '#/components/messages/SecretScanningAlertValidatedMessage'
    bindings:
      http:
        method: POST

# ---------------------------------------------------------------------------
# Operations — one receive operation per channel/event type
# ---------------------------------------------------------------------------
operations:

  onPing:
    action: receive
    channel:
      $ref: '#/channels/ping'
    summary: Receive a ping confirmation for a newly created webhook
    messages:
      - $ref: '#/channels/ping/messages/pingMessage'

  onPush:
    action: receive
    channel:
      $ref: '#/channels/push'
    summary: Receive a push event when commits or refs are pushed
    messages:
      - $ref: '#/channels/push/messages/pushMessage'

  onPullRequest:
    action: receive
    channel:
      $ref: '#/channels/pullRequest'
    summary: Receive pull request activity events
    messages:
      - $ref: '#/channels/pullRequest/messages/pullRequestAssigned'
      - $ref: '#/channels/pullRequest/messages/pullRequestAutoMergeDisabled'
      - $ref: '#/channels/pullRequest/messages/pullRequestAutoMergeEnabled'
      - $ref: '#/channels/pullRequest/messages/pullRequestClosed'
      - $ref: '#/channels/pullRequest/messages/pullRequestConvertedToDraft'
      - $ref: '#/channels/pullRequest/messages/pullRequestDemilestoned'
      - $ref: '#/channels/pullRequest/messages/pullRequestDequeued'
      - $ref: '#/channels/pullRequest/messages/pullRequestEdited'
      - $ref: '#/channels/pullRequest/messages/pullRequestEnqueued'
      - $ref: '#/channels/pullRequest/messages/pullRequestLabeled'
      - $ref: '#/channels/pullRequest/messages/pullRequestLocked'
      - $ref: '#/channels/pullRequest/messages/pullRequestMilestoned'
      - $ref: '#/channels/pullRequest/messages/pullRequestOpened'
      - $ref: '#/channels/pullRequest/messages/pullRequestReadyForReview'
      - $ref: '#/channels/pullRequest/messages/pullRequestReopened'
      - $ref: '#/channels/pullRequest/messages/pullRequestReviewRequestRemoved'
      - $ref: '#/channels/pullRequest/messages/pullRequestReviewRequested'
      - $ref: '#/channels/pullRequest/messages/pullRequestSynchronize'
      - $ref: '#/channels/pullRequest/messages/pullRequestUnassigned'
      - $ref: '#/channels/pullRequest/messages/pullRequestUnlabeled'
      - $ref: '#/channels/pullRequest/messages/pullRequestUnlocked'
  onIssues:
    action: receive
    channel:
      $ref: '#/channels/issues'
    summary: Receive issue activity events
    messages:
      - $ref: '#/channels/issues/messages/issuesAssigned'
      - $ref: '#/channels/issues/messages/issuesClosed'
      - $ref: '#/channels/issues/messages/issuesDeleted'
      - $ref: '#/channels/issues/messages/issuesDemilestoned'
      - $ref: '#/channels/issues/messages/issuesEdited'
      - $ref: '#/channels/issues/messages/issuesLabeled'
      - $ref: '#/channels/issues/messages/issuesLocked'
      - $ref: '#/channels/issues/messages/issuesMilestoned'
      - $ref: '#/channels/issues/messages/issuesOpened'
      - $ref: '#/channels/issues/messages/issuesPinned'
      - $ref: '#/channels/issues/messages/issuesReopened'
      - $ref: '#/channels/issues/messages/issuesTransferred'
      - $ref: '#/channels/issues/messages/issuesTyped'
      - $ref: '#/channels/issues/messages/issuesUnassigned'
      - $ref: '#/channels/issues/messages/issuesUnlabeled'
      - $ref: '#/channels/issues/messages/issuesUnlocked'
      - $ref: '#/channels/issues/messages/issuesUnpinned'
      - $ref: '#/channels/issues/messages/issuesUntyped'
  onRelease:
    action: receive
    channel:
      $ref: '#/channels/release'
    summary: Receive release activity events
    messages:
      - $ref: '#/channels/release/messages/releaseCreated'
      - $ref: '#/channels/release/messages/releaseDeleted'
      - $ref: '#/channels/release/messages/releaseEdited'
      - $ref: '#/channels/release/messages/releasePrereleased'
      - $ref: '#/channels/release/messages/releasePublished'
      - $ref: '#/channels/release/messages/releaseReleased'
      - $ref: '#/channels/release/messages/releaseUnpublished'
  onIssueComment:
    action: receive
    channel:
      $ref: '#/channels/issueComment'
    summary: Receive issue or pull request comment events
    messages:
      - $ref: '#/channels/issueComment/messages/issueCommentCreated'
      - $ref: '#/channels/issueComment/messages/issueCommentDeleted'
      - $ref: '#/channels/issueComment/messages/issueCommentEdited'
      - $ref: '#/channels/issueComment/messages/issueCommentPinned'
      - $ref: '#/channels/issueComment/messages/issueCommentUnpinned'
  onPullRequestReview:
    action: receive
    channel:
      $ref: '#/channels/pullRequestReview'
    summary: Receive pull request review events
    messages:
      - $ref: '#/channels/pullRequestReview/messages/pullRequestReviewDismissed'
      - $ref: '#/channels/pullRequestReview/messages/pullRequestReviewEdited'
      - $ref: '#/channels/pullRequestReview/messages/pullRequestReviewSubmitted'
  onPullRequestReviewComment:
    action: receive
    channel:
      $ref: '#/channels/pullRequestReviewComment'
    summary: Receive pull request diff comment events
    messages:
      - $ref: '#/channels/pullRequestReviewComment/messages/pullRequestReviewCommentCreated'
      - $ref: '#/channels/pullRequestReviewComment/messages/pullRequestReviewCommentDeleted'
      - $ref: '#/channels/pullRequestReviewComment/messages/pullRequestReviewCommentEdited'
  onCreate:
    action: receive
    channel:
      $ref: '#/channels/create'
    summary: Receive branch or tag creation events
    messages:
      - $ref: '#/channels/create/messages/createMessage'

  onDelete:
    action: receive
    channel:
      $ref: '#/channels/delete'
    summary: Receive branch or tag deletion events
    messages:
      - $ref: '#/channels/delete/messages/deleteMessage'

  onFork:
    action: receive
    channel:
      $ref: '#/channels/fork'
    summary: Receive repository fork events
    messages:
      - $ref: '#/channels/fork/messages/forkMessage'

  onStar:
    action: receive
    channel:
      $ref: '#/channels/star'
    summary: Receive repository star/unstar events
    messages:
      - $ref: '#/channels/star/messages/starCreated'
      - $ref: '#/channels/star/messages/starDeleted'
  onWatch:
    action: receive
    channel:
      $ref: '#/channels/watch'
    summary: Receive repository watch events
    messages:
      - $ref: '#/channels/watch/messages/watchMessage'

  onCheckRun:
    action: receive
    channel:
      $ref: '#/channels/checkRun'
    summary: Receive check run activity events
    messages:
      - $ref: '#/channels/checkRun/messages/checkRunCompleted'
      - $ref: '#/channels/checkRun/messages/checkRunCreated'
      - $ref: '#/channels/checkRun/messages/checkRunRequestedAction'
      - $ref: '#/channels/checkRun/messages/checkRunRerequested'
  onCheckSuite:
    action: receive
    channel:
      $ref: '#/channels/checkSuite'
    summary: Receive check suite activity events
    messages:
      - $ref: '#/channels/checkSuite/messages/checkSuiteCompleted'
      - $ref: '#/channels/checkSuite/messages/checkSuiteRequested'
      - $ref: '#/channels/checkSuite/messages/checkSuiteRerequested'
  onDeployment:
    action: receive
    channel:
      $ref: '#/channels/deployment'
    summary: Receive deployment creation events
    messages:
      - $ref: '#/channels/deployment/messages/deploymentMessage'

  onDeploymentStatus:
    action: receive
    channel:
      $ref: '#/channels/deploymentStatus'
    summary: Receive deployment status events
    messages:
      - $ref: '#/channels/deploymentStatus/messages/deploymentStatusMessage'

  onWorkflowRun:
    action: receive
    channel:
      $ref: '#/channels/workflowRun'
    summary: Receive GitHub Actions workflow run events
    messages:
      - $ref: '#/channels/workflowRun/messages/workflowRunCompleted'
      - $ref: '#/channels/workflowRun/messages/workflowRunInProgress'
      - $ref: '#/channels/workflowRun/messages/workflowRunRequested'
  onWorkflowJob:
    action: receive
    channel:
      $ref: '#/channels/workflowJob'
    summary: Receive GitHub Actions workflow job events
    messages:
      - $ref: '#/channels/workflowJob/messages/workflowJobCompleted'
      - $ref: '#/channels/workflowJob/messages/workflowJobInProgress'
      - $ref: '#/channels/workflowJob/messages/workflowJobQueued'
      - $ref: '#/channels/workflowJob/messages/workflowJobWaiting'
  onRepository:
    action: receive
    channel:
      $ref: '#/channels/repository'
    summary: Receive repository lifecycle events
    messages:
      - $ref: '#/channels/repository/messages/repositoryArchived'
      - $ref: '#/channels/repository/messages/repositoryCreated'
      - $ref: '#/channels/repository/messages/repositoryDeleted'
      - $ref: '#/channels/repository/messages/repositoryEdited'
      - $ref: '#/channels/repository/messages/repositoryPrivatized'
      - $ref: '#/channels/repository/messages/repositoryPublicized'
      - $ref: '#/channels/repository/messages/repositoryRenamed'
      - $ref: '#/channels/repository/messages/repositoryTransferred'
      - $ref: '#/channels/repository/messages/repositoryUnarchived'
  onMember:
    action: receive
    channel:
      $ref: '#/channels/member'
    summary: Receive repository collaborator events
    messages:
      - $ref: '#/channels/member/messages/memberAdded'
      - $ref: '#/channels/member/messages/memberEdited'
      - $ref: '#/channels/member/messages/memberRemoved'
  onDiscussion:
    action: receive
    channel:
      $ref: '#/channels/discussion'
    summary: Receive discussion activity events
    messages:
      - $ref: '#/channels/discussion/messages/discussionAnswered'
      - $ref: '#/channels/discussion/messages/discussionCategoryChanged'
      - $ref: '#/channels/discussion/messages/discussionClosed'
      - $ref: '#/channels/discussion/messages/discussionCreated'
      - $ref: '#/channels/discussion/messages/discussionDeleted'
      - $ref: '#/channels/discussion/messages/discussionEdited'
      - $ref: '#/channels/discussion/messages/discussionLabeled'
      - $ref: '#/channels/discussion/messages/discussionLocked'
      - $ref: '#/channels/discussion/messages/discussionPinned'
      - $ref: '#/channels/discussion/messages/discussionReopened'
      - $ref: '#/channels/discussion/messages/discussionTransferred'
      - $ref: '#/channels/discussion/messages/discussionUnanswered'
      - $ref: '#/channels/discussion/messages/discussionUnlabeled'
      - $ref: '#/channels/discussion/messages/discussionUnlocked'
      - $ref: '#/channels/discussion/messages/discussionUnpinned'
  onDiscussionComment:
    action: receive
    channel:
      $ref: '#/channels/discussionComment'
    summary: Receive discussion comment events
    messages:
      - $ref: '#/channels/discussionComment/messages/discussionCommentCreated'
      - $ref: '#/channels/discussionComment/messages/discussionCommentDeleted'
      - $ref: '#/channels/discussionComment/messages/discussionCommentEdited'
  onLabel:
    action: receive
    channel:
      $ref: '#/channels/label'
    summary: Receive label management events
    messages:
      - $ref: '#/channels/label/messages/labelCreated'
      - $ref: '#/channels/label/messages/labelDeleted'
      - $ref: '#/channels/label/messages/labelEdited'
  onMilestone:
    action: receive
    channel:
      $ref: '#/channels/milestone'
    summary: Receive milestone activity events
    messages:
      - $ref: '#/channels/milestone/messages/milestoneClosed'
      - $ref: '#/channels/milestone/messages/milestoneCreated'
      - $ref: '#/channels/milestone/messages/milestoneDeleted'
      - $ref: '#/channels/milestone/messages/milestoneEdited'
      - $ref: '#/channels/milestone/messages/milestoneOpened'
  onStatus:
    action: receive
    channel:
      $ref: '#/channels/status'
    summary: Receive commit status change events
    messages:
      - $ref: '#/channels/status/messages/statusMessage'

  onWorkflowDispatch:
    action: receive
    channel:
      $ref: '#/channels/workflowDispatch'
    summary: Receive manually triggered workflow events
    messages:
      - $ref: '#/channels/workflowDispatch/messages/workflowDispatchMessage'

  onPullRequestReviewThread:
    action: receive
    channel:
      $ref: '#/channels/pullRequestReviewThread'
    summary: Receive pull request review thread resolved/unresolved events
    messages:
      - $ref: '#/channels/pullRequestReviewThread/messages/pullRequestReviewThreadResolved'
      - $ref: '#/channels/pullRequestReviewThread/messages/pullRequestReviewThreadUnresolved'
  onTeam:
    action: receive
    channel:
      $ref: '#/channels/team'
    summary: Receive organization team activity events
    messages:
      - $ref: '#/channels/team/messages/teamAddedToRepository'
      - $ref: '#/channels/team/messages/teamCreated'
      - $ref: '#/channels/team/messages/teamDeleted'
      - $ref: '#/channels/team/messages/teamEdited'
      - $ref: '#/channels/team/messages/teamRemovedFromRepository'
  onOrganization:
    action: receive
    channel:
      $ref: '#/channels/organization'
    summary: Receive organization membership events
    messages:
      - $ref: '#/channels/organization/messages/organizationDeleted'
      - $ref: '#/channels/organization/messages/organizationMemberAdded'
      - $ref: '#/channels/organization/messages/organizationMemberInvited'
      - $ref: '#/channels/organization/messages/organizationMemberRemoved'
      - $ref: '#/channels/organization/messages/organizationRenamed'
  onDeploymentProtectionRule:
    action: receive
    channel:
      $ref: '#/channels/deploymentProtectionRule'
    summary: Receive deployment protection rule request events
    messages:
      - $ref: '#/channels/deploymentProtectionRule/messages/deploymentProtectionRuleMessage'

  onDeploymentReview:
    action: receive
    channel:
      $ref: '#/channels/deploymentReview'
    summary: Receive deployment review approved, rejected, or requested events
    messages:
      - $ref: '#/channels/deploymentReview/messages/deploymentReviewApproved'
      - $ref: '#/channels/deploymentReview/messages/deploymentReviewRejected'
      - $ref: '#/channels/deploymentReview/messages/deploymentReviewRequested'
  onInstallationTarget:
    action: receive
    channel:
      $ref: '#/channels/installationTarget'
    summary: Receive GitHub App installation account renamed events
    messages:
      - $ref: '#/channels/installationTarget/messages/installationTargetMessage'

  onIssueDependencies:
    action: receive
    channel:
      $ref: '#/channels/issueDependencies'
    summary: Receive issue dependency events
    messages:
      - $ref: '#/channels/issueDependencies/messages/issueDependenciesBlockedByAdded'
      - $ref: '#/channels/issueDependencies/messages/issueDependenciesBlockedByRemoved'
      - $ref: '#/channels/issueDependencies/messages/issueDependenciesBlockingAdded'
      - $ref: '#/channels/issueDependencies/messages/issueDependenciesBlockingRemoved'
  onMarketplacePurchase:
    action: receive
    channel:
      $ref: '#/channels/marketplacePurchase'
    summary: Receive GitHub Marketplace purchase activity events
    messages:
      - $ref: '#/channels/marketplacePurchase/messages/marketplacePurchaseCancelled'
      - $ref: '#/channels/marketplacePurchase/messages/marketplacePurchaseChanged'
      - $ref: '#/channels/marketplacePurchase/messages/marketplacePurchasePendingChange'
      - $ref: '#/channels/marketplacePurchase/messages/marketplacePurchasePendingChangeCancelled'
      - $ref: '#/channels/marketplacePurchase/messages/marketplacePurchasePurchased'
  onPersonalAccessTokenRequest:
    action: receive
    channel:
      $ref: '#/channels/personalAccessTokenRequest'
    summary: Receive fine-grained personal access token request events
    messages:
      - $ref: '#/channels/personalAccessTokenRequest/messages/personalAccessTokenRequestApproved'
      - $ref: '#/channels/personalAccessTokenRequest/messages/personalAccessTokenRequestCancelled'
      - $ref: '#/channels/personalAccessTokenRequest/messages/personalAccessTokenRequestCreated'
      - $ref: '#/channels/personalAccessTokenRequest/messages/personalAccessTokenRequestDenied'
  onProject:
    action: receive
    channel:
      $ref: '#/channels/project'
    summary: Receive classic project board activity events
    messages:
      - $ref: '#/channels/project/messages/projectClosed'
      - $ref: '#/channels/project/messages/projectCreated'
      - $ref: '#/channels/project/messages/projectDeleted'
      - $ref: '#/channels/project/messages/projectEdited'
      - $ref: '#/channels/project/messages/projectReopened'
  onProjectCard:
    action: receive
    channel:
      $ref: '#/channels/projectCard'
    summary: Receive classic project card activity events
    messages:
      - $ref: '#/channels/projectCard/messages/projectCardConverted'
      - $ref: '#/channels/projectCard/messages/projectCardCreated'
      - $ref: '#/channels/projectCard/messages/projectCardDeleted'
      - $ref: '#/channels/projectCard/messages/projectCardEdited'
      - $ref: '#/channels/projectCard/messages/projectCardMoved'
  onProjectColumn:
    action: receive
    channel:
      $ref: '#/channels/projectColumn'
    summary: Receive classic project column activity events
    messages:
      - $ref: '#/channels/projectColumn/messages/projectColumnCreated'
      - $ref: '#/channels/projectColumn/messages/projectColumnDeleted'
      - $ref: '#/channels/projectColumn/messages/projectColumnEdited'
      - $ref: '#/channels/projectColumn/messages/projectColumnMoved'
  onRepositoryImport:
    action: receive
    channel:
      $ref: '#/channels/repositoryImport'
    summary: Receive repository import completed events
    messages:
      - $ref: '#/channels/repositoryImport/messages/repositoryImportMessage'

  onSubIssues:
    action: receive
    channel:
      $ref: '#/channels/subIssues'
    summary: Receive sub-issue added or removed events
    messages:
      - $ref: '#/channels/subIssues/messages/subIssuesParentIssueAdded'
      - $ref: '#/channels/subIssues/messages/subIssuesParentIssueRemoved'
      - $ref: '#/channels/subIssues/messages/subIssuesSubIssueAdded'
      - $ref: '#/channels/subIssues/messages/subIssuesSubIssueRemoved'
  onPageBuild:
    action: receive
    channel:
      $ref: '#/channels/pageBuild'
    summary: Receive GitHub Pages build events
    messages:
      - $ref: '#/channels/pageBuild/messages/pageBuildMessage'

  onTeamAdd:
    action: receive
    channel:
      $ref: '#/channels/teamAdd'
    summary: Receive repository added to team events
    messages:
      - $ref: '#/channels/teamAdd/messages/teamAddMessage'

  onSponsorship:
    action: receive
    channel:
      $ref: '#/channels/sponsorship'
    summary: Receive GitHub Sponsors sponsorship activity events
    messages:
      - $ref: '#/channels/sponsorship/messages/sponsorshipCancelled'
      - $ref: '#/channels/sponsorship/messages/sponsorshipCreated'
      - $ref: '#/channels/sponsorship/messages/sponsorshipEdited'
      - $ref: '#/channels/sponsorship/messages/sponsorshipPendingCancellation'
      - $ref: '#/channels/sponsorship/messages/sponsorshipPendingTierChange'
      - $ref: '#/channels/sponsorship/messages/sponsorshipTierChanged'
  onSecurityAndAnalysis:
    action: receive
    channel:
      $ref: '#/channels/securityAndAnalysis'
    summary: Receive code security and analysis feature toggle events
    messages:
      - $ref: '#/channels/securityAndAnalysis/messages/securityAndAnalysisMessage'

  onBranchProtectionConfiguration:
    action: receive
    channel:
      $ref: '#/channels/branchProtectionConfiguration'
    summary: Receive branch protection configuration enabled/disabled events
    messages:
      - $ref: '#/channels/branchProtectionConfiguration/messages/branchProtectionConfigurationDisabled'
      - $ref: '#/channels/branchProtectionConfiguration/messages/branchProtectionConfigurationEnabled'
  onCustomProperty:
    action: receive
    channel:
      $ref: '#/channels/customProperty'
    summary: Receive organization custom property events
    messages:
      - $ref: '#/channels/customProperty/messages/customPropertyCreated'
      - $ref: '#/channels/customProperty/messages/customPropertyDeleted'
      - $ref: '#/channels/customProperty/messages/customPropertyPromoteToEnterprise'
      - $ref: '#/channels/customProperty/messages/customPropertyUpdated'
  onCustomPropertyValues:
    action: receive
    channel:
      $ref: '#/channels/customPropertyValues'
    summary: Receive repository custom property values updated events
    messages:
      - $ref: '#/channels/customPropertyValues/messages/customPropertyValuesMessage'

  onProjectsV2Item:
    action: receive
    channel:
      $ref: '#/channels/projectsV2Item'
    summary: Receive Projects v2 item activity events
    messages:
      - $ref: '#/channels/projectsV2Item/messages/projectsV2ItemArchived'
      - $ref: '#/channels/projectsV2Item/messages/projectsV2ItemConverted'
      - $ref: '#/channels/projectsV2Item/messages/projectsV2ItemCreated'
      - $ref: '#/channels/projectsV2Item/messages/projectsV2ItemDeleted'
      - $ref: '#/channels/projectsV2Item/messages/projectsV2ItemEdited'
      - $ref: '#/channels/projectsV2Item/messages/projectsV2ItemReordered'
      - $ref: '#/channels/projectsV2Item/messages/projectsV2ItemRestored'
  onProjectsV2StatusUpdate:
    action: receive
    channel:
      $ref: '#/channels/projectsV2StatusUpdate'
    summary: Receive Projects v2 status update events
    messages:
      - $ref: '#/channels/projectsV2StatusUpdate/messages/projectsV2StatusUpdateCreated'
      - $ref: '#/channels/projectsV2StatusUpdate/messages/projectsV2StatusUpdateDeleted'
      - $ref: '#/channels/projectsV2StatusUpdate/messages/projectsV2StatusUpdateEdited'
  onRegistryPackage:
    action: receive
    channel:
      $ref: '#/channels/registryPackage'
    summary: Receive GitHub Packages registry events (legacy)
    messages:
      - $ref: '#/channels/registryPackage/messages/registryPackagePublished'
      - $ref: '#/channels/registryPackage/messages/registryPackageUpdated'
  onRepositoryRuleset:
    action: receive
    channel:
      $ref: '#/channels/repositoryRuleset'
    summary: Receive repository ruleset events
    messages:
      - $ref: '#/channels/repositoryRuleset/messages/repositoryRulesetCreated'
      - $ref: '#/channels/repositoryRuleset/messages/repositoryRulesetDeleted'
      - $ref: '#/channels/repositoryRuleset/messages/repositoryRulesetEdited'
  onRepositoryVulnerabilityAlert:
    action: receive
    channel:
      $ref: '#/channels/repositoryVulnerabilityAlert'
    summary: Receive repository vulnerability alert events (deprecated — use onDependabotAlert)
    messages:
      - $ref: '#/channels/repositoryVulnerabilityAlert/messages/repositoryVulnerabilityAlertCreate'
      - $ref: '#/channels/repositoryVulnerabilityAlert/messages/repositoryVulnerabilityAlertDismiss'
      - $ref: '#/channels/repositoryVulnerabilityAlert/messages/repositoryVulnerabilityAlertReopen'
      - $ref: '#/channels/repositoryVulnerabilityAlert/messages/repositoryVulnerabilityAlertResolve'
  onSecretScanningAlertLocation:
    action: receive
    channel:
      $ref: '#/channels/secretScanningAlertLocation'
    summary: Receive secret scanning alert location added events
    messages:
      - $ref: '#/channels/secretScanningAlertLocation/messages/secretScanningAlertLocationMessage'

  onSecretScanningScan:
    action: receive
    channel:
      $ref: '#/channels/secretScanningScan'
    summary: Receive secret scanning scan completed events
    messages:
      - $ref: '#/channels/secretScanningScan/messages/secretScanningScanMessage'

  onBranchProtectionRule:
    action: receive
    channel:
      $ref: '#/channels/branchProtectionRule'
    summary: Receive branch protection rule events
    messages:
      - $ref: '#/channels/branchProtectionRule/messages/branchProtectionRuleCreated'
      - $ref: '#/channels/branchProtectionRule/messages/branchProtectionRuleDeleted'
      - $ref: '#/channels/branchProtectionRule/messages/branchProtectionRuleEdited'
  onCodeScanningAlert:
    action: receive
    channel:
      $ref: '#/channels/codeScanningAlert'
    summary: Receive code scanning alert events
    messages:
      - $ref: '#/channels/codeScanningAlert/messages/codeScanningAlertAppearedInBranch'
      - $ref: '#/channels/codeScanningAlert/messages/codeScanningAlertClosedByUser'
      - $ref: '#/channels/codeScanningAlert/messages/codeScanningAlertCreated'
      - $ref: '#/channels/codeScanningAlert/messages/codeScanningAlertFixed'
      - $ref: '#/channels/codeScanningAlert/messages/codeScanningAlertReopened'
      - $ref: '#/channels/codeScanningAlert/messages/codeScanningAlertReopenedByUser'
      - $ref: '#/channels/codeScanningAlert/messages/codeScanningAlertUpdatedAssignment'
  onDeployKey:
    action: receive
    channel:
      $ref: '#/channels/deployKey'
    summary: Receive deploy key created or deleted events
    messages:
      - $ref: '#/channels/deployKey/messages/deployKeyCreated'
      - $ref: '#/channels/deployKey/messages/deployKeyDeleted'
  onGithubAppAuthorization:
    action: receive
    channel:
      $ref: '#/channels/githubAppAuthorization'
    summary: Receive GitHub App authorization revocation events
    messages:
      - $ref: '#/channels/githubAppAuthorization/messages/githubAppAuthorizationMessage'

  onMeta:
    action: receive
    channel:
      $ref: '#/channels/meta'
    summary: Receive webhook deleted events
    messages:
      - $ref: '#/channels/meta/messages/metaMessage'

  onOrgBlock:
    action: receive
    channel:
      $ref: '#/channels/orgBlock'
    summary: Receive organization block/unblock events
    messages:
      - $ref: '#/channels/orgBlock/messages/orgBlockBlocked'
      - $ref: '#/channels/orgBlock/messages/orgBlockUnblocked'
  onPackage:
    action: receive
    channel:
      $ref: '#/channels/package'
    summary: Receive GitHub Packages published or updated events
    messages:
      - $ref: '#/channels/package/messages/packagePublished'
      - $ref: '#/channels/package/messages/packageUpdated'
  onProjectsV2:
    action: receive
    channel:
      $ref: '#/channels/projectsV2'
    summary: Receive Projects v2 activity events
    messages:
      - $ref: '#/channels/projectsV2/messages/projectsV2Closed'
      - $ref: '#/channels/projectsV2/messages/projectsV2Created'
      - $ref: '#/channels/projectsV2/messages/projectsV2Deleted'
      - $ref: '#/channels/projectsV2/messages/projectsV2Edited'
      - $ref: '#/channels/projectsV2/messages/projectsV2Reopened'
  onRepositoryAdvisory:
    action: receive
    channel:
      $ref: '#/channels/repositoryAdvisory'
    summary: Receive repository security advisory events
    messages:
      - $ref: '#/channels/repositoryAdvisory/messages/repositoryAdvisoryPublished'
      - $ref: '#/channels/repositoryAdvisory/messages/repositoryAdvisoryReported'
  onSecurityAdvisory:
    action: receive
    channel:
      $ref: '#/channels/securityAdvisory'
    summary: Receive global security advisory events
    messages:
      - $ref: '#/channels/securityAdvisory/messages/securityAdvisoryPublished'
      - $ref: '#/channels/securityAdvisory/messages/securityAdvisoryUpdated'
      - $ref: '#/channels/securityAdvisory/messages/securityAdvisoryWithdrawn'
  onCommitComment:
    action: receive
    channel:
      $ref: '#/channels/commitComment'
    summary: Receive commit comment events
    messages:
      - $ref: '#/channels/commitComment/messages/commitCommentMessage'

  onDependabotAlert:
    action: receive
    channel:
      $ref: '#/channels/dependabotAlert'
    summary: Receive Dependabot security alert events
    messages:
      - $ref: '#/channels/dependabotAlert/messages/dependabotAlertAssigneesChanged'
      - $ref: '#/channels/dependabotAlert/messages/dependabotAlertAutoDismissed'
      - $ref: '#/channels/dependabotAlert/messages/dependabotAlertAutoReopened'
      - $ref: '#/channels/dependabotAlert/messages/dependabotAlertCreated'
      - $ref: '#/channels/dependabotAlert/messages/dependabotAlertDismissed'
      - $ref: '#/channels/dependabotAlert/messages/dependabotAlertFixed'
      - $ref: '#/channels/dependabotAlert/messages/dependabotAlertReintroduced'
      - $ref: '#/channels/dependabotAlert/messages/dependabotAlertReopened'
  onGollum:
    action: receive
    channel:
      $ref: '#/channels/gollum'
    summary: Receive wiki page created or updated events
    messages:
      - $ref: '#/channels/gollum/messages/gollumMessage'

  onInstallation:
    action: receive
    channel:
      $ref: '#/channels/installation'
    summary: Receive GitHub App installation events
    messages:
      - $ref: '#/channels/installation/messages/installationCreated'
      - $ref: '#/channels/installation/messages/installationDeleted'
      - $ref: '#/channels/installation/messages/installationNewPermissionsAccepted'
      - $ref: '#/channels/installation/messages/installationSuspend'
      - $ref: '#/channels/installation/messages/installationUnsuspend'
  onInstallationRepositories:
    action: receive
    channel:
      $ref: '#/channels/installationRepositories'
    summary: Receive GitHub App repository access change events
    messages:
      - $ref: '#/channels/installationRepositories/messages/installationRepositoriesAdded'
      - $ref: '#/channels/installationRepositories/messages/installationRepositoriesRemoved'
  onMembership:
    action: receive
    channel:
      $ref: '#/channels/membership'
    summary: Receive team membership added/removed events
    messages:
      - $ref: '#/channels/membership/messages/membershipAdded'
      - $ref: '#/channels/membership/messages/membershipRemoved'
  onMergeGroup:
    action: receive
    channel:
      $ref: '#/channels/mergeGroup'
    summary: Receive merge queue group events
    messages:
      - $ref: '#/channels/mergeGroup/messages/mergeGroupChecksRequested'
      - $ref: '#/channels/mergeGroup/messages/mergeGroupDestroyed'
  onPublic:
    action: receive
    channel:
      $ref: '#/channels/public'
    summary: Receive repository made-public events
    messages:
      - $ref: '#/channels/public/messages/publicMessage'

  onRepositoryDispatch:
    action: receive
    channel:
      $ref: '#/channels/repositoryDispatch'
    summary: Receive custom repository dispatch events
    messages:
      - $ref: '#/channels/repositoryDispatch/messages/repositoryDispatchMessage'

  onSecretScanningAlert:
    action: receive
    channel:
      $ref: '#/channels/secretScanningAlert'
    summary: Receive secret scanning alert events
    messages:
      - $ref: '#/channels/secretScanningAlert/messages/secretScanningAlertAssigned'
      - $ref: '#/channels/secretScanningAlert/messages/secretScanningAlertCreated'
      - $ref: '#/channels/secretScanningAlert/messages/secretScanningAlertPubliclyLeaked'
      - $ref: '#/channels/secretScanningAlert/messages/secretScanningAlertReopened'
      - $ref: '#/channels/secretScanningAlert/messages/secretScanningAlertResolved'
      - $ref: '#/channels/secretScanningAlert/messages/secretScanningAlertUnassigned'
      - $ref: '#/channels/secretScanningAlert/messages/secretScanningAlertValidated'
components:

  # -------------------------------------------------------------------------
  # Messages
  # -------------------------------------------------------------------------
  messages:

    PingMessage:
      name: ping
      title: Ping Event
      summary: Confirmation that a webhook was configured correctly
      x-ballerina-event-type: "ping"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/PingPayload'

    PushMessage:
      name: push
      title: Push Event
      summary: A commit, branch, or tag was pushed to a repository
      x-ballerina-event-type: "push"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/PushPayload'

    PullRequestAssignedMessage:
      name: pull_request
      title: Pull Request Assigned
      summary: Activity on a pull request
      x-ballerina-event-type: "pull_request_assigned"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/PullRequestPayload'
    PullRequestAutoMergeDisabledMessage:
      name: pull_request
      title: Pull Request Auto Merge Disabled
      summary: Activity on a pull request
      x-ballerina-event-type: "pull_request_auto_merge_disabled"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/PullRequestPayload'
    PullRequestAutoMergeEnabledMessage:
      name: pull_request
      title: Pull Request Auto Merge Enabled
      summary: Activity on a pull request
      x-ballerina-event-type: "pull_request_auto_merge_enabled"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/PullRequestPayload'
    PullRequestClosedMessage:
      name: pull_request
      title: Pull Request Closed
      summary: Activity on a pull request
      x-ballerina-event-type: "pull_request_closed"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/PullRequestPayload'
    PullRequestConvertedToDraftMessage:
      name: pull_request
      title: Pull Request Converted To Draft
      summary: Activity on a pull request
      x-ballerina-event-type: "pull_request_converted_to_draft"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/PullRequestPayload'
    PullRequestDemilestonedMessage:
      name: pull_request
      title: Pull Request Demilestoned
      summary: Activity on a pull request
      x-ballerina-event-type: "pull_request_demilestoned"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/PullRequestPayload'
    PullRequestDequeuedMessage:
      name: pull_request
      title: Pull Request Dequeued
      summary: Activity on a pull request
      x-ballerina-event-type: "pull_request_dequeued"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/PullRequestPayload'
    PullRequestEditedMessage:
      name: pull_request
      title: Pull Request Edited
      summary: Activity on a pull request
      x-ballerina-event-type: "pull_request_edited"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/PullRequestPayload'
    PullRequestEnqueuedMessage:
      name: pull_request
      title: Pull Request Enqueued
      summary: Activity on a pull request
      x-ballerina-event-type: "pull_request_enqueued"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/PullRequestPayload'
    PullRequestLabeledMessage:
      name: pull_request
      title: Pull Request Labeled
      summary: Activity on a pull request
      x-ballerina-event-type: "pull_request_labeled"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/PullRequestPayload'
    PullRequestLockedMessage:
      name: pull_request
      title: Pull Request Locked
      summary: Activity on a pull request
      x-ballerina-event-type: "pull_request_locked"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/PullRequestPayload'
    PullRequestMilestonedMessage:
      name: pull_request
      title: Pull Request Milestoned
      summary: Activity on a pull request
      x-ballerina-event-type: "pull_request_milestoned"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/PullRequestPayload'
    PullRequestOpenedMessage:
      name: pull_request
      title: Pull Request Opened
      summary: Activity on a pull request
      x-ballerina-event-type: "pull_request_opened"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/PullRequestPayload'
    PullRequestReadyForReviewMessage:
      name: pull_request
      title: Pull Request Ready For Review
      summary: Activity on a pull request
      x-ballerina-event-type: "pull_request_ready_for_review"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/PullRequestPayload'
    PullRequestReopenedMessage:
      name: pull_request
      title: Pull Request Reopened
      summary: Activity on a pull request
      x-ballerina-event-type: "pull_request_reopened"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/PullRequestPayload'
    PullRequestReviewRequestRemovedMessage:
      name: pull_request
      title: Pull Request Review Request Removed
      summary: Activity on a pull request
      x-ballerina-event-type: "pull_request_review_request_removed"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/PullRequestPayload'
    PullRequestReviewRequestedMessage:
      name: pull_request
      title: Pull Request Review Requested
      summary: Activity on a pull request
      x-ballerina-event-type: "pull_request_review_requested"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/PullRequestPayload'
    PullRequestSynchronizeMessage:
      name: pull_request
      title: Pull Request Synchronize
      summary: Activity on a pull request
      x-ballerina-event-type: "pull_request_synchronize"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/PullRequestPayload'
    PullRequestUnassignedMessage:
      name: pull_request
      title: Pull Request Unassigned
      summary: Activity on a pull request
      x-ballerina-event-type: "pull_request_unassigned"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/PullRequestPayload'
    PullRequestUnlabeledMessage:
      name: pull_request
      title: Pull Request Unlabeled
      summary: Activity on a pull request
      x-ballerina-event-type: "pull_request_unlabeled"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/PullRequestPayload'
    PullRequestUnlockedMessage:
      name: pull_request
      title: Pull Request Unlocked
      summary: Activity on a pull request
      x-ballerina-event-type: "pull_request_unlocked"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/PullRequestPayload'
    IssuesAssignedMessage:
      name: issues
      title: Issues Assigned
      summary: Activity on an issue
      x-ballerina-event-type: "issues_assigned"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/IssuesPayload'
    IssuesClosedMessage:
      name: issues
      title: Issues Closed
      summary: Activity on an issue
      x-ballerina-event-type: "issues_closed"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/IssuesPayload'
    IssuesDeletedMessage:
      name: issues
      title: Issues Deleted
      summary: Activity on an issue
      x-ballerina-event-type: "issues_deleted"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/IssuesPayload'
    IssuesDemilestonedMessage:
      name: issues
      title: Issues Demilestoned
      summary: Activity on an issue
      x-ballerina-event-type: "issues_demilestoned"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/IssuesPayload'
    IssuesEditedMessage:
      name: issues
      title: Issues Edited
      summary: Activity on an issue
      x-ballerina-event-type: "issues_edited"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/IssuesPayload'
    IssuesLabeledMessage:
      name: issues
      title: Issues Labeled
      summary: Activity on an issue
      x-ballerina-event-type: "issues_labeled"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/IssuesPayload'
    IssuesLockedMessage:
      name: issues
      title: Issues Locked
      summary: Activity on an issue
      x-ballerina-event-type: "issues_locked"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/IssuesPayload'
    IssuesMilestonedMessage:
      name: issues
      title: Issues Milestoned
      summary: Activity on an issue
      x-ballerina-event-type: "issues_milestoned"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/IssuesPayload'
    IssuesOpenedMessage:
      name: issues
      title: Issues Opened
      summary: Activity on an issue
      x-ballerina-event-type: "issues_opened"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/IssuesPayload'
    IssuesPinnedMessage:
      name: issues
      title: Issues Pinned
      summary: Activity on an issue
      x-ballerina-event-type: "issues_pinned"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/IssuesPayload'
    IssuesReopenedMessage:
      name: issues
      title: Issues Reopened
      summary: Activity on an issue
      x-ballerina-event-type: "issues_reopened"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/IssuesPayload'
    IssuesTransferredMessage:
      name: issues
      title: Issues Transferred
      summary: Activity on an issue
      x-ballerina-event-type: "issues_transferred"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/IssuesPayload'
    IssuesTypedMessage:
      name: issues
      title: Issues Typed
      summary: Activity on an issue
      x-ballerina-event-type: "issues_typed"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/IssuesPayload'
    IssuesUnassignedMessage:
      name: issues
      title: Issues Unassigned
      summary: Activity on an issue
      x-ballerina-event-type: "issues_unassigned"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/IssuesPayload'
    IssuesUnlabeledMessage:
      name: issues
      title: Issues Unlabeled
      summary: Activity on an issue
      x-ballerina-event-type: "issues_unlabeled"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/IssuesPayload'
    IssuesUnlockedMessage:
      name: issues
      title: Issues Unlocked
      summary: Activity on an issue
      x-ballerina-event-type: "issues_unlocked"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/IssuesPayload'
    IssuesUnpinnedMessage:
      name: issues
      title: Issues Unpinned
      summary: Activity on an issue
      x-ballerina-event-type: "issues_unpinned"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/IssuesPayload'
    IssuesUntypedMessage:
      name: issues
      title: Issues Untyped
      summary: Activity on an issue
      x-ballerina-event-type: "issues_untyped"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/IssuesPayload'
    ReleaseCreatedMessage:
      name: release
      title: Release Created
      summary: Activity on a release
      x-ballerina-event-type: "release_created"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/ReleasePayload'
    ReleaseDeletedMessage:
      name: release
      title: Release Deleted
      summary: Activity on a release
      x-ballerina-event-type: "release_deleted"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/ReleasePayload'
    ReleaseEditedMessage:
      name: release
      title: Release Edited
      summary: Activity on a release
      x-ballerina-event-type: "release_edited"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/ReleasePayload'
    ReleasePrereleasedMessage:
      name: release
      title: Release Prereleased
      summary: Activity on a release
      x-ballerina-event-type: "release_prereleased"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/ReleasePayload'
    ReleasePublishedMessage:
      name: release
      title: Release Published
      summary: Activity on a release
      x-ballerina-event-type: "release_published"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/ReleasePayload'
    ReleaseReleasedMessage:
      name: release
      title: Release Released
      summary: Activity on a release
      x-ballerina-event-type: "release_released"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/ReleasePayload'
    ReleaseUnpublishedMessage:
      name: release
      title: Release Unpublished
      summary: Activity on a release
      x-ballerina-event-type: "release_unpublished"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/ReleasePayload'
    IssueCommentCreatedMessage:
      name: issue_comment
      title: Issue Comment Created
      summary: A comment on an issue or pull request was created, deleted, or edited
      x-ballerina-event-type: "issue_comment_created"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/IssueCommentPayload'
    IssueCommentDeletedMessage:
      name: issue_comment
      title: Issue Comment Deleted
      summary: A comment on an issue or pull request was created, deleted, or edited
      x-ballerina-event-type: "issue_comment_deleted"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/IssueCommentPayload'
    IssueCommentEditedMessage:
      name: issue_comment
      title: Issue Comment Edited
      summary: A comment on an issue or pull request was created, deleted, or edited
      x-ballerina-event-type: "issue_comment_edited"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/IssueCommentPayload'
    IssueCommentPinnedMessage:
      name: issue_comment
      title: Issue Comment Pinned
      summary: A comment on an issue or pull request was created, deleted, or edited
      x-ballerina-event-type: "issue_comment_pinned"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/IssueCommentPayload'
    IssueCommentUnpinnedMessage:
      name: issue_comment
      title: Issue Comment Unpinned
      summary: A comment on an issue or pull request was created, deleted, or edited
      x-ballerina-event-type: "issue_comment_unpinned"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/IssueCommentPayload'
    PullRequestReviewDismissedMessage:
      name: pull_request_review
      title: Pull Request Review Dismissed
      summary: Activity on a pull request review
      x-ballerina-event-type: "pull_request_review_dismissed"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/PullRequestReviewPayload'
    PullRequestReviewEditedMessage:
      name: pull_request_review
      title: Pull Request Review Edited
      summary: Activity on a pull request review
      x-ballerina-event-type: "pull_request_review_edited"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/PullRequestReviewPayload'
    PullRequestReviewSubmittedMessage:
      name: pull_request_review
      title: Pull Request Review Submitted
      summary: Activity on a pull request review
      x-ballerina-event-type: "pull_request_review_submitted"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/PullRequestReviewPayload'
    PullRequestReviewCommentCreatedMessage:
      name: pull_request_review_comment
      title: Pull Request Review Comment Created
      summary: Activity on a pull request diff comment
      x-ballerina-event-type: "pull_request_review_comment_created"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/PullRequestReviewCommentPayload'
    PullRequestReviewCommentDeletedMessage:
      name: pull_request_review_comment
      title: Pull Request Review Comment Deleted
      summary: Activity on a pull request diff comment
      x-ballerina-event-type: "pull_request_review_comment_deleted"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/PullRequestReviewCommentPayload'
    PullRequestReviewCommentEditedMessage:
      name: pull_request_review_comment
      title: Pull Request Review Comment Edited
      summary: Activity on a pull request diff comment
      x-ballerina-event-type: "pull_request_review_comment_edited"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/PullRequestReviewCommentPayload'
    CreateMessage:
      name: create
      title: Create Event
      summary: A Git branch or tag was created
      x-ballerina-event-type: "create"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/CreatePayload'

    DeleteMessage:
      name: delete
      title: Delete Event
      summary: A Git branch or tag was deleted
      x-ballerina-event-type: "delete"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/DeletePayload'

    ForkMessage:
      name: fork
      title: Fork Event
      summary: A repository was forked
      x-ballerina-event-type: "fork"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/ForkPayload'

    StarCreatedMessage:
      name: star
      title: Star Created
      summary: A repository was starred or unstarred
      x-ballerina-event-type: "star_created"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/StarPayload'
    StarDeletedMessage:
      name: star
      title: Star Deleted
      summary: A repository was starred or unstarred
      x-ballerina-event-type: "star_deleted"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/StarPayload'
    WatchMessage:
      name: watch
      title: Watch Event
      summary: Someone started watching a repository
      x-ballerina-event-type: "watch_started"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/WatchPayload'

    CheckRunCompletedMessage:
      name: check_run
      title: Check Run Completed
      summary: Activity on a check run
      x-ballerina-event-type: "check_run_completed"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/CheckRunPayload'
    CheckRunCreatedMessage:
      name: check_run
      title: Check Run Created
      summary: Activity on a check run
      x-ballerina-event-type: "check_run_created"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/CheckRunPayload'
    CheckRunRequestedActionMessage:
      name: check_run
      title: Check Run Requested Action
      summary: Activity on a check run
      x-ballerina-event-type: "check_run_requested_action"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/CheckRunPayload'
    CheckRunRerequestedMessage:
      name: check_run
      title: Check Run Rerequested
      summary: Activity on a check run
      x-ballerina-event-type: "check_run_rerequested"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/CheckRunPayload'
    CheckSuiteCompletedMessage:
      name: check_suite
      title: Check Suite Completed
      summary: Activity on a check suite
      x-ballerina-event-type: "check_suite_completed"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/CheckSuitePayload'
    CheckSuiteRequestedMessage:
      name: check_suite
      title: Check Suite Requested
      summary: Activity on a check suite
      x-ballerina-event-type: "check_suite_requested"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/CheckSuitePayload'
    CheckSuiteRerequestedMessage:
      name: check_suite
      title: Check Suite Rerequested
      summary: Activity on a check suite
      x-ballerina-event-type: "check_suite_rerequested"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/CheckSuitePayload'
    DeploymentMessage:
      name: deployment
      title: Deployment Event
      summary: A deployment was created
      x-ballerina-event-type: "deployment_created"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/DeploymentPayload'

    DeploymentStatusMessage:
      name: deployment_status
      title: Deployment Status Event
      summary: A new deployment status was created
      x-ballerina-event-type: "deployment_status_created"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/DeploymentStatusPayload'

    WorkflowRunCompletedMessage:
      name: workflow_run
      title: Workflow Run Completed
      summary: Activity on a GitHub Actions workflow run
      x-ballerina-event-type: "workflow_run_completed"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/WorkflowRunPayload'
    WorkflowRunInProgressMessage:
      name: workflow_run
      title: Workflow Run In Progress
      summary: Activity on a GitHub Actions workflow run
      x-ballerina-event-type: "workflow_run_in_progress"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/WorkflowRunPayload'
    WorkflowRunRequestedMessage:
      name: workflow_run
      title: Workflow Run Requested
      summary: Activity on a GitHub Actions workflow run
      x-ballerina-event-type: "workflow_run_requested"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/WorkflowRunPayload'
    WorkflowJobCompletedMessage:
      name: workflow_job
      title: Workflow Job Completed
      summary: Activity on a GitHub Actions workflow job
      x-ballerina-event-type: "workflow_job_completed"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/WorkflowJobPayload'
    WorkflowJobInProgressMessage:
      name: workflow_job
      title: Workflow Job In Progress
      summary: Activity on a GitHub Actions workflow job
      x-ballerina-event-type: "workflow_job_in_progress"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/WorkflowJobPayload'
    WorkflowJobQueuedMessage:
      name: workflow_job
      title: Workflow Job Queued
      summary: Activity on a GitHub Actions workflow job
      x-ballerina-event-type: "workflow_job_queued"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/WorkflowJobPayload'
    WorkflowJobWaitingMessage:
      name: workflow_job
      title: Workflow Job Waiting
      summary: Activity on a GitHub Actions workflow job
      x-ballerina-event-type: "workflow_job_waiting"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/WorkflowJobPayload'
    RepositoryArchivedMessage:
      name: repository
      title: Repository Archived
      summary: Activity on a repository
      x-ballerina-event-type: "repository_archived"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/RepositoryPayload'
    RepositoryCreatedMessage:
      name: repository
      title: Repository Created
      summary: Activity on a repository
      x-ballerina-event-type: "repository_created"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/RepositoryPayload'
    RepositoryDeletedMessage:
      name: repository
      title: Repository Deleted
      summary: Activity on a repository
      x-ballerina-event-type: "repository_deleted"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/RepositoryPayload'
    RepositoryEditedMessage:
      name: repository
      title: Repository Edited
      summary: Activity on a repository
      x-ballerina-event-type: "repository_edited"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/RepositoryPayload'
    RepositoryPrivatizedMessage:
      name: repository
      title: Repository Privatized
      summary: Activity on a repository
      x-ballerina-event-type: "repository_privatized"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/RepositoryPayload'
    RepositoryPublicizedMessage:
      name: repository
      title: Repository Publicized
      summary: Activity on a repository
      x-ballerina-event-type: "repository_publicized"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/RepositoryPayload'
    RepositoryRenamedMessage:
      name: repository
      title: Repository Renamed
      summary: Activity on a repository
      x-ballerina-event-type: "repository_renamed"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/RepositoryPayload'
    RepositoryTransferredMessage:
      name: repository
      title: Repository Transferred
      summary: Activity on a repository
      x-ballerina-event-type: "repository_transferred"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/RepositoryPayload'
    RepositoryUnarchivedMessage:
      name: repository
      title: Repository Unarchived
      summary: Activity on a repository
      x-ballerina-event-type: "repository_unarchived"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/RepositoryPayload'
    MemberAddedMessage:
      name: member
      title: Member Added
      summary: Activity relating to repository collaborators
      x-ballerina-event-type: "member_added"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/MemberPayload'
    MemberEditedMessage:
      name: member
      title: Member Edited
      summary: Activity relating to repository collaborators
      x-ballerina-event-type: "member_edited"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/MemberPayload'
    MemberRemovedMessage:
      name: member
      title: Member Removed
      summary: Activity relating to repository collaborators
      x-ballerina-event-type: "member_removed"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/MemberPayload'
    DiscussionAnsweredMessage:
      name: discussion
      title: Discussion Answered
      summary: Activity relating to a GitHub Discussion
      x-ballerina-event-type: "discussion_answered"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/DiscussionPayload'
    DiscussionCategoryChangedMessage:
      name: discussion
      title: Discussion Category Changed
      summary: Activity relating to a GitHub Discussion
      x-ballerina-event-type: "discussion_category_changed"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/DiscussionPayload'
    DiscussionClosedMessage:
      name: discussion
      title: Discussion Closed
      summary: Activity relating to a GitHub Discussion
      x-ballerina-event-type: "discussion_closed"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/DiscussionPayload'
    DiscussionCreatedMessage:
      name: discussion
      title: Discussion Created
      summary: Activity relating to a GitHub Discussion
      x-ballerina-event-type: "discussion_created"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/DiscussionPayload'
    DiscussionDeletedMessage:
      name: discussion
      title: Discussion Deleted
      summary: Activity relating to a GitHub Discussion
      x-ballerina-event-type: "discussion_deleted"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/DiscussionPayload'
    DiscussionEditedMessage:
      name: discussion
      title: Discussion Edited
      summary: Activity relating to a GitHub Discussion
      x-ballerina-event-type: "discussion_edited"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/DiscussionPayload'
    DiscussionLabeledMessage:
      name: discussion
      title: Discussion Labeled
      summary: Activity relating to a GitHub Discussion
      x-ballerina-event-type: "discussion_labeled"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/DiscussionPayload'
    DiscussionLockedMessage:
      name: discussion
      title: Discussion Locked
      summary: Activity relating to a GitHub Discussion
      x-ballerina-event-type: "discussion_locked"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/DiscussionPayload'
    DiscussionPinnedMessage:
      name: discussion
      title: Discussion Pinned
      summary: Activity relating to a GitHub Discussion
      x-ballerina-event-type: "discussion_pinned"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/DiscussionPayload'
    DiscussionReopenedMessage:
      name: discussion
      title: Discussion Reopened
      summary: Activity relating to a GitHub Discussion
      x-ballerina-event-type: "discussion_reopened"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/DiscussionPayload'
    DiscussionTransferredMessage:
      name: discussion
      title: Discussion Transferred
      summary: Activity relating to a GitHub Discussion
      x-ballerina-event-type: "discussion_transferred"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/DiscussionPayload'
    DiscussionUnansweredMessage:
      name: discussion
      title: Discussion Unanswered
      summary: Activity relating to a GitHub Discussion
      x-ballerina-event-type: "discussion_unanswered"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/DiscussionPayload'
    DiscussionUnlabeledMessage:
      name: discussion
      title: Discussion Unlabeled
      summary: Activity relating to a GitHub Discussion
      x-ballerina-event-type: "discussion_unlabeled"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/DiscussionPayload'
    DiscussionUnlockedMessage:
      name: discussion
      title: Discussion Unlocked
      summary: Activity relating to a GitHub Discussion
      x-ballerina-event-type: "discussion_unlocked"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/DiscussionPayload'
    DiscussionUnpinnedMessage:
      name: discussion
      title: Discussion Unpinned
      summary: Activity relating to a GitHub Discussion
      x-ballerina-event-type: "discussion_unpinned"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/DiscussionPayload'
    DiscussionCommentCreatedMessage:
      name: discussion_comment
      title: Discussion Comment Created
      summary: Activity relating to a comment on a discussion
      x-ballerina-event-type: "discussion_comment_created"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/DiscussionCommentPayload'
    DiscussionCommentDeletedMessage:
      name: discussion_comment
      title: Discussion Comment Deleted
      summary: Activity relating to a comment on a discussion
      x-ballerina-event-type: "discussion_comment_deleted"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/DiscussionCommentPayload'
    DiscussionCommentEditedMessage:
      name: discussion_comment
      title: Discussion Comment Edited
      summary: Activity relating to a comment on a discussion
      x-ballerina-event-type: "discussion_comment_edited"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/DiscussionCommentPayload'
    LabelCreatedMessage:
      name: label
      title: Label Created
      summary: A label was created, deleted, or edited
      x-ballerina-event-type: "label_created"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/LabelPayload'
    LabelDeletedMessage:
      name: label
      title: Label Deleted
      summary: A label was created, deleted, or edited
      x-ballerina-event-type: "label_deleted"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/LabelPayload'
    LabelEditedMessage:
      name: label
      title: Label Edited
      summary: A label was created, deleted, or edited
      x-ballerina-event-type: "label_edited"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/LabelPayload'
    MilestoneClosedMessage:
      name: milestone
      title: Milestone Closed
      summary: Activity relating to milestones
      x-ballerina-event-type: "milestone_closed"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/MilestonePayload'
    MilestoneCreatedMessage:
      name: milestone
      title: Milestone Created
      summary: Activity relating to milestones
      x-ballerina-event-type: "milestone_created"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/MilestonePayload'
    MilestoneDeletedMessage:
      name: milestone
      title: Milestone Deleted
      summary: Activity relating to milestones
      x-ballerina-event-type: "milestone_deleted"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/MilestonePayload'
    MilestoneEditedMessage:
      name: milestone
      title: Milestone Edited
      summary: Activity relating to milestones
      x-ballerina-event-type: "milestone_edited"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/MilestonePayload'
    MilestoneOpenedMessage:
      name: milestone
      title: Milestone Opened
      summary: Activity relating to milestones
      x-ballerina-event-type: "milestone_opened"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/MilestonePayload'
    StatusMessage:
      name: status
      title: Status Event
      summary: The status of a Git commit changed
      x-ballerina-event-type: "status"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/StatusPayload'

    WorkflowDispatchMessage:
      name: workflow_dispatch
      title: Workflow Dispatch Event
      summary: A GitHub Actions workflow was manually triggered
      x-ballerina-event-type: "workflow_dispatch"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/WorkflowDispatchPayload'

    PullRequestReviewThreadResolvedMessage:
      name: pull_request_review_thread
      title: Pull Request Review Thread Resolved
      summary: A pull request review comment thread was resolved or unresolved
      x-ballerina-event-type: "pull_request_review_thread_resolved"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/PullRequestReviewThreadPayload'
    PullRequestReviewThreadUnresolvedMessage:
      name: pull_request_review_thread
      title: Pull Request Review Thread Unresolved
      summary: A pull request review comment thread was resolved or unresolved
      x-ballerina-event-type: "pull_request_review_thread_unresolved"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/PullRequestReviewThreadPayload'
    TeamAddedToRepositoryMessage:
      name: team
      title: Team Added To Repository
      summary: Activity relating to teams in an organization
      x-ballerina-event-type: "team_added_to_repository"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/TeamPayload'
    TeamCreatedMessage:
      name: team
      title: Team Created
      summary: Activity relating to teams in an organization
      x-ballerina-event-type: "team_created"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/TeamPayload'
    TeamDeletedMessage:
      name: team
      title: Team Deleted
      summary: Activity relating to teams in an organization
      x-ballerina-event-type: "team_deleted"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/TeamPayload'
    TeamEditedMessage:
      name: team
      title: Team Edited
      summary: Activity relating to teams in an organization
      x-ballerina-event-type: "team_edited"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/TeamPayload'
    TeamRemovedFromRepositoryMessage:
      name: team
      title: Team Removed From Repository
      summary: Activity relating to teams in an organization
      x-ballerina-event-type: "team_removed_from_repository"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/TeamPayload'
    OrganizationDeletedMessage:
      name: organization
      title: Organization Deleted
      summary: Activity relating to an organization and its members
      x-ballerina-event-type: "organization_deleted"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/OrganizationPayload'
    OrganizationMemberAddedMessage:
      name: organization
      title: Organization Member Added
      summary: Activity relating to an organization and its members
      x-ballerina-event-type: "organization_member_added"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/OrganizationPayload'
    OrganizationMemberInvitedMessage:
      name: organization
      title: Organization Member Invited
      summary: Activity relating to an organization and its members
      x-ballerina-event-type: "organization_member_invited"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/OrganizationPayload'
    OrganizationMemberRemovedMessage:
      name: organization
      title: Organization Member Removed
      summary: Activity relating to an organization and its members
      x-ballerina-event-type: "organization_member_removed"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/OrganizationPayload'
    OrganizationRenamedMessage:
      name: organization
      title: Organization Renamed
      summary: Activity relating to an organization and its members
      x-ballerina-event-type: "organization_renamed"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/OrganizationPayload'
    DeploymentProtectionRuleMessage:
      name: deployment_protection_rule
      title: Deployment Protection Rule Event
      summary: A deployment protection rule was requested for an environment
      x-ballerina-event-type: "deployment_protection_rule"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/DeploymentProtectionRulePayload'

    DeploymentReviewApprovedMessage:
      name: deployment_review
      title: Deployment Review Approved
      summary: A deployment review was requested, approved, or rejected
      x-ballerina-event-type: "deployment_review_approved"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/DeploymentReviewPayload'
    DeploymentReviewRejectedMessage:
      name: deployment_review
      title: Deployment Review Rejected
      summary: A deployment review was requested, approved, or rejected
      x-ballerina-event-type: "deployment_review_rejected"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/DeploymentReviewPayload'
    DeploymentReviewRequestedMessage:
      name: deployment_review
      title: Deployment Review Requested
      summary: A deployment review was requested, approved, or rejected
      x-ballerina-event-type: "deployment_review_requested"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/DeploymentReviewPayload'
    InstallationTargetMessage:
      name: installation_target
      title: Installation Target Event
      summary: The account where a GitHub App is installed was renamed
      x-ballerina-event-type: "installation_target_renamed"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/InstallationTargetPayload'

    IssueDependenciesBlockedByAddedMessage:
      name: issue_dependencies
      title: Issue Dependencies Blocked By Added
      summary: An issue was marked as blocked by another issue
      x-ballerina-event-type: "issue_dependencies_blocked_by_added"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/IssueDependenciesPayload'
    IssueDependenciesBlockedByRemovedMessage:
      name: issue_dependencies
      title: Issue Dependencies Blocked By Removed
      summary: An issue was unmarked as blocked by another issue
      x-ballerina-event-type: "issue_dependencies_blocked_by_removed"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/IssueDependenciesPayload'
    IssueDependenciesBlockingAddedMessage:
      name: issue_dependencies
      title: Issue Dependencies Blocking Added
      summary: An issue was marked as blocking another issue
      x-ballerina-event-type: "issue_dependencies_blocking_added"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/IssueDependenciesPayload'
    IssueDependenciesBlockingRemovedMessage:
      name: issue_dependencies
      title: Issue Dependencies Blocking Removed
      summary: An issue was unmarked as blocking another issue
      x-ballerina-event-type: "issue_dependencies_blocking_removed"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/IssueDependenciesPayload'
    MarketplacePurchaseCancelledMessage:
      name: marketplace_purchase
      title: Marketplace Purchase Cancelled
      summary: Activity on a GitHub Marketplace purchase
      x-ballerina-event-type: "marketplace_purchase_cancelled"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/MarketplacePurchasePayload'
    MarketplacePurchaseChangedMessage:
      name: marketplace_purchase
      title: Marketplace Purchase Changed
      summary: Activity on a GitHub Marketplace purchase
      x-ballerina-event-type: "marketplace_purchase_changed"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/MarketplacePurchasePayload'
    MarketplacePurchasePendingChangeMessage:
      name: marketplace_purchase
      title: Marketplace Purchase Pending Change
      summary: Activity on a GitHub Marketplace purchase
      x-ballerina-event-type: "marketplace_purchase_pending_change"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/MarketplacePurchasePayload'
    MarketplacePurchasePendingChangeCancelledMessage:
      name: marketplace_purchase
      title: Marketplace Purchase Pending Change Cancelled
      summary: Activity on a GitHub Marketplace purchase
      x-ballerina-event-type: "marketplace_purchase_pending_change_cancelled"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/MarketplacePurchasePayload'
    MarketplacePurchasePurchasedMessage:
      name: marketplace_purchase
      title: Marketplace Purchase Purchased
      summary: Activity on a GitHub Marketplace purchase
      x-ballerina-event-type: "marketplace_purchase_purchased"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/MarketplacePurchasePayload'
    PersonalAccessTokenRequestApprovedMessage:
      name: personal_access_token_request
      title: Personal Access Token Request Approved
      summary: A fine-grained personal access token request was created, approved, denied, or cancelled
      x-ballerina-event-type: "personal_access_token_request_approved"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/PersonalAccessTokenRequestPayload'
    PersonalAccessTokenRequestCancelledMessage:
      name: personal_access_token_request
      title: Personal Access Token Request Cancelled
      summary: A fine-grained personal access token request was created, approved, denied, or cancelled
      x-ballerina-event-type: "personal_access_token_request_cancelled"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/PersonalAccessTokenRequestPayload'
    PersonalAccessTokenRequestCreatedMessage:
      name: personal_access_token_request
      title: Personal Access Token Request Created
      summary: A fine-grained personal access token request was created, approved, denied, or cancelled
      x-ballerina-event-type: "personal_access_token_request_created"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/PersonalAccessTokenRequestPayload'
    PersonalAccessTokenRequestDeniedMessage:
      name: personal_access_token_request
      title: Personal Access Token Request Denied
      summary: A fine-grained personal access token request was created, approved, denied, or cancelled
      x-ballerina-event-type: "personal_access_token_request_denied"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/PersonalAccessTokenRequestPayload'
    ProjectClosedMessage:
      name: project
      title: Project Closed
      summary: Activity on a classic project board
      x-ballerina-event-type: "project_closed"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/ProjectPayload'
    ProjectCreatedMessage:
      name: project
      title: Project Created
      summary: Activity on a classic project board
      x-ballerina-event-type: "project_created"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/ProjectPayload'
    ProjectDeletedMessage:
      name: project
      title: Project Deleted
      summary: Activity on a classic project board
      x-ballerina-event-type: "project_deleted"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/ProjectPayload'
    ProjectEditedMessage:
      name: project
      title: Project Edited
      summary: Activity on a classic project board
      x-ballerina-event-type: "project_edited"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/ProjectPayload'
    ProjectReopenedMessage:
      name: project
      title: Project Reopened
      summary: Activity on a classic project board
      x-ballerina-event-type: "project_reopened"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/ProjectPayload'
    ProjectCardConvertedMessage:
      name: project_card
      title: Project Card Converted
      summary: Activity on a classic project card
      x-ballerina-event-type: "project_card_converted"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/ProjectCardPayload'
    ProjectCardCreatedMessage:
      name: project_card
      title: Project Card Created
      summary: Activity on a classic project card
      x-ballerina-event-type: "project_card_created"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/ProjectCardPayload'
    ProjectCardDeletedMessage:
      name: project_card
      title: Project Card Deleted
      summary: Activity on a classic project card
      x-ballerina-event-type: "project_card_deleted"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/ProjectCardPayload'
    ProjectCardEditedMessage:
      name: project_card
      title: Project Card Edited
      summary: Activity on a classic project card
      x-ballerina-event-type: "project_card_edited"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/ProjectCardPayload'
    ProjectCardMovedMessage:
      name: project_card
      title: Project Card Moved
      summary: Activity on a classic project card
      x-ballerina-event-type: "project_card_moved"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/ProjectCardPayload'
    ProjectColumnCreatedMessage:
      name: project_column
      title: Project Column Created
      summary: Activity on a classic project column
      x-ballerina-event-type: "project_column_created"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/ProjectColumnPayload'
    ProjectColumnDeletedMessage:
      name: project_column
      title: Project Column Deleted
      summary: Activity on a classic project column
      x-ballerina-event-type: "project_column_deleted"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/ProjectColumnPayload'
    ProjectColumnEditedMessage:
      name: project_column
      title: Project Column Edited
      summary: Activity on a classic project column
      x-ballerina-event-type: "project_column_edited"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/ProjectColumnPayload'
    ProjectColumnMovedMessage:
      name: project_column
      title: Project Column Moved
      summary: Activity on a classic project column
      x-ballerina-event-type: "project_column_moved"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/ProjectColumnPayload'
    RepositoryImportMessage:
      name: repository_import
      title: Repository Import Event
      summary: A repository import finished on GitHub.com
      x-ballerina-event-type: "repository_import"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/RepositoryImportPayload'

    SubIssuesSubIssueAddedMessage:
      name: sub_issues
      title: Sub Issues Sub Issue Added
      summary: A sub-issue was added to an issue
      x-ballerina-event-type: "sub_issues_sub_issue_added"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/SubIssuesPayload'
    SubIssuesSubIssueRemovedMessage:
      name: sub_issues
      title: Sub Issues Sub Issue Removed
      summary: A sub-issue was removed from an issue
      x-ballerina-event-type: "sub_issues_sub_issue_removed"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/SubIssuesPayload'
    SubIssuesParentIssueAddedMessage:
      name: sub_issues
      title: Sub Issues Parent Issue Added
      summary: A sub-issue was added to or removed from an issue
      x-ballerina-event-type: "sub_issues_parent_issue_added"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/SubIssuesPayload'
    SubIssuesParentIssueRemovedMessage:
      name: sub_issues
      title: Sub Issues Parent Issue Removed
      summary: A sub-issue was added to or removed from an issue
      x-ballerina-event-type: "sub_issues_parent_issue_removed"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/SubIssuesPayload'
    PageBuildMessage:
      name: page_build
      title: Page Build Event
      summary: A GitHub Pages site build was triggered
      x-ballerina-event-type: "page_build"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/PageBuildPayload'

    TeamAddMessage:
      name: team_add
      title: Team Add Event
      summary: A repository was added to a team
      x-ballerina-event-type: "team_add"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/TeamAddPayload'

    SponsorshipCancelledMessage:
      name: sponsorship
      title: Sponsorship Cancelled
      summary: Activity relating to a GitHub Sponsors sponsorship
      x-ballerina-event-type: "sponsorship_cancelled"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/SponsorshipPayload'
    SponsorshipCreatedMessage:
      name: sponsorship
      title: Sponsorship Created
      summary: Activity relating to a GitHub Sponsors sponsorship
      x-ballerina-event-type: "sponsorship_created"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/SponsorshipPayload'
    SponsorshipEditedMessage:
      name: sponsorship
      title: Sponsorship Edited
      summary: Activity relating to a GitHub Sponsors sponsorship
      x-ballerina-event-type: "sponsorship_edited"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/SponsorshipPayload'
    SponsorshipPendingCancellationMessage:
      name: sponsorship
      title: Sponsorship Pending Cancellation
      summary: Activity relating to a GitHub Sponsors sponsorship
      x-ballerina-event-type: "sponsorship_pending_cancellation"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/SponsorshipPayload'
    SponsorshipPendingTierChangeMessage:
      name: sponsorship
      title: Sponsorship Pending Tier Change
      summary: Activity relating to a GitHub Sponsors sponsorship
      x-ballerina-event-type: "sponsorship_pending_tier_change"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/SponsorshipPayload'
    SponsorshipTierChangedMessage:
      name: sponsorship
      title: Sponsorship Tier Changed
      summary: Activity relating to a GitHub Sponsors sponsorship
      x-ballerina-event-type: "sponsorship_tier_changed"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/SponsorshipPayload'
    SecurityAndAnalysisMessage:
      name: security_and_analysis
      title: Security and Analysis Event
      summary: Code security and analysis features were enabled or disabled for a repository
      x-ballerina-event-type: "security_and_analysis"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/SecurityAndAnalysisPayload'

    BranchProtectionConfigurationDisabledMessage:
      name: branch_protection_configuration
      title: Branch Protection Configuration Disabled
      summary: Branch protection configurations were enabled or disabled for a repository
      x-ballerina-event-type: "branch_protection_configuration_disabled"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/BranchProtectionConfigurationPayload'
    BranchProtectionConfigurationEnabledMessage:
      name: branch_protection_configuration
      title: Branch Protection Configuration Enabled
      summary: Branch protection configurations were enabled or disabled for a repository
      x-ballerina-event-type: "branch_protection_configuration_enabled"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/BranchProtectionConfigurationPayload'
    CustomPropertyCreatedMessage:
      name: custom_property
      title: Custom Property Created
      summary: An organization custom property was created, deleted, or updated
      x-ballerina-event-type: "custom_property_created"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/CustomPropertyPayload'
    CustomPropertyDeletedMessage:
      name: custom_property
      title: Custom Property Deleted
      summary: An organization custom property was created, deleted, or updated
      x-ballerina-event-type: "custom_property_deleted"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/CustomPropertyPayload'
    CustomPropertyPromoteToEnterpriseMessage:
      name: custom_property
      title: Custom Property Promote To Enterprise
      summary: An organization custom property was created, deleted, or updated
      x-ballerina-event-type: "custom_property_promote_to_enterprise"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/CustomPropertyPayload'
    CustomPropertyUpdatedMessage:
      name: custom_property
      title: Custom Property Updated
      summary: An organization custom property was created, deleted, or updated
      x-ballerina-event-type: "custom_property_updated"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/CustomPropertyPayload'
    CustomPropertyValuesMessage:
      name: custom_property_values
      title: Custom Property Values Event
      summary: The custom property values of a repository were updated
      x-ballerina-event-type: "custom_property_values_updated"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/CustomPropertyValuesPayload'

    ProjectsV2ItemArchivedMessage:
      name: projects_v2_item
      title: Projects V2 Item Archived
      summary: Activity relating to an item on an organization-level project
      x-ballerina-event-type: "projects_v2_item_archived"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/ProjectsV2ItemPayload'
    ProjectsV2ItemConvertedMessage:
      name: projects_v2_item
      title: Projects V2 Item Converted
      summary: Activity relating to an item on an organization-level project
      x-ballerina-event-type: "projects_v2_item_converted"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/ProjectsV2ItemPayload'
    ProjectsV2ItemCreatedMessage:
      name: projects_v2_item
      title: Projects V2 Item Created
      summary: Activity relating to an item on an organization-level project
      x-ballerina-event-type: "projects_v2_item_created"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/ProjectsV2ItemPayload'
    ProjectsV2ItemDeletedMessage:
      name: projects_v2_item
      title: Projects V2 Item Deleted
      summary: Activity relating to an item on an organization-level project
      x-ballerina-event-type: "projects_v2_item_deleted"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/ProjectsV2ItemPayload'
    ProjectsV2ItemEditedMessage:
      name: projects_v2_item
      title: Projects V2 Item Edited
      summary: Activity relating to an item on an organization-level project
      x-ballerina-event-type: "projects_v2_item_edited"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/ProjectsV2ItemPayload'
    ProjectsV2ItemReorderedMessage:
      name: projects_v2_item
      title: Projects V2 Item Reordered
      summary: Activity relating to an item on an organization-level project
      x-ballerina-event-type: "projects_v2_item_reordered"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/ProjectsV2ItemPayload'
    ProjectsV2ItemRestoredMessage:
      name: projects_v2_item
      title: Projects V2 Item Restored
      summary: Activity relating to an item on an organization-level project
      x-ballerina-event-type: "projects_v2_item_restored"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/ProjectsV2ItemPayload'
    ProjectsV2StatusUpdateCreatedMessage:
      name: projects_v2_status_update
      title: Projects V2 Status Update Created
      summary: A status update was added, deleted, or edited on an organization project
      x-ballerina-event-type: "projects_v2_status_update_created"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/ProjectsV2StatusUpdatePayload'
    ProjectsV2StatusUpdateDeletedMessage:
      name: projects_v2_status_update
      title: Projects V2 Status Update Deleted
      summary: A status update was added, deleted, or edited on an organization project
      x-ballerina-event-type: "projects_v2_status_update_deleted"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/ProjectsV2StatusUpdatePayload'
    ProjectsV2StatusUpdateEditedMessage:
      name: projects_v2_status_update
      title: Projects V2 Status Update Edited
      summary: A status update was added, deleted, or edited on an organization project
      x-ballerina-event-type: "projects_v2_status_update_edited"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/ProjectsV2StatusUpdatePayload'
    RegistryPackagePublishedMessage:
      name: registry_package
      title: Registry Package Published
      summary: A package was published or updated in a registry (legacy event)
      x-ballerina-event-type: "registry_package_published"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/RegistryPackagePayload'
    RegistryPackageUpdatedMessage:
      name: registry_package
      title: Registry Package Updated
      summary: A package was published or updated in a registry (legacy event)
      x-ballerina-event-type: "registry_package_updated"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/RegistryPackagePayload'
    RepositoryRulesetCreatedMessage:
      name: repository_ruleset
      title: Repository Ruleset Created
      summary: A repository ruleset was created, deleted, or edited
      x-ballerina-event-type: "repository_ruleset_created"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/RepositoryRulesetPayload'
    RepositoryRulesetDeletedMessage:
      name: repository_ruleset
      title: Repository Ruleset Deleted
      summary: A repository ruleset was created, deleted, or edited
      x-ballerina-event-type: "repository_ruleset_deleted"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/RepositoryRulesetPayload'
    RepositoryRulesetEditedMessage:
      name: repository_ruleset
      title: Repository Ruleset Edited
      summary: A repository ruleset was created, deleted, or edited
      x-ballerina-event-type: "repository_ruleset_edited"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/RepositoryRulesetPayload'
    RepositoryVulnerabilityAlertCreateMessage:
      name: repository_vulnerability_alert
      title: Repository Vulnerability Alert Create
      summary: Activity relating to a repository vulnerability alert (deprecated)
      x-ballerina-event-type: "repository_vulnerability_alert_create"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/RepositoryVulnerabilityAlertPayload'
    RepositoryVulnerabilityAlertDismissMessage:
      name: repository_vulnerability_alert
      title: Repository Vulnerability Alert Dismiss
      summary: Activity relating to a repository vulnerability alert (deprecated)
      x-ballerina-event-type: "repository_vulnerability_alert_dismiss"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/RepositoryVulnerabilityAlertPayload'
    RepositoryVulnerabilityAlertReopenMessage:
      name: repository_vulnerability_alert
      title: Repository Vulnerability Alert Reopen
      summary: Activity relating to a repository vulnerability alert (deprecated)
      x-ballerina-event-type: "repository_vulnerability_alert_reopen"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/RepositoryVulnerabilityAlertPayload'
    RepositoryVulnerabilityAlertResolveMessage:
      name: repository_vulnerability_alert
      title: Repository Vulnerability Alert Resolve
      summary: Activity relating to a repository vulnerability alert (deprecated)
      x-ballerina-event-type: "repository_vulnerability_alert_resolve"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/RepositoryVulnerabilityAlertPayload'
    SecretScanningAlertLocationMessage:
      name: secret_scanning_alert_location
      title: Secret Scanning Alert Location Event
      summary: A new location was added to an existing secret scanning alert
      x-ballerina-event-type: "secret_scanning_alert_location"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/SecretScanningAlertLocationPayload'

    SecretScanningScanMessage:
      name: secret_scanning_scan
      title: Secret Scanning Scan Event
      summary: Secret scanning completed a scan on a repository
      x-ballerina-event-type: "secret_scanning_scan"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/SecretScanningScanPayload'

    BranchProtectionRuleCreatedMessage:
      name: branch_protection_rule
      title: Branch Protection Rule Created
      summary: A branch protection rule was created, deleted, or edited
      x-ballerina-event-type: "branch_protection_rule_created"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/BranchProtectionRulePayload'
    BranchProtectionRuleDeletedMessage:
      name: branch_protection_rule
      title: Branch Protection Rule Deleted
      summary: A branch protection rule was created, deleted, or edited
      x-ballerina-event-type: "branch_protection_rule_deleted"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/BranchProtectionRulePayload'
    BranchProtectionRuleEditedMessage:
      name: branch_protection_rule
      title: Branch Protection Rule Edited
      summary: A branch protection rule was created, deleted, or edited
      x-ballerina-event-type: "branch_protection_rule_edited"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/BranchProtectionRulePayload'
    CodeScanningAlertAppearedInBranchMessage:
      name: code_scanning_alert
      title: Code Scanning Alert Appeared In Branch
      summary: Activity relating to a code scanning alert
      x-ballerina-event-type: "code_scanning_alert_appeared_in_branch"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/CodeScanningAlertPayload'
    CodeScanningAlertClosedByUserMessage:
      name: code_scanning_alert
      title: Code Scanning Alert Closed By User
      summary: Activity relating to a code scanning alert
      x-ballerina-event-type: "code_scanning_alert_closed_by_user"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/CodeScanningAlertPayload'
    CodeScanningAlertCreatedMessage:
      name: code_scanning_alert
      title: Code Scanning Alert Created
      summary: Activity relating to a code scanning alert
      x-ballerina-event-type: "code_scanning_alert_created"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/CodeScanningAlertPayload'
    CodeScanningAlertFixedMessage:
      name: code_scanning_alert
      title: Code Scanning Alert Fixed
      summary: Activity relating to a code scanning alert
      x-ballerina-event-type: "code_scanning_alert_fixed"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/CodeScanningAlertPayload'
    CodeScanningAlertReopenedMessage:
      name: code_scanning_alert
      title: Code Scanning Alert Reopened
      summary: Activity relating to a code scanning alert
      x-ballerina-event-type: "code_scanning_alert_reopened"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/CodeScanningAlertPayload'
    CodeScanningAlertReopenedByUserMessage:
      name: code_scanning_alert
      title: Code Scanning Alert Reopened By User
      summary: Activity relating to a code scanning alert
      x-ballerina-event-type: "code_scanning_alert_reopened_by_user"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/CodeScanningAlertPayload'
    CodeScanningAlertUpdatedAssignmentMessage:
      name: code_scanning_alert
      title: Code Scanning Alert Updated Assignment
      summary: Activity relating to a code scanning alert
      x-ballerina-event-type: "code_scanning_alert_updated_assignment"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/CodeScanningAlertPayload'
    DeployKeyCreatedMessage:
      name: deploy_key
      title: Deploy Key Created
      summary: A deploy key was created or deleted
      x-ballerina-event-type: "deploy_key_created"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/DeployKeyPayload'
    DeployKeyDeletedMessage:
      name: deploy_key
      title: Deploy Key Deleted
      summary: A deploy key was created or deleted
      x-ballerina-event-type: "deploy_key_deleted"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/DeployKeyPayload'
    GithubAppAuthorizationMessage:
      name: github_app_authorization
      title: GitHub App Authorization Event
      summary: A user revoked their GitHub App authorization
      x-ballerina-event-type: "github_app_authorization_revoked"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/GithubAppAuthorizationPayload'

    MetaMessage:
      name: meta
      title: Meta Event
      summary: A webhook was deleted
      x-ballerina-event-type: "meta_deleted"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/MetaPayload'

    OrgBlockBlockedMessage:
      name: org_block
      title: Org Block Blocked
      summary: A non-member was blocked or unblocked from the organization
      x-ballerina-event-type: "org_block_blocked"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/OrgBlockPayload'
    OrgBlockUnblockedMessage:
      name: org_block
      title: Org Block Unblocked
      summary: A non-member was blocked or unblocked from the organization
      x-ballerina-event-type: "org_block_unblocked"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/OrgBlockPayload'
    PackagePublishedMessage:
      name: package
      title: Package Published
      summary: A GitHub Package was published or updated
      x-ballerina-event-type: "package_published"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/PackagePayload'
    PackageUpdatedMessage:
      name: package
      title: Package Updated
      summary: A GitHub Package was published or updated
      x-ballerina-event-type: "package_updated"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/PackagePayload'
    ProjectsV2ClosedMessage:
      name: projects_v2
      title: Projects V2 Closed
      summary: Activity relating to an organization-level project (Projects v2)
      x-ballerina-event-type: "projects_v2_closed"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/ProjectsV2Payload'
    ProjectsV2CreatedMessage:
      name: projects_v2
      title: Projects V2 Created
      summary: Activity relating to an organization-level project (Projects v2)
      x-ballerina-event-type: "projects_v2_created"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/ProjectsV2Payload'
    ProjectsV2DeletedMessage:
      name: projects_v2
      title: Projects V2 Deleted
      summary: Activity relating to an organization-level project (Projects v2)
      x-ballerina-event-type: "projects_v2_deleted"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/ProjectsV2Payload'
    ProjectsV2EditedMessage:
      name: projects_v2
      title: Projects V2 Edited
      summary: Activity relating to an organization-level project (Projects v2)
      x-ballerina-event-type: "projects_v2_edited"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/ProjectsV2Payload'
    ProjectsV2ReopenedMessage:
      name: projects_v2
      title: Projects V2 Reopened
      summary: Activity relating to an organization-level project (Projects v2)
      x-ballerina-event-type: "projects_v2_reopened"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/ProjectsV2Payload'
    RepositoryAdvisoryPublishedMessage:
      name: repository_advisory
      title: Repository Advisory Published
      summary: Activity relating to a repository security advisory
      x-ballerina-event-type: "repository_advisory_published"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/RepositoryAdvisoryPayload'
    RepositoryAdvisoryReportedMessage:
      name: repository_advisory
      title: Repository Advisory Reported
      summary: Activity relating to a repository security advisory
      x-ballerina-event-type: "repository_advisory_reported"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/RepositoryAdvisoryPayload'
    SecurityAdvisoryPublishedMessage:
      name: security_advisory
      title: Security Advisory Published
      summary: Activity relating to a GitHub-reviewed global security advisory
      x-ballerina-event-type: "security_advisory_published"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/SecurityAdvisoryPayload'
    SecurityAdvisoryUpdatedMessage:
      name: security_advisory
      title: Security Advisory Updated
      summary: Activity relating to a GitHub-reviewed global security advisory
      x-ballerina-event-type: "security_advisory_updated"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/SecurityAdvisoryPayload'
    SecurityAdvisoryWithdrawnMessage:
      name: security_advisory
      title: Security Advisory Withdrawn
      summary: Activity relating to a GitHub-reviewed global security advisory
      x-ballerina-event-type: "security_advisory_withdrawn"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/SecurityAdvisoryPayload'
    CommitCommentMessage:
      name: commit_comment
      title: Commit Comment Event
      summary: Someone commented on a commit
      x-ballerina-event-type: "commit_comment_created"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/CommitCommentPayload'

    DependabotAlertAssigneesChangedMessage:
      name: dependabot_alert
      title: Dependabot Alert Assignees Changed
      summary: Activity relating to a Dependabot security alert
      x-ballerina-event-type: "dependabot_alert_assignees_changed"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/DependabotAlertPayload'
    DependabotAlertAutoDismissedMessage:
      name: dependabot_alert
      title: Dependabot Alert Auto Dismissed
      summary: Activity relating to a Dependabot security alert
      x-ballerina-event-type: "dependabot_alert_auto_dismissed"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/DependabotAlertPayload'
    DependabotAlertAutoReopenedMessage:
      name: dependabot_alert
      title: Dependabot Alert Auto Reopened
      summary: Activity relating to a Dependabot security alert
      x-ballerina-event-type: "dependabot_alert_auto_reopened"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/DependabotAlertPayload'
    DependabotAlertCreatedMessage:
      name: dependabot_alert
      title: Dependabot Alert Created
      summary: Activity relating to a Dependabot security alert
      x-ballerina-event-type: "dependabot_alert_created"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/DependabotAlertPayload'
    DependabotAlertDismissedMessage:
      name: dependabot_alert
      title: Dependabot Alert Dismissed
      summary: Activity relating to a Dependabot security alert
      x-ballerina-event-type: "dependabot_alert_dismissed"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/DependabotAlertPayload'
    DependabotAlertFixedMessage:
      name: dependabot_alert
      title: Dependabot Alert Fixed
      summary: Activity relating to a Dependabot security alert
      x-ballerina-event-type: "dependabot_alert_fixed"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/DependabotAlertPayload'
    DependabotAlertReintroducedMessage:
      name: dependabot_alert
      title: Dependabot Alert Reintroduced
      summary: Activity relating to a Dependabot security alert
      x-ballerina-event-type: "dependabot_alert_reintroduced"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/DependabotAlertPayload'
    DependabotAlertReopenedMessage:
      name: dependabot_alert
      title: Dependabot Alert Reopened
      summary: Activity relating to a Dependabot security alert
      x-ballerina-event-type: "dependabot_alert_reopened"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/DependabotAlertPayload'
    GollumMessage:
      name: gollum
      title: Gollum (Wiki) Event
      summary: A wiki page was created or updated
      x-ballerina-event-type: "gollum"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/GollumPayload'

    InstallationCreatedMessage:
      name: installation
      title: Installation Created
      summary: Activity relating to a GitHub App installation
      x-ballerina-event-type: "installation_created"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/InstallationPayload'
    InstallationDeletedMessage:
      name: installation
      title: Installation Deleted
      summary: Activity relating to a GitHub App installation
      x-ballerina-event-type: "installation_deleted"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/InstallationPayload'
    InstallationNewPermissionsAcceptedMessage:
      name: installation
      title: Installation New Permissions Accepted
      summary: Activity relating to a GitHub App installation
      x-ballerina-event-type: "installation_new_permissions_accepted"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/InstallationPayload'
    InstallationSuspendMessage:
      name: installation
      title: Installation Suspend
      summary: Activity relating to a GitHub App installation
      x-ballerina-event-type: "installation_suspend"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/InstallationPayload'
    InstallationUnsuspendMessage:
      name: installation
      title: Installation Unsuspend
      summary: Activity relating to a GitHub App installation
      x-ballerina-event-type: "installation_unsuspend"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/InstallationPayload'
    InstallationRepositoriesAddedMessage:
      name: installation_repositories
      title: Installation Repositories Added
      summary: The set of repositories a GitHub App installation can access changed
      x-ballerina-event-type: "installation_repositories_added"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/InstallationRepositoriesPayload'
    InstallationRepositoriesRemovedMessage:
      name: installation_repositories
      title: Installation Repositories Removed
      summary: The set of repositories a GitHub App installation can access changed
      x-ballerina-event-type: "installation_repositories_removed"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/InstallationRepositoriesPayload'
    MembershipAddedMessage:
      name: membership
      title: Membership Added
      summary: A user was added to or removed from a team
      x-ballerina-event-type: "membership_added"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/MembershipPayload'
    MembershipRemovedMessage:
      name: membership
      title: Membership Removed
      summary: A user was added to or removed from a team
      x-ballerina-event-type: "membership_removed"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/MembershipPayload'
    MergeGroupChecksRequestedMessage:
      name: merge_group
      title: Merge Group Checks Requested
      summary: Activity relating to a merge group in a merge queue
      x-ballerina-event-type: "merge_group_checks_requested"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/MergeGroupPayload'
    MergeGroupDestroyedMessage:
      name: merge_group
      title: Merge Group Destroyed
      summary: Activity relating to a merge group in a merge queue
      x-ballerina-event-type: "merge_group_destroyed"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/MergeGroupPayload'
    PublicMessage:
      name: public
      title: Public Event
      summary: A repository was made public
      x-ballerina-event-type: "public"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/PublicPayload'

    RepositoryDispatchMessage:
      name: repository_dispatch
      title: Repository Dispatch Event
      summary: A custom event was triggered via the repository dispatches endpoint
      x-ballerina-event-type: "repository_dispatch"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/RepositoryDispatchPayload'

    SecretScanningAlertAssignedMessage:
      name: secret_scanning_alert
      title: Secret Scanning Alert Assigned
      summary: Activity relating to a secret scanning alert
      x-ballerina-event-type: "secret_scanning_alert_assigned"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/SecretScanningAlertPayload'
    SecretScanningAlertCreatedMessage:
      name: secret_scanning_alert
      title: Secret Scanning Alert Created
      summary: Activity relating to a secret scanning alert
      x-ballerina-event-type: "secret_scanning_alert_created"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/SecretScanningAlertPayload'
    SecretScanningAlertPubliclyLeakedMessage:
      name: secret_scanning_alert
      title: Secret Scanning Alert Publicly Leaked
      summary: Activity relating to a secret scanning alert
      x-ballerina-event-type: "secret_scanning_alert_publicly_leaked"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/SecretScanningAlertPayload'
    SecretScanningAlertReopenedMessage:
      name: secret_scanning_alert
      title: Secret Scanning Alert Reopened
      summary: Activity relating to a secret scanning alert
      x-ballerina-event-type: "secret_scanning_alert_reopened"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/SecretScanningAlertPayload'
    SecretScanningAlertResolvedMessage:
      name: secret_scanning_alert
      title: Secret Scanning Alert Resolved
      summary: Activity relating to a secret scanning alert
      x-ballerina-event-type: "secret_scanning_alert_resolved"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/SecretScanningAlertPayload'
    SecretScanningAlertUnassignedMessage:
      name: secret_scanning_alert
      title: Secret Scanning Alert Unassigned
      summary: Activity relating to a secret scanning alert
      x-ballerina-event-type: "secret_scanning_alert_unassigned"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/SecretScanningAlertPayload'
    SecretScanningAlertValidatedMessage:
      name: secret_scanning_alert
      title: Secret Scanning Alert Validated
      summary: Activity relating to a secret scanning alert
      x-ballerina-event-type: "secret_scanning_alert_validated"
      headers:
        $ref: '#/components/schemas/WebhookHeaders'
      payload:
        $ref: '#/components/schemas/SecretScanningAlertPayload'
  schemas:

    # -----------------------------------------------------------------------
    # Delivery headers present on every GitHub webhook POST request
    # -----------------------------------------------------------------------
    WebhookHeaders:
      description: "HTTP headers sent with every GitHub webhook delivery."
      type: object
      required:
        - X-GitHub-Event
        - X-GitHub-Delivery
        - X-GitHub-Hook-ID
      properties:
        X-GitHub-Event:
          type: string
          description: The name of the event that triggered the delivery
          examples:
            - push
            - pull_request
            - issues
        X-GitHub-Delivery:
          type: string
          format: uuid
          description: A globally unique identifier (GUID) for this delivery
        X-GitHub-Hook-ID:
          type: integer
          description: The unique identifier of the webhook
        X-GitHub-Hook-Installation-Target-ID:
          type: integer
          description: The unique identifier of the resource where the webhook was created
        X-GitHub-Hook-Installation-Target-Type:
          type: string
          description: The type of resource where the webhook was created
          enum:
            - repository
            - organization
            - app
            - business
            - marketplace
        X-Hub-Signature:
          type: string
          description: |
            HMAC hex digest of the request body using SHA-1. Sent only when a
            webhook secret is configured. Use X-Hub-Signature-256 instead.
        X-Hub-Signature-256:
          type: string
          description: |
            HMAC hex digest of the request body using SHA-256. Sent only when
            a webhook secret is configured. Preferred over X-Hub-Signature.
        User-Agent:
          type: string
          description: Always has the prefix GitHub-Hookshot/
          examples:
            - GitHub-Hookshot/044aadd

    # -----------------------------------------------------------------------
    # Common reusable object schemas
    # -----------------------------------------------------------------------

    User:
      type: object
      description: A GitHub user
      required:
        - login
        - id
      properties:
        login:
          type: string
          description: The user's GitHub username
        id:
          type: integer
          description: The user's unique numeric identifier
        node_id:
          description: "The GraphQL node ID."
          type: string
        avatar_url:
          description: "The URL of the user's or organization's avatar image."
          type: string
          format: uri
        gravatar_id:
          description: "The user's Gravatar ID (legacy field, typically empty)."
          type: string
          nullable: true
        url:
          description: "The API URL of the resource."
          type: string
          format: uri
        html_url:
          description: "The URL of the resource on GitHub.com."
          type: string
          format: uri
        type:
          description: "The account type: User, Organization, or Bot."
          type: string
          enum:
            - User
            - Bot
            - Organization
        site_admin:
          description: "Whether the user is a GitHub site administrator."
          type: boolean

    Repository:
      type: object
      description: A repository on GitHub
      required:
        - id
        - name
        - full_name
        - owner
        - private
      properties:
        id:
          type: integer
          description: Unique identifier of the repository.
        node_id:
          type: string
          description: The GraphQL identifier of the repository.
        name:
          type: string
          description: The repository name
        full_name:
          type: string
          description: The full repository name including owner (e.g. octocat/Hello-World)
        owner:
          $ref: '#/components/schemas/User'
        private:
          type: boolean
          description: Whether the repository is private
        html_url:
          type: string
          format: uri
          description: The URL to view the repository on GitHub.com.
        description:
          type: string
          nullable: true
          description: The repository description.
        fork:
          type: boolean
          description: Whether the repository is a fork.
        url:
          type: string
          format: uri
          description: The URL to get more information about the repository from the GitHub API.
        homepage:
          description: "The repository's homepage URL, if set."
          type: string
          nullable: true
        language:
          description: "The repository's primary programming language."
          type: string
          nullable: true
        forks_count:
          description: "The number of forks of the repository."
          type: integer
        stargazers_count:
          description: "The number of users who have starred the repository."
          type: integer
        watchers_count:
          description: "The number of users watching the repository."
          type: integer
        size:
          type: integer
          description: The size of the repository, in kilobytes. Size is calculated hourly. When a repository is initially created, the size is 0.
        default_branch:
          type: string
          description: The default branch of the repository.
        open_issues_count:
          description: "The number of open issues and pull requests in the repository."
          type: integer
        topics:
          description: "The list of topics associated with the repository."
          type: array
          items:
            type: string
        has_issues:
          type: boolean
          description: Whether issues are enabled.
        has_projects:
          type: boolean
          description: Whether projects are enabled.
        has_wiki:
          type: boolean
          description: Whether the wiki is enabled.
        has_pages:
          description: "Whether GitHub Pages is enabled for the repository."
          type: boolean
        has_downloads:
          type: boolean
          description: Whether downloads are enabled.
        archived:
          type: boolean
          description: Whether the repository is archived.
        disabled:
          type: boolean
          description: Returns whether or not this repository is disabled.
        visibility:
          type: string
          enum:
            - public
            - private
            - internal
          description: "The repository visibility: public, private, or internal."
        pushed_at:
          description: "The date and time the repository was last pushed to."
          type: string
          format: date-time
          nullable: true
        created_at:
          description: "The date and time the resource was created, in ISO 8601 format."
          type: string
          format: date-time
        updated_at:
          description: "The date and time the resource was last updated, in ISO 8601 format."
          type: string
          format: date-time
        license:
          description: "A repository's software license."
          type: object
          nullable: true
          properties:
            key:
              description: "The SPDX license key identifier, e.g. mit."
              type: string
            name:
              description: "The full name of the license."
              type: string
            spdx_id:
              description: "The SPDX identifier of the license."
              type: string
            url:
              description: "The API URL of the resource."
              type: string
              nullable: true

    Organization:
      type: object
      description: A GitHub organization
      properties:
        login:
          description: "The organization's login name."
          type: string
        id:
          description: "The unique identifier of the resource."
          type: integer
        node_id:
          description: "The GraphQL node ID."
          type: string
        url:
          description: "The API URL of the resource."
          type: string
          format: uri
        html_url:
          description: "The URL of the resource on GitHub.com."
          type: string
          format: uri
        repos_url:
          description: "The API URL listing the organization's repositories."
          type: string
          format: uri
        avatar_url:
          description: "The URL of the user's or organization's avatar image."
          type: string
          format: uri
        description:
          description: "A short description of the organization."
          type: string
          nullable: true

    Installation:
      type: object
      description: A GitHub App installation
      properties:
        id:
          type: integer
          description: The ID of the installation.
        node_id:
          description: "The GraphQL node ID."
          type: string

    Enterprise:
      type: object
      description: A GitHub Enterprise account
      properties:
        id:
          type: integer
          description: Unique identifier of the enterprise.
        slug:
          type: string
          description: The slug url identifier for the enterprise.
        name:
          type: string
          description: The name of the enterprise.
        node_id:
          description: "The GraphQL node ID."
          type: string
        avatar_url:
          description: "The URL of the user's or organization's avatar image."
          type: string
          format: uri
        description:
          type: string
          nullable: true
          description: A short description of the enterprise.
        website_url:
          type: string
          nullable: true
          description: The enterprise's website URL.
        html_url:
          description: "The URL of the resource on GitHub.com."
          type: string
          format: uri
        created_at:
          description: "The date and time the resource was created, in ISO 8601 format."
          type: string
          format: date-time
        updated_at:
          description: "The date and time the resource was last updated, in ISO 8601 format."
          type: string
          format: date-time

    # Common payload fields present on most webhook events
    CommonPayload:
      description: "Shared sender/repository/organization/installation/enterprise fields; not referenced elsewhere in this module."
      type: object
      properties:
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    Label:
      type: object
      description: A label on an issue or pull request
      required:
        - id
        - name
        - color
      properties:
        id:
          type: integer
          description: Unique identifier for the label.
        node_id:
          description: "The GraphQL node ID."
          type: string
        url:
          type: string
          format: uri
          description: URL for the label.
        name:
          type: string
          description: The name of the label.
        color:
          type: string
          description: 6-character hex color code
        default:
          type: boolean
          description: Whether this label comes by default in a new repository.
        description:
          type: string
          nullable: true

          description: Optional description of the label, such as its purpose.
    Milestone:
      type: object
      nullable: true
      description: A milestone on an issue or pull request
      properties:
        id:
          description: "The unique identifier of the resource."
          type: integer
        node_id:
          description: "The GraphQL node ID."
          type: string
        number:
          type: integer
          description: The number of the milestone.
        title:
          type: string
          description: The title of the milestone.
        description:
          description: "A description of the milestone."
          type: string
          nullable: true
        state:
          type: string
          enum:
            - open
            - closed
          description: The state of the milestone.
        open_issues:
          description: "The number of open issues associated with the milestone."
          type: integer
        closed_issues:
          description: "The number of closed issues associated with the milestone."
          type: integer
        created_at:
          description: "The date and time the resource was created, in ISO 8601 format."
          type: string
          format: date-time
        updated_at:
          description: "The date and time the resource was last updated, in ISO 8601 format."
          type: string
          format: date-time
        due_on:
          description: "The date the milestone is due, in ISO 8601 format."
          type: string
          format: date-time
          nullable: true
        closed_at:
          description: "The date and time the milestone was closed, in ISO 8601 format."
          type: string
          format: date-time
          nullable: true
        creator:
          $ref: '#/components/schemas/User'

    Issue:
      type: object
      description: An issue on GitHub
      required:
        - id
        - number
        - title
        - state
      properties:
        id:
          description: "The unique identifier of the resource."
          type: integer
        node_id:
          description: "The GraphQL node ID."
          type: string
        url:
          type: string
          format: uri
          description: URL for the issue.
        html_url:
          description: "The URL of the resource on GitHub.com."
          type: string
          format: uri
        number:
          type: integer
          description: Number uniquely identifying the issue within its repository.
        title:
          type: string
          description: Title of the issue.
        body:
          type: string
          nullable: true
          description: Contents of the issue.
        state:
          type: string
          enum:
            - open
            - closed
          description: State of the issue; either `open` or `closed`.
        locked:
          description: "Whether the issue's conversation has been locked."
          type: boolean
        user:
          $ref: '#/components/schemas/User'
        labels:
          description: "Labels attached to the issue."
          type: array
          items:
            $ref: '#/components/schemas/Label'
        assignee:
          $ref: '#/components/schemas/User'
          nullable: true
        assignees:
          description: "The users assigned to the issue."
          type: array
          items:
            $ref: '#/components/schemas/User'
        milestone:
          $ref: '#/components/schemas/Milestone'
        comments:
          description: "The number of comments on the issue."
          type: integer
        created_at:
          description: "The date and time the resource was created, in ISO 8601 format."
          type: string
          format: date-time
        updated_at:
          description: "The date and time the resource was last updated, in ISO 8601 format."
          type: string
          format: date-time
        closed_at:
          description: "The date and time the issue was closed, if closed."
          type: string
          format: date-time
          nullable: true
        author_association:
          type: string
          enum:
            - COLLABORATOR
            - CONTRIBUTOR
            - FIRST_TIMER
            - FIRST_TIME_CONTRIBUTOR
            - MANNEQUIN
            - MEMBER
            - NONE
            - OWNER
          description: How the author is associated with the repository.
        active_lock_reason:
          description: "The reason the issue's conversation was locked, if locked."
          type: string
          nullable: true

    PullRequestRef:
      type: object
      description: A pull request head or base ref
      properties:
        label:
          description: "The ref's label in owner:branch form, e.g. octocat:main."
          type: string
        ref:
          description: "The name of the branch or tag."
          type: string
        sha:
          description: "The SHA of the commit at the tip of the ref."
          type: string
        user:
          $ref: '#/components/schemas/User'
        repo:
          $ref: '#/components/schemas/Repository'

    PullRequest:
      type: object
      description: A pull request
      required:
        - id
        - number
        - state
        - title
      properties:
        id:
          description: "The unique identifier of the resource."
          type: integer
        node_id:
          description: "The GraphQL node ID."
          type: string
        url:
          description: "The API URL of the resource."
          type: string
          format: uri
        html_url:
          description: "The URL of the resource on GitHub.com."
          type: string
          format: uri
        diff_url:
          description: "The URL to the pull request's diff in unified diff format."
          type: string
          format: uri
        patch_url:
          description: "The URL to the pull request's patch file."
          type: string
          format: uri
        number:
          type: integer
          description: Number uniquely identifying the pull request within its repository.
        state:
          type: string
          enum:
            - open
            - closed
          description: State of this Pull Request. Either `open` or `closed`.
        locked:
          description: "Whether the pull request's conversation is locked."
          type: boolean
        title:
          type: string
          description: The title of the pull request.
        body:
          description: "The pull request's description text."
          type: string
          nullable: true
        user:
          $ref: '#/components/schemas/User'
        labels:
          description: "The labels attached to the pull request."
          type: array
          items:
            $ref: '#/components/schemas/Label'
        assignee:
          $ref: '#/components/schemas/User'
          nullable: true
        assignees:
          description: "The users assigned to the pull request."
          type: array
          items:
            $ref: '#/components/schemas/User'
        milestone:
          $ref: '#/components/schemas/Milestone'
        head:
          $ref: '#/components/schemas/PullRequestRef'
        base:
          $ref: '#/components/schemas/PullRequestRef'
        draft:
          type: boolean
          description: Indicates whether or not the pull request is a draft.
        merged:
          description: "Whether the pull request has been merged."
          type: boolean
          nullable: true
        mergeable:
          description: "Whether the pull request can be merged into its base branch."
          type: boolean
          nullable: true
        rebaseable:
          description: "Whether the pull request can be rebased against its base branch."
          type: boolean
          nullable: true
        mergeable_state:
          description: "The mergeability state of the pull request, e.g. clean or dirty."
          type: string
        merge_commit_sha:
          description: "The SHA of the merge commit, or the last commit if not yet merged."
          type: string
          nullable: true
        comments:
          description: "The number of issue comments on the pull request."
          type: integer
        review_comments:
          description: "The number of review comments on the pull request."
          type: integer
        commits:
          description: "The number of commits in the pull request."
          type: integer
        additions:
          description: "The number of lines added across the pull request's commits."
          type: integer
        deletions:
          description: "The number of lines deleted across the pull request's commits."
          type: integer
        changed_files:
          description: "The number of files changed in the pull request."
          type: integer
        created_at:
          description: "The date and time the resource was created, in ISO 8601 format."
          type: string
          format: date-time
        updated_at:
          description: "The date and time the resource was last updated, in ISO 8601 format."
          type: string
          format: date-time
        closed_at:
          description: "The date and time the pull request was closed, in ISO 8601 format."
          type: string
          format: date-time
          nullable: true
        merged_at:
          description: "The date and time the pull request was merged, in ISO 8601 format."
          type: string
          format: date-time
          nullable: true
        merged_by:
          $ref: '#/components/schemas/User'
          nullable: true
        author_association:
          type: string
          description: How the author is associated with the repository.
          enum:
            - COLLABORATOR
            - CONTRIBUTOR
            - FIRST_TIMER
            - FIRST_TIME_CONTRIBUTOR
            - MANNEQUIN
            - MEMBER
            - NONE
            - OWNER
        auto_merge:
          type: object
          nullable: true
          description: Details of an auto-merge request, if one is enabled on this pull request
          properties:
            enabled_by:
              $ref: '#/components/schemas/User'
            merge_method:
              type: string
              enum:
                - merge
                - squash
                - rebase
              description: The merge method to use.
            commit_title:
              type: string
              description: Title for the merge commit message.
            commit_message:
              type: string

              description: Commit message for the merge commit.
    Release:
      type: object
      description: A GitHub release
      required:
        - id
        - tag_name
        - name
      properties:
        id:
          description: "The unique identifier of the resource."
          type: integer
        node_id:
          description: "The GraphQL node ID."
          type: string
        url:
          description: "The API URL of the resource."
          type: string
          format: uri
        html_url:
          description: "The URL of the resource on GitHub.com."
          type: string
          format: uri
        assets_url:
          description: "The API URL listing the release's assets."
          type: string
          format: uri
        upload_url:
          description: "The URI template for uploading assets to this release."
          type: string
        tag_name:
          type: string
          description: The name of the tag.
        name:
          description: "The name of the release."
          type: string
          nullable: true
        body:
          description: "Text describing the contents of the tag."
          type: string
          nullable: true
        draft:
          type: boolean
          description: Whether the release is a draft or published.
        prerelease:
          type: boolean
          description: Whether the release is identified as a prerelease or a full release.
        target_commitish:
          type: string
          description: Specifies the commitish value that determines where the Git tag is created from.
        author:
          $ref: '#/components/schemas/User'
        assets:
          description: "The release's downloadable file attachments."
          type: array
          items:
            type: object
            description: A file attached to a release
            required:
              - id
              - node_id
              - name
              - content_type
              - state
              - size
              - download_count
              - created_at
              - updated_at
              - url
              - browser_download_url
            properties:
              url:
                description: "The API URL of the resource."
                type: string
                format: uri
              browser_download_url:
                description: "The direct download URL for the asset file."
                type: string
                format: uri
              id:
                description: "The unique identifier of the resource."
                type: integer
              node_id:
                description: "The GraphQL node ID."
                type: string
              name:
                type: string
                description: The file name of the asset.
              label:
                description: "The user-facing display label for the asset, if set."
                type: string
                nullable: true
              state:
                type: string
                enum:
                  - uploaded
                  - open
                description: State of the release asset.
              content_type:
                description: "The MIME type of the asset file."
                type: string
              size:
                description: "The size of the asset file, in bytes."
                type: integer
              digest:
                description: "The checksum digest of the asset file, when available."
                type: string
                nullable: true
              download_count:
                description: "The number of times the asset has been downloaded."
                type: integer
              created_at:
                description: "The date and time the resource was created, in ISO 8601 format."
                type: string
                format: date-time
              updated_at:
                description: "The date and time the resource was last updated, in ISO 8601 format."
                type: string
                format: date-time
              uploader:
                $ref: '#/components/schemas/User'
        created_at:
          description: "The date and time the resource was created, in ISO 8601 format."
          type: string
          format: date-time
        published_at:
          description: "The date and time the release was published, in ISO 8601 format."
          type: string
          format: date-time
          nullable: true

    CommitAuthor:
      type: object
      description: Git author/committer metadata
      properties:
        name:
          type: string
          description: The git author's name.
        email:
          type: string
          format: email
          description: The git author's email address.
        username:
          description: "The git author's GitHub username, if their email matched a GitHub account."
          type: string

    Commit:
      type: object
      description: A Git commit
      required:
        - id
        - message
      properties:
        id:
          type: string
          description: The commit SHA
        tree_id:
          description: "The SHA of the Git tree associated with this commit."
          type: string
        distinct:
          type: boolean
          description: Whether this commit is distinct from any that have been pushed before
        message:
          type: string
          description: The commit message.
        timestamp:
          type: string
          format: date-time
          description: The ISO 8601 timestamp of the commit.
        url:
          type: string
          format: uri
          description: URL that points to the commit API resource.
        author:
          $ref: '#/components/schemas/CommitAuthor'
        committer:
          $ref: '#/components/schemas/CommitAuthor'
        added:
          type: array
          description: Files added in this commit
          items:
            type: string
        removed:
          type: array
          description: Files removed in this commit
          items:
            type: string
        modified:
          type: array
          description: Files modified in this commit
          items:
            type: string

    IssueComment:
      type: object
      description: A comment on an issue or pull request
      required:
        - id
        - body
      properties:
        id:
          type: integer
          description: Unique identifier of the issue comment.
        node_id:
          description: "The GraphQL node ID."
          type: string
        url:
          type: string
          format: uri
          description: URL for the issue comment.
        html_url:
          description: "The URL of the resource on GitHub.com."
          type: string
          format: uri
        body:
          type: string
          description: Contents of the issue comment.
        user:
          $ref: '#/components/schemas/User'
        created_at:
          description: "The date and time the resource was created, in ISO 8601 format."
          type: string
          format: date-time
        updated_at:
          description: "The date and time the resource was last updated, in ISO 8601 format."
          type: string
          format: date-time
        author_association:
          type: string
          description: How the author is associated with the repository.

    PullRequestReview:
      type: object
      description: A pull request review
      required:
        - id
        - state
      properties:
        id:
          type: integer
          description: Unique identifier of the review.
        node_id:
          description: "The GraphQL node ID."
          type: string
        user:
          $ref: '#/components/schemas/User'
        body:
          type: string
          nullable: true
          description: The text of the review.
        state:
          description: "The review state, e.g. approved, changes_requested, commented, dismissed."
          type: string
          enum:
            - APPROVED
            - CHANGES_REQUESTED
            - COMMENTED
            - DISMISSED
            - PENDING
        html_url:
          description: "The URL of the resource on GitHub.com."
          type: string
          format: uri
        pull_request_url:
          description: "The API URL of the reviewed pull request."
          type: string
          format: uri
        submitted_at:
          description: "The date and time the review was submitted, in ISO 8601 format."
          type: string
          format: date-time
        commit_id:
          type: string
          description: A commit SHA for the review. If the commit object was garbage collected or forcibly deleted, then it no longer exists in Git and this value will be `null`.
        author_association:
          type: string
          description: How the author is associated with the repository.

    PullRequestReviewComment:
      type: object
      description: A comment on a pull request diff
      required:
        - id
        - body
      properties:
        id:
          type: integer
          description: The ID of the pull request review comment.
        node_id:
          type: string
          description: The node ID of the pull request review comment.
        pull_request_review_id:
          type: integer
          nullable: true
          description: The ID of the pull request review to which the comment belongs.
        url:
          type: string
          format: uri
          description: URL for the pull request review comment.
        html_url:
          type: string
          format: uri
          description: HTML URL for the pull request review comment.
        body:
          type: string
          description: The text of the comment.
        diff_hunk:
          type: string
          description: The diff of the line that the comment refers to.
        path:
          type: string
          description: The relative path of the file to which the comment applies.
        position:
          type: integer
          nullable: true
          description: The line index in the diff to which the comment applies. This field is closing down; use `line` instead.
        original_position:
          type: integer
          description: The index of the original line in the diff to which the comment applies. This field is closing down; use `original_line` instead.
        commit_id:
          type: string
          description: The SHA of the commit to which the comment applies.
        original_commit_id:
          type: string
          description: The SHA of the original commit to which the comment applies.
        user:
          $ref: '#/components/schemas/User'
        created_at:
          description: "The date and time the resource was created, in ISO 8601 format."
          type: string
          format: date-time
        updated_at:
          description: "The date and time the resource was last updated, in ISO 8601 format."
          type: string
          format: date-time
        author_association:
          type: string
          description: How the author is associated with the repository.
        side:
          type: string
          enum:
            - LEFT
            - RIGHT
          description: The side of the diff to which the comment applies. The side of the last line of the range for a multi-line comment.
        start_side:
          type: string
          enum:
            - LEFT
            - RIGHT
          nullable: true

          description: The side of the first line of the range for a multi-line comment.
    CheckRun:
      type: object
      description: A check performed on the code of a given code change
      required:
        - id
        - name
      properties:
        id:
          type: integer
          description: The id of the check.
        name:
          type: string
          description: The name of the check run.
        node_id:
          description: "The GraphQL node ID."
          type: string
        head_sha:
          type: string
          description: The SHA of the commit that is being checked.
        external_id:
          description: "A reference for the check run on the integrator's system."
          type: string
          nullable: true
        url:
          description: "The API URL of the resource."
          type: string
          format: uri
        html_url:
          description: "The URL of the resource on GitHub.com."
          type: string
          format: uri
        details_url:
          description: "The URL of the integrator's site with the full details of the check."
          type: string
          format: uri
          nullable: true
        status:
          type: string
          enum:
            - queued
            - in_progress
            - completed
            - waiting
            - pending
          description: The phase of the lifecycle that the check is currently in. Statuses of waiting, requested, and pending are reserved for GitHub Actions check runs.
        conclusion:
          type: string
          nullable: true
          enum:
            - action_required
            - cancelled
            - failure
            - neutral
            - skipped
            - stale
            - startup_failure
            - success
            - timed_out
            - 
          description: The result of the completed check run. This value will be `null` until the check run has completed.
        started_at:
          type: string
          format: date-time
          nullable: true
          description: The time that the check run began.
        completed_at:
          type: string
          format: date-time
          nullable: true
          description: The time the check completed.
        output:
          description: "The check run's title, summary, and detailed output text."
          type: object
          properties:
            title:
              description: "The title of the check run output, or null if not set."
              type: string
              nullable: true
            summary:
              description: "The summary of the check run output, or null if not set."
              type: string
              nullable: true
            text:
              description: "The detailed text of the check run output, or null if not set."
              type: string
              nullable: true
            annotations_count:
              description: "The number of annotations included in the output."
              type: integer
            annotations_url:
              description: "The API URL listing the annotations for this check run."
              type: string
              format: uri
        check_suite:
          description: "The check suite this check run belongs to."
          type: object
          properties:
            id:
              description: "The unique identifier of the resource."
              type: integer
        app:
          $ref: '#/components/schemas/App'
        pull_requests:
          type: array
          items:
            $ref: '#/components/schemas/PullRequestMinimal'

          description: Pull requests that are open with a `head_sha` or `head_branch` that matches the check. The returned pull requests do not necessarily indicate pull requests that triggered the check.
    CheckSuite:
      type: object
      description: A check suite
      required:
        - id
      properties:
        id:
          description: "The unique identifier of the resource."
          type: integer
        node_id:
          description: "The GraphQL node ID."
          type: string
        head_branch:
          type: string
          nullable: true
          description: The head branch name the changes are on.
        head_sha:
          type: string
          description: The SHA of the head commit that is being checked.
        status:
          type: string
          enum:
            - queued
            - in_progress
            - completed
          description: The summary status for all check runs that are part of the check suite. Can be `queued`, `requested`, `in_progress`, or `completed`.
        conclusion:
          type: string
          nullable: true
          enum:
            - action_required
            - cancelled
            - failure
            - neutral
            - skipped
            - stale
            - success
            - timed_out
            - 
          description: The summary conclusion for all check runs that are part of the check suite. This value will be `null` until the check suite has `completed`.
        url:
          type: string
          format: uri
          description: URL that points to the check suite API resource.
        before:
          description: "The SHA of the most recent commit on the branch before the push that triggered this check suite."
          type: string
          nullable: true
        after:
          description: "The SHA of the most recent commit on the branch after this check suite's push."
          type: string
          nullable: true
        pull_requests:
          type: array
          items:
            $ref: '#/components/schemas/PullRequestMinimal'
          description: An array of pull requests that match this check suite. A pull request matches a check suite if they have the same `head_sha` and `head_branch`. When the check suite's `head_branch` is in a forked repository it will be `null` and the `pull_requests` array will be empty.
        app:
          $ref: '#/components/schemas/App'
        created_at:
          description: "The date and time the resource was created, in ISO 8601 format."
          type: string
          format: date-time
        updated_at:
          description: "The date and time the resource was last updated, in ISO 8601 format."
          type: string
          format: date-time

    Deployment:
      type: object
      description: A deployment request for a specific ref
      required:
        - id
        - ref
        - sha
        - task
        - environment
      properties:
        id:
          type: integer
          description: Unique identifier of the deployment.
        node_id:
          description: "The GraphQL node ID."
          type: string
        sha:
          description: "The SHA of the commit that was deployed."
          type: string
        ref:
          type: string
          description: The ref to deploy. This can be a branch, tag, or sha.
        task:
          type: string
          description: Parameter to specify a task to execute.
        payload:
          type: object
          additionalProperties: true
          description: JSON payload with extra information about the deployment
        original_environment:
          description: "The environment name originally requested, before normalization to `environment`."
          type: string
        environment:
          type: string
          description: Name of the target deployment environment.
        description:
          description: "A short description of the deployment."
          type: string
          nullable: true
        creator:
          $ref: '#/components/schemas/User'
        created_at:
          description: "The date and time the resource was created, in ISO 8601 format."
          type: string
          format: date-time
        updated_at:
          description: "The date and time the resource was last updated, in ISO 8601 format."
          type: string
          format: date-time
        statuses_url:
          description: "The API URL listing the deployment's statuses."
          type: string
          format: uri
        repository_url:
          description: "The API URL of the repository the deployment belongs to."
          type: string
          format: uri
        transient_environment:
          type: boolean
          description: "Specifies if the given environment will no longer exist at some point in the future. Default: false."
        production_environment:
          type: boolean
          description: "Specifies if the given environment is one that end-users directly interact with. Default: false."
        performed_via_github_app:
          $ref: '#/components/schemas/App'
          nullable: true

    DeploymentStatus:
      type: object
      description: A deployment status
      required:
        - id
        - state
      properties:
        id:
          description: "The unique identifier of the resource."
          type: integer
        node_id:
          description: "The GraphQL node ID."
          type: string
        state:
          type: string
          enum:
            - error
            - failure
            - inactive
            - in_progress
            - queued
            - pending
            - success
            - waiting
          description: The new state. Can be `pending`, `success`, `failure`, or `error`.
        creator:
          $ref: '#/components/schemas/User'
        description:
          type: string
          nullable: true
          description: The optional human-readable description added to the status.
        environment:
          type: string
          description: The environment of the deployment that the status is for.
        environment_url:
          type: string
          nullable: true
          description: The URL for accessing your environment.
        log_url:
          type: string
          nullable: true
          description: The URL to associate with this status.
        target_url:
          type: string
          nullable: true
          description: The optional link added to the status.
        deployment_url:
          description: "The API URL of the deployment this status belongs to."
          type: string
          format: uri
        repository_url:
          description: "The API URL of the repository the deployment belongs to."
          type: string
          format: uri
        created_at:
          description: "The date and time the resource was created, in ISO 8601 format."
          type: string
          format: date-time
        updated_at:
          description: "The date and time the resource was last updated, in ISO 8601 format."
          type: string
          format: date-time
        performed_via_github_app:
          $ref: '#/components/schemas/App'
          nullable: true

    Workflow:
      type: object
      description: A GitHub Actions workflow
      properties:
        id:
          description: "The unique identifier of the resource."
          type: integer
        node_id:
          description: "The GraphQL node ID."
          type: string
        name:
          description: "The name of the workflow."
          type: string
        path:
          description: "The path of the workflow file relative to the repository root."
          type: string
        state:
          description: "The state of the workflow, e.g. active or disabled_manually."
          type: string
        created_at:
          description: "The date and time the resource was created, in ISO 8601 format."
          type: string
          format: date-time
        updated_at:
          description: "The date and time the resource was last updated, in ISO 8601 format."
          type: string
          format: date-time
        url:
          description: "The API URL of the resource."
          type: string
          format: uri
        html_url:
          description: "The URL of the resource on GitHub.com."
          type: string
          format: uri
        badge_url:
          description: "The URL of the workflow's status badge image."
          type: string
          format: uri

    WorkflowRun:
      type: object
      description: A GitHub Actions workflow run
      required:
        - id
        - name
        - status
      properties:
        id:
          type: integer
          description: The ID of the workflow run.
        name:
          type: string
          description: The name of the workflow run.
        node_id:
          description: "The GraphQL node ID."
          type: string
        check_suite_id:
          type: integer
          description: The ID of the associated check suite.
        check_suite_node_id:
          type: string
          description: The node ID of the associated check suite.
        head_branch:
          description: "The name of the branch the workflow run is on."
          type: string
          nullable: true
        head_sha:
          type: string
          description: The SHA of the head commit that points to the version of the workflow being run.
        run_number:
          type: integer
          description: The auto incrementing run number for the workflow run.
        event:
          description: "The event that triggered the workflow run, e.g. push, pull_request."
          type: string
        status:
          description: "The status of the workflow run, e.g. queued, in_progress, completed."
          type: string
          enum:
            - requested
            - in_progress
            - completed
            - queued
            - waiting
            - pending
        conclusion:
          description: "The result of the completed workflow run, e.g. success, failure, cancelled."
          type: string
          nullable: true
          enum:
            - action_required
            - cancelled
            - failure
            - neutral
            - skipped
            - stale
            - success
            - timed_out
            - startup_failure
            - 
        workflow_id:
          type: integer
          description: The ID of the parent workflow.
        url:
          type: string
          format: uri
          description: The URL to the workflow run.
        html_url:
          description: "The URL of the resource on GitHub.com."
          type: string
          format: uri
        pull_requests:
          type: array
          items:
            $ref: '#/components/schemas/PullRequestMinimal'
          description: Pull requests that are open with a `head_sha` or `head_branch` that matches the workflow run. The returned pull requests do not necessarily indicate pull requests that triggered the run.
        created_at:
          description: "The date and time the resource was created, in ISO 8601 format."
          type: string
          format: date-time
        updated_at:
          description: "The date and time the resource was last updated, in ISO 8601 format."
          type: string
          format: date-time
        run_attempt:
          type: integer
          description: Attempt number of the run, 1 for first attempt and higher if the workflow was re-run.
        run_started_at:
          type: string
          format: date-time
          description: The start time of the latest run. Resets on re-run.
        actor:
          $ref: '#/components/schemas/User'
        triggering_actor:
          $ref: '#/components/schemas/User'
        jobs_url:
          type: string
          format: uri
          description: The URL to the jobs for the workflow run.
        logs_url:
          type: string
          format: uri
          description: The URL to download the logs for the workflow run.
        check_suite_url:
          type: string
          format: uri
          description: The URL to the associated check suite.
        artifacts_url:
          type: string
          format: uri
          description: The URL to the artifacts for the workflow run.
        cancel_url:
          type: string
          format: uri
          description: The URL to cancel the workflow run.
        rerun_url:
          type: string
          format: uri
          description: The URL to rerun the workflow run.
        workflow_url:
          type: string
          format: uri
          description: The URL to the workflow.
        head_commit:
          $ref: '#/components/schemas/Commit'
        repository:
          $ref: '#/components/schemas/Repository'

    Package:
      type: object
      description: The ecosystem and name identifying a Dependabot-tracked package
      properties:
        ecosystem:
          type: string
          description: The package's language or package management ecosystem.
        name:
          type: string

          description: The unique package name within its ecosystem.
    SecurityVulnerability:
      type: object
      description: A vulnerable version range affecting a package, and the version it was patched in
      properties:
        package:
          $ref: '#/components/schemas/Package'
        severity:
          type: string
          description: The severity of the vulnerability.
        vulnerable_version_range:
          type: string
          description: Conditions that identify vulnerable versions of this vulnerability's package.
        first_patched_version:
          type: object
          nullable: true
          properties:
            identifier:
              type: string

              description: The package version that patches this vulnerability.
          description: Details pertaining to the package version that patches this vulnerability.
    Tier:
      type: object
      description: A GitHub Sponsors tier
      required:
        - node_id
        - name
        - monthly_price_in_cents
        - monthly_price_in_dollars
      properties:
        node_id:
          description: "The GraphQL node ID."
          type: string
        created_at:
          description: "The date and time the resource was created, in ISO 8601 format."
          type: string
          format: date-time
        description:
          description: "A description of the sponsorship tier."
          type: string
        monthly_price_in_cents:
          description: "The monthly price of the tier, in cents."
          type: integer
        monthly_price_in_dollars:
          description: "The monthly price of the tier, in dollars."
          type: integer
        name:
          description: "The name of the sponsorship tier."
          type: string
        is_one_time:
          description: "Whether this is a one-time, non-recurring sponsorship tier."
          type: boolean
        is_custom_amount:
          description: "Whether the sponsor can choose a custom amount for this tier."
          type: boolean

    MarketplacePurchase:
      type: object
      description: The GitHub Marketplace purchase
      required:
        - account
        - billing_cycle
        - unit_count
        - plan
      properties:
        account:
          description: "The GitHub account (user or organization) that made the Marketplace purchase."
          type: object
          required:
            - type
            - id
            - login
          properties:
            type:
              description: "The type of account, either User or Organization."
              type: string
              enum:
                - User
                - Organization
            id:
              description: "The unique identifier of the resource."
              type: integer
            node_id:
              description: "The GraphQL node ID."
              type: string
            login:
              description: "The username or organization login of the account."
              type: string
            organization_billing_email:
              description: "The organization's billing email address, if applicable."
              type: string
              nullable: true
        billing_cycle:
          description: "The billing cycle of the plan, e.g. monthly or yearly."
          type: string
          enum:
            - monthly
            - yearly
        unit_count:
          description: "The number of units purchased on the plan."
          type: integer
        on_free_trial:
          description: "Whether the account is currently on a free trial."
          type: boolean
        free_trial_ends_on:
          description: "The date the free trial ends, in ISO 8601 format."
          type: string
          format: date-time
          nullable: true
        next_billing_date:
          description: "The date of the next billing charge, in ISO 8601 format."
          type: string
          format: date-time
          nullable: true
        plan:
          description: "The Marketplace plan associated with the purchase."
          type: object
          required:
            - id
            - name
            - description
            - monthly_price_in_cents
            - yearly_price_in_cents
            - price_model
          properties:
            id:
              description: "The unique identifier of the resource."
              type: integer
            name:
              description: "The name of the Marketplace plan."
              type: string
            description:
              description: "The description of the Marketplace plan."
              type: string
            monthly_price_in_cents:
              description: "The monthly price of the plan, in cents."
              type: integer
            yearly_price_in_cents:
              description: "The yearly price of the plan, in cents."
              type: integer
            price_model:
              description: "The pricing model of the plan, e.g. FLAT_RATE or PER_UNIT."
              type: string
              enum:
                - FREE
                - FLAT_RATE
                - PER_UNIT
            has_free_trial:
              description: "Whether the plan offers a free trial."
              type: boolean
            unit_name:
              description: "The name of the unit used to calculate pricing, e.g. seat."
              type: string
              nullable: true
            bullets:
              description: "The list of feature bullet points describing the plan."
              type: array
              items:
                type: string

    RulePatternParameters:
      type: object
      description: |
        Shared by the 5 pattern-matching ruleset rules (commit_message_pattern,
        commit_author_email_pattern, committer_email_pattern, branch_name_pattern,
        tag_name_pattern) - identical parameters shape for all five per GitHub's own schema.
      required:
        - operator
        - pattern
      properties:
        name:
          type: string
          description: How this rule appears when configuring it.
        negate:
          type: boolean
          description: If true, the rule will fail if the pattern matches.
        operator:
          type: string
          enum:
            - starts_with
            - ends_with
            - contains
            - regex
          description: The operator to use for matching.
        pattern:
          type: string

          description: The pattern to match with.
    RuleActor:
      type: object
      description: An actor allowed to dismiss pull request reviews
      required:
        - id
        - type
      properties:
        id:
          type: integer
          description: The ID of the actor that can bypass the rule.
        type:
          type: string
          description: The type of actor that can bypass the rule.
          enum:
            - User
            - Team
            - IntegrationInstallation
            - RepositoryRole

    RuleReviewer:
      type: object
      description: A required reviewing team
      required:
        - id
        - type
      properties:
        id:
          type: integer
          description: The ID of the team required to review matching pull requests.
        type:
          type: string
          description: The type of actor required to review - always `Team`.
          enum:
            - Team

    RuleDismissalRestriction:
      type: object
      description: Specify people, teams, or apps allowed to dismiss pull request reviews.
      required:
        - enabled
      properties:
        allowed_actors:
          type: array
          items:
            $ref: '#/components/schemas/RuleActor'
          description: Specify people, teams, or apps allowed to dismiss pull request reviews.
        enabled:
          type: boolean

          description: Whether to restrict review dismissal to specific actors.
    RuleRequiredReviewerConfiguration:
      type: object
      description: A reviewing team, and file patterns describing which files they must approve changes to.
      required:
        - file_patterns
        - minimum_approvals
        - reviewer
      properties:
        file_patterns:
          type: array
          items:
            type: string
          description: The file patterns the required reviewer must approve changes to.
        minimum_approvals:
          type: integer
          description: The number of approvals required from the reviewing team.
        reviewer:
          $ref: '#/components/schemas/RuleReviewer'

    RuleStatusCheckConfiguration:
      type: object
      description: Required status check
      required:
        - context
      properties:
        context:
          type: string
          description: The status check context name that must pass for this rule to pass.
        integration_id:
          type: integer
          description: The ID of the GitHub App that must provide this status check.

    RuleWorkflowFileReference:
      type: object
      description: A workflow that must run for this rule to pass
      required:
        - path
        - repository_id
      properties:
        path:
          type: string
          description: The path to the workflow file.
        ref:
          type: string
          description: The ref (branch or tag) of the workflow file to use.
        repository_id:
          type: integer
          description: The ID of the repository where the workflow is defined.
        sha:
          type: string

          description: The commit SHA of the workflow file to use.
    RuleCodeScanningTool:
      type: object
      description: A tool that must provide code scanning results for this rule to pass.
      required:
        - alerts_threshold
        - security_alerts_threshold
        - tool
      properties:
        alerts_threshold:
          type: string
          enum:
            - none
            - errors
            - errors_and_warnings
            - all
          description: The severity level at which code scanning results that raise alerts block a reference update.
        security_alerts_threshold:
          type: string
          enum:
            - none
            - critical
            - high_or_higher
            - medium_or_higher
            - all
          description: The severity level at which code scanning results that raise security alerts block a reference update.
        tool:
          type: string

          description: The name of a code scanning tool.
    Category:
      type: object
      description: A discussion category
      required:
        - id
        - node_id
        - repository_id
        - name
        - slug
      properties:
        id:
          description: "The unique identifier of the resource."
          type: integer
        node_id:
          description: "The GraphQL node ID."
          type: string
        repository_id:
          description: "The ID of the repository this discussion category belongs to."
          type: integer
        emoji:
          description: "The emoji associated with the discussion category."
          type: string
        name:
          description: "The name of the discussion category."
          type: string
        description:
          description: "The description of the discussion category."
          type: string
        created_at:
          description: "The date and time the resource was created, in ISO 8601 format."
          type: string
          format: date-time
        updated_at:
          description: "The date and time the resource was last updated, in ISO 8601 format."
          type: string
          format: date-time
        slug:
          description: "The URL-friendly identifier of the resource."
          type: string
        is_answerable:
          description: "Whether discussions in this category can be marked as answered."
          type: boolean

    PullRequestMinimal:
      type: object
      description: |
        A minimal cross-reference to a pull request, as delivered in workflow_run/check_suite/
        check_run/deployment_review's pull_requests[] - NOT the full pull request resource (no
        title, state, user, etc.), so this must not $ref PullRequest or PullRequestRef.
      required:
        - id
        - number
        - url
        - head
        - base
      properties:
        id:
          description: "The pull request's unique identifier."
          type: integer
        number:
          description: "The pull request number."
          type: integer
        url:
          description: "The API URL of the resource."
          type: string
          format: uri
        head:
          description: "The head (source) branch, SHA, and repository of a minimal pull request reference."
          type: object
          properties:
            ref:
              description: "The name of the branch."
              type: string
            sha:
              description: "The SHA of the commit at the head of this branch."
              type: string
            repo:
              description: "A minimal repository reference (id, URL, name) used within a pull request's head ref."
              type: object
              properties:
                id:
                  description: "The unique identifier of the resource."
                  type: integer
                url:
                  description: "The API URL of the resource."
                  type: string
                  format: uri
                name:
                  description: "The name of the repository."
                  type: string
        base:
          description: "The base (target) branch, SHA, and repository of a minimal pull request reference."
          type: object
          properties:
            ref:
              description: "The name of the branch."
              type: string
            sha:
              description: "The SHA of the commit at the base of this branch."
              type: string
            repo:
              description: "A minimal repository reference (id, URL, name) used within a pull request's base ref."
              type: object
              properties:
                id:
                  description: "The unique identifier of the resource."
                  type: integer
                url:
                  description: "The API URL of the resource."
                  type: string
                  format: uri
                name:
                  description: "The name of the repository."
                  type: string

    App:
      type: object
      description: A GitHub App
      required:
        - id
        - node_id
        - name
      properties:
        id:
          type: integer
          description: Unique identifier of the GitHub app.
        node_id:
          description: "The GraphQL node ID."
          type: string
        owner:
          $ref: '#/components/schemas/User'
        name:
          type: string
          description: The name of the GitHub app.
        description:
          description: "The description of the GitHub App."
          type: string
          nullable: true
        external_url:
          description: "The URL of the GitHub App's external website."
          type: string
          format: uri
        html_url:
          description: "The URL of the resource on GitHub.com."
          type: string
          format: uri
        created_at:
          description: "The date and time the resource was created, in ISO 8601 format."
          type: string
          format: date-time
        updated_at:
          description: "The date and time the resource was last updated, in ISO 8601 format."
          type: string
          format: date-time
        permissions:
          type: object
          additionalProperties:
            type: string
          description: The set of permissions for the GitHub app.
        events:
          type: array
          items:
            type: string

          description: The list of events for the GitHub app. Note that the `installation_target`, `security_advisory`, and `meta` events are not included because they are global events and not specific to an installation.
    WorkflowJob:
      type: object
      description: A job in a GitHub Actions workflow run
      required:
        - id
        - name
        - status
      properties:
        id:
          type: integer
          description: The id of the job.
        run_id:
          type: integer
          description: The id of the associated workflow run.
        run_url:
          description: "The API URL of the associated workflow run."
          type: string
          format: uri
        run_attempt:
          type: integer
          description: Attempt number of the associated workflow run, 1 for first attempt and higher if the workflow was re-run.
        node_id:
          description: "The GraphQL node ID."
          type: string
        head_sha:
          type: string
          description: The SHA of the commit that is being run.
        url:
          description: "The API URL of the resource."
          type: string
          format: uri
        html_url:
          description: "The URL of the resource on GitHub.com."
          type: string
          format: uri
        status:
          type: string
          enum:
            - queued
            - in_progress
            - completed
            - waiting
          description: The phase of the lifecycle that the job is currently in.
        conclusion:
          type: string
          nullable: true
          enum:
            - action_required
            - cancelled
            - failure
            - neutral
            - skipped
            - success
            - timed_out
            - 
          description: The outcome of the job.
        created_at:
          type: string
          format: date-time
          description: The time that the job created, in ISO 8601 format.
        started_at:
          type: string
          format: date-time
          description: The time that the job started, in ISO 8601 format.
        completed_at:
          type: string
          format: date-time
          nullable: true
          description: The time that the job finished, in ISO 8601 format.
        name:
          type: string
          description: The name of the job.
        steps:
          type: array
          items:
            description: "A single step within a GitHub Actions job."
            type: object
            properties:
              name:
                description: "The name of the step."
                type: string
              status:
                description: "The phase of the lifecycle that the step is currently in."
                type: string
              conclusion:
                description: "The result of the completed step, or null if not yet completed."
                type: string
                nullable: true
              number:
                description: "The step's position within the job."
                type: integer
              started_at:
                description: "The time the step began, or null if not yet started."
                type: string
                format: date-time
                nullable: true
              completed_at:
                description: "The time the step completed, or null if not yet completed."
                type: string
                format: date-time
                nullable: true
          description: Steps in this job.
        check_run_url:
          description: "The API URL of the associated check run."
          type: string
          format: uri
        labels:
          type: array
          items:
            type: string
          description: Labels for the workflow job. Specified by the "runs_on" attribute in the action's workflow file.
        runner_id:
          type: integer
          nullable: true
          description: The ID of the runner to which this job has been assigned. (If a runner hasn't yet been assigned, this will be null.)
        runner_name:
          type: string
          nullable: true
          description: The name of the runner to which this job has been assigned. (If a runner hasn't yet been assigned, this will be null.)
        runner_group_id:
          type: integer
          nullable: true
          description: The ID of the runner group to which this job has been assigned. (If a runner hasn't yet been assigned, this will be null.)
        runner_group_name:
          type: string
          nullable: true
          description: The name of the runner group to which this job has been assigned. (If a runner hasn't yet been assigned, this will be null.)
        workflow_name:
          type: string
          nullable: true
          description: The name of the workflow.
        head_branch:
          type: string
          nullable: true

          description: The name of the current branch.
    # -----------------------------------------------------------------------
    # Event payload schemas
    # -----------------------------------------------------------------------

    PingPayload:
      type: object
      description: Payload for the ping event
      properties:
        zen:
          type: string
          description: Random string of GitHub zen
        hook_id:
          type: integer
          description: The ID of the webhook that triggered the ping
        hook:
          type: object
          description: The webhook that is being pinged
          properties:
            type:
              description: "The kind of target the webhook is attached to. Can be one of: Repository, Organization, App."
              type: string
              enum:
                - Repository
                - Organization
                - App
                - Business
                - Marketplace
            id:
              type: integer
              description: Unique identifier of the webhook.
            name:
              type: string
              description: The name of a valid service, use 'web' for a webhook.
            active:
              type: boolean
              description: Determines whether the hook is actually triggered on pushes.
            events:
              type: array
              items:
                type: string
              description: "Determines what events the hook is triggered for. Default: ['push']."
            config:
              type: object
              description: Configuration object of the webhook.
              properties:
                content_type:
                  type: string
                  enum:
                    - json
                    - form
                  description: The media type used to serialize the payloads. The default is `form`.
                insecure_ssl:
                  type: string
                  description: Determines whether the SSL certificate of the host for `url` will be verified when delivering payloads. Supported values include `0` (verification is performed) and `1` (verification is not performed). The default is `0`.
                url:
                  type: string
                  format: uri
                  description: The URL to which the payloads will be delivered.
            updated_at:
              description: "The date and time the resource was last updated, in ISO 8601 format."
              type: string
              format: date-time
            created_at:
              description: "The date and time the resource was created, in ISO 8601 format."
              type: string
              format: date-time
            url:
              description: "The API URL of the resource."
              type: string
              format: uri
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'

    PushPayload:
      type: object
      description: Payload for the push event
      required:
        - ref
        - before
        - after
        - created
        - deleted
        - forced
        - commits
        - compare
        - pusher
        - sender
        - repository
      properties:
        ref:
          type: string
          description: The full git ref that was pushed (e.g. refs/heads/main or refs/tags/v3.14.1)
        before:
          type: string
          description: The SHA of the most recent commit on ref before the push
        after:
          type: string
          description: The SHA of the most recent commit on ref after the push
        base_ref:
          type: string
          nullable: true
          description: The base ref for the push (if applicable)
        created:
          type: boolean
          description: Whether this push created the ref
        deleted:
          type: boolean
          description: Whether this push deleted the ref
        forced:
          type: boolean
          description: Whether this push was a force push of the ref
        compare:
          type: string
          description: URL showing the changes in this ref update
        commits:
          type: array
          description: Array of commit objects (maximum 2048)
          items:
            $ref: '#/components/schemas/Commit'
        head_commit:
          $ref: '#/components/schemas/Commit'
          nullable: true
        pusher:
          $ref: '#/components/schemas/CommitAuthor'
          description: Metaproperties for the Git author/committer
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    PullRequestPayload:
      type: object
      description: Payload for pull_request events
      required:
        - action
        - number
        - pull_request
        - sender
        - repository
      properties:
        action:
          type: string
          description: The action that was performed
          enum:
            - assigned
            - auto_merge_disabled
            - auto_merge_enabled
            - closed
            - converted_to_draft
            - demilestoned
            - dequeued
            - edited
            - enqueued
            - labeled
            - locked
            - milestoned
            - opened
            - ready_for_review
            - reopened
            - review_request_removed
            - review_requested
            - synchronize
            - unassigned
            - unlabeled
            - unlocked
        number:
          type: integer
          description: The pull request number
        pull_request:
          $ref: '#/components/schemas/PullRequest'
        assignee:
          $ref: '#/components/schemas/User'
        changes:
          type: object
          description: For edited events, the changes to the pull request
          properties:
            title:
              type: object
              description: The pull request's previous title, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: string
            body:
              type: object
              description: The pull request's previous body text, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: string
            base:
              type: object
              description: The pull request's previous base branch, before this edit.
              properties:
                ref:
                  description: "The pull request's previous base branch ref, before this edit."
                  type: object
                  properties:
                    from:
                      description: The previous value, before this change.
                      type: string
                sha:
                  description: "The pull request's previous base branch SHA, before this edit."
                  type: object
                  properties:
                    from:
                      description: The previous value, before this change.
                      type: string
        requested_reviewer:
          $ref: '#/components/schemas/User'
        label:
          $ref: '#/components/schemas/Label'
        milestone:
          $ref: '#/components/schemas/Milestone'
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    IssuesPayload:
      type: object
      description: Payload for issues events
      required:
        - action
        - issue
        - sender
        - repository
      properties:
        action:
          type: string
          description: The action that was performed
          enum:
            - assigned
            - closed
            - deleted
            - demilestoned
            - edited
            - labeled
            - locked
            - milestoned
            - opened
            - pinned
            - reopened
            - transferred
            - typed
            - unassigned
            - unlabeled
            - unlocked
            - unpinned
            - untyped
        issue:
          $ref: '#/components/schemas/Issue'
        assignee:
          $ref: '#/components/schemas/User'
          nullable: true
        label:
          $ref: '#/components/schemas/Label'
        changes:
          type: object
          description: For edited events, the changes to the issue
          properties:
            title:
              type: object
              description: The issue's previous title, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: string
            body:
              type: object
              description: The issue's previous body text, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: string
        milestone:
          $ref: '#/components/schemas/Milestone'
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    ReleasePayload:
      type: object
      description: Payload for release events
      required:
        - action
        - release
        - sender
        - repository
      properties:
        action:
          type: string
          description: The action that was performed
          enum:
            - created
            - deleted
            - edited
            - prereleased
            - published
            - released
            - unpublished
        release:
          $ref: '#/components/schemas/Release'
        changes:
          type: object
          description: For edited events, the changes to the release
          properties:
            body:
              type: object
              description: The release's previous body text, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: string
            name:
              type: object
              description: The release's previous name, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: string
            tag_name:
              type: object
              description: The release's previous tag name, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: string
            make_latest:
              type: object
              description: Whether this release was explicitly edited to be the latest
              properties:
                to:
                  description: "Whether the release became the latest release (best-effort; not documented by GitHub)."
                  type: boolean
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    IssueCommentPayload:
      type: object
      description: Payload for issue_comment events
      required:
        - action
        - issue
        - comment
        - sender
        - repository
      properties:
        action:
          description: "The action that was performed. Can be one of: created, edited, deleted, pinned, unpinned."
          type: string
          enum:
            - created
            - deleted
            - edited
            - pinned
            - unpinned
        issue:
          $ref: '#/components/schemas/Issue'
        comment:
          $ref: '#/components/schemas/IssueComment'
        changes:
          type: object
          description: For edited events, the changes to the comment
          required:
            - body
          properties:
            body:
              type: object
              description: The comment's previous body text, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: string
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    PullRequestReviewPayload:
      type: object
      description: Payload for pull_request_review events
      required:
        - action
        - review
        - pull_request
        - sender
        - repository
      properties:
        action:
          description: "The action that was performed. Can be one of: submitted, edited, dismissed."
          type: string
          enum:
            - dismissed
            - edited
            - submitted
        review:
          $ref: '#/components/schemas/PullRequestReview'
        pull_request:
          $ref: '#/components/schemas/PullRequest'
        changes:
          type: object
          description: For edited events, the changes to the review
          properties:
            body:
              type: object
              description: The review's previous body text, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: string
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    PullRequestReviewCommentPayload:
      type: object
      description: Payload for pull_request_review_comment events
      required:
        - action
        - comment
        - pull_request
        - sender
        - repository
      properties:
        action:
          description: "The action that was performed. Can be one of: created, deleted, edited."
          type: string
          enum:
            - created
            - deleted
            - edited
        comment:
          $ref: '#/components/schemas/PullRequestReviewComment'
        pull_request:
          $ref: '#/components/schemas/PullRequest'
        changes:
          type: object
          description: For edited events, the changes to the comment
          properties:
            body:
              type: object
              description: The comment's previous body text, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: string
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    CreatePayload:
      type: object
      description: Payload for create events (branch or tag created)
      required:
        - ref
        - ref_type
        - master_branch
        - pusher_type
        - sender
        - repository
      properties:
        ref:
          type: string
          description: The git ref resource (branch or tag name)
        ref_type:
          type: string
          description: The type of Git ref object created
          enum:
            - branch
            - tag
        master_branch:
          type: string
          description: The name of the repository's default branch (usually main)
        description:
          type: string
          nullable: true
          description: The repository's current description
        pusher_type:
          type: string
          description: The pusher type; either user or a deploy key
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    DeletePayload:
      type: object
      description: Payload for delete events (branch or tag deleted)
      required:
        - ref
        - ref_type
        - pusher_type
        - sender
        - repository
      properties:
        ref:
          type: string
          description: The git ref resource (branch or tag name)
        ref_type:
          type: string
          description: The type of Git ref object deleted
          enum:
            - branch
            - tag
        pusher_type:
          type: string
          description: The pusher type; either user or a deploy key
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    ForkPayload:
      type: object
      description: Payload for fork events
      required:
        - forkee
        - sender
        - repository
      properties:
        forkee:
          $ref: '#/components/schemas/Repository'
          description: The created (forked) repository
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    StarPayload:
      type: object
      description: Payload for star events
      required:
        - action
        - starred_at
      properties:
        action:
          description: "The action that was performed. Can be one of: created, deleted."
          type: string
          enum:
            - created
            - deleted
        starred_at:
          type: string
          format: date-time
          nullable: true
          description: The time the star was created (ISO 8601). Null for the deleted action.
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    WatchPayload:
      type: object
      description: Payload for watch events (someone started watching the repository)
      required:
        - action
        - sender
        - repository
      properties:
        action:
          description: "The action that was performed. Always started."
          type: string
          enum:
            - started
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    CheckRunPayload:
      type: object
      description: Payload for check_run events
      required:
        - action
        - check_run
      properties:
        action:
          description: "The action that was performed. Can be one of: created, completed, requested_action, rerequested."
          type: string
          enum:
            - completed
            - created
            - requested_action
            - rerequested
        check_run:
          $ref: '#/components/schemas/CheckRun'
        requested_action:
          type: object
          description: Present for requested_action events
          properties:
            identifier:
              type: string
              description: The integrator reference of the action requested by the user.
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    CheckSuitePayload:
      type: object
      description: Payload for check_suite events
      required:
        - action
        - check_suite
      properties:
        action:
          description: "The action that was performed. Can be one of: completed, requested, rerequested."
          type: string
          enum:
            - completed
            - requested
            - rerequested
        check_suite:
          $ref: '#/components/schemas/CheckSuite'
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    DeploymentPayload:
      type: object
      description: Payload for deployment events
      required:
        - action
        - deployment
        - workflow
        - workflow_run
        - sender
        - repository
      properties:
        action:
          description: "The action that was performed. Always created."
          type: string
          enum:
            - created
        deployment:
          $ref: '#/components/schemas/Deployment'
        workflow:
          $ref: '#/components/schemas/Workflow'
          nullable: true
          description: The workflow that triggered the deployment (if applicable)
        workflow_run:
          $ref: '#/components/schemas/WorkflowRun'
          nullable: true
          description: The workflow run that triggered the deployment (if applicable)
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    DeploymentStatusPayload:
      type: object
      description: Payload for deployment_status events
      required:
        - action
        - deployment
        - deployment_status
        - sender
        - repository
      properties:
        action:
          description: "The action that was performed. Always created."
          type: string
          enum:
            - created
        deployment:
          $ref: '#/components/schemas/Deployment'
        deployment_status:
          $ref: '#/components/schemas/DeploymentStatus'
        check_run:
          $ref: '#/components/schemas/CheckRun'
          nullable: true
        workflow:
          $ref: '#/components/schemas/Workflow'
          nullable: true
        workflow_run:
          $ref: '#/components/schemas/WorkflowRun'
          nullable: true
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    WorkflowRunPayload:
      type: object
      description: Payload for workflow_run events
      required:
        - action
        - workflow_run
        - workflow
      properties:
        action:
          description: "The action that was performed. Can be one of: in_progress, completed, requested."
          type: string
          enum:
            - completed
            - in_progress
            - requested
        workflow_run:
          $ref: '#/components/schemas/WorkflowRun'
        workflow:
          $ref: '#/components/schemas/Workflow'
          nullable: true
          description: The workflow that is being run
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    WorkflowJobPayload:
      type: object
      description: Payload for workflow_job events
      required:
        - action
        - workflow_job
      properties:
        action:
          description: "The action that was performed. Can be one of: queued, waiting, completed, in_progress."
          type: string
          enum:
            - completed
            - in_progress
            - queued
            - waiting
        workflow_job:
          $ref: '#/components/schemas/WorkflowJob'
        deployment:
          $ref: '#/components/schemas/Deployment'
          description: The deployment associated with the workflow job (if applicable)
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    RepositoryPayload:
      type: object
      description: Payload for repository events
      required:
        - action
      properties:
        action:
          description: "The action that was performed. Can be one of: created, deleted, archived, unarchived, edited, renamed, transferred, publicized, privatized."
          type: string
          enum:
            - archived
            - created
            - deleted
            - edited
            - privatized
            - publicized
            - renamed
            - transferred
            - unarchived
        changes:
          type: object
          description: For edited/renamed/transferred events, the changes that occurred
          properties:
            description:
              type: object
              description: The repository's previous description, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: string
                  nullable: true
            homepage:
              type: object
              description: The repository's previous homepage URL, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: string
                  nullable: true
            topics:
              type: object
              description: The repository's previous topics, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: array
                  items:
                    type: string
            default_branch:
              type: object
              description: The repository's previous default branch, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: string
            repository:
              type: object
              description: Present for a renamed event
              properties:
                name:
                  description: "The repository's previous name, before this rename."
                  type: object
                  properties:
                    from:
                      description: The previous value, before this change.
                      type: string
            owner:
              type: object
              description: Present for a transferred event
              properties:
                from:
                  description: The previous value, before this change.
                  type: object
                  properties:
                    user:
                      $ref: '#/components/schemas/User'
                    organization:
                      $ref: '#/components/schemas/Organization'
        repository:
          $ref: '#/components/schemas/Repository'
        sender:
          $ref: '#/components/schemas/User'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    MemberPayload:
      type: object
      description: Payload for member events (repository collaborator activity)
      required:
        - action
        - member
        - sender
        - repository
      properties:
        action:
          description: "The action that was performed. Can be one of: edited, added, removed."
          type: string
          enum:
            - added
            - edited
            - removed
        member:
          $ref: '#/components/schemas/User'
          nullable: true
        changes:
          type: object
          description: For edited events, the changes to the member's permissions
          properties:
            old_permission:
              type: object
              description: The collaborator's previous permission level, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: string
            permission:
              type: object
              description: The collaborator's permission level change.
              properties:
                from:
                  type: string
                  description: The collaborator's permission level before this change.
                to:
                  type: string
                  description: The collaborator's permission level after this change.
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    # -----------------------------------------------------------------------
    # Batch 2 reusable sub-schemas
    # -----------------------------------------------------------------------

    Discussion:
      type: object
      description: A GitHub Discussion in a repository
      required:
        - id
        - number
        - title
        - state
      properties:
        id:
          description: "The unique identifier of the resource."
          type: integer
        node_id:
          description: "The GraphQL node ID."
          type: string
        number:
          description: "The discussion's number within the repository."
          type: integer
        title:
          type: string
          description: The discussion post's title.
        body:
          type: string
          nullable: true
          description: The discussion post's body text.
        state:
          type: string
          enum:
            - open
            - closed
            - converted
            - locked
          description: The current state of the discussion. `converting` means that the discussion is being converted from an issue. `transferring` means that the discussion is being transferred from another repository.
        category:
          $ref: '#/components/schemas/Category'
        user:
          $ref: '#/components/schemas/User'
        html_url:
          description: "The URL of the resource on GitHub.com."
          type: string
          format: uri
        comments:
          description: "The number of comments on the discussion."
          type: integer
        labels:
          type: array
          items:
            $ref: '#/components/schemas/Label'
          description: Color-coded labels categorizing the discussion.
        locked:
          description: "Whether the discussion is locked."
          type: boolean
        active_lock_reason:
          description: "The reason the discussion was locked, if applicable."
          type: string
          nullable: true
        answer_html_url:
          description: "The URL of the comment marked as the answer, if any."
          type: string
          nullable: true
        answer_chosen_at:
          description: "The date and time an answer was chosen, in ISO 8601 format."
          type: string
          format: date-time
          nullable: true
        answer_chosen_by:
          $ref: '#/components/schemas/User'
          nullable: true
        created_at:
          description: "The date and time the resource was created, in ISO 8601 format."
          type: string
          format: date-time
        updated_at:
          description: "The date and time the resource was last updated, in ISO 8601 format."
          type: string
          format: date-time

    Team:
      type: object
      description: A GitHub organization team
      required:
        - id
        - name
        - slug
      properties:
        id:
          type: integer
          description: Unique identifier of the team.
        node_id:
          description: "The GraphQL node ID."
          type: string
        name:
          type: string
          description: Name of the team.
        slug:
          description: "The URL-friendly identifier of the resource."
          type: string
        description:
          type: string
          nullable: true
          description: Description of the team.
        privacy:
          description: "The level of privacy this team has (e.g. closed, secret)."
          type: string
          enum:
            - closed
            - secret
        notification_setting:
          type: string
          enum:
            - notifications_enabled
            - notifications_disabled
          description: Whether team members will receive notifications when their team is @mentioned.
        permission:
          type: string
          description: Permission that the team will have for its repositories.
        url:
          type: string
          format: uri
          description: URL for the team.
        html_url:
          description: "The URL of the resource on GitHub.com."
          type: string
          format: uri
        members_url:
          description: "The API URL listing the members of this team."
          type: string
        repositories_url:
          description: "The API URL listing the repositories associated with this resource."
          type: string
          format: uri
        parent:
          type: object
          nullable: true
          description: |
            A reduced "team-simple" shape, not a full Team - no nested parent (a team's parent
            is never itself nested further, avoiding unbounded recursion).
          required:
            - id
            - node_id
            - name
            - slug
            - type
          properties:
            id:
              type: integer
              description: Unique identifier of the team.
            node_id:
              description: "The GraphQL node ID."
              type: string
            url:
              type: string
              format: uri
              description: URL for the team.
            members_url:
              description: "The API URL listing the members of this team."
              type: string
            name:
              type: string
              description: Name of the team.
            description:
              type: string
              nullable: true
              description: Description of the team.
            permission:
              type: string
              description: Permission that the team will have for its repositories.
            privacy:
              type: string
              description: The level of privacy this team should have.
            notification_setting:
              type: string
              description: The notification setting the team has set.
            html_url:
              description: "The URL of the resource on GitHub.com."
              type: string
              format: uri
            repositories_url:
              description: "The API URL listing the repositories associated with this resource."
              type: string
              format: uri
            slug:
              description: "The URL-friendly identifier of the resource."
              type: string
            type:
              type: string
              enum:
                - enterprise
                - organization
              description: The ownership type of the team.
            organization_id:
              type: integer
              description: Unique identifier of the organization to which this team belongs.
            enterprise_id:
              type: integer

              description: Unique identifier of the enterprise to which this team belongs.
    # -----------------------------------------------------------------------
    # Batch 2 event payload schemas
    # -----------------------------------------------------------------------

    DiscussionPayload:
      type: object
      description: Payload for discussion events
      required:
        - action
        - discussion
      properties:
        action:
          description: "The action that was performed. Can be one of: created, edited, deleted, pinned, unpinned, locked, unlocked, transferred, category_changed, answered, unanswered, labeled, unlabeled, closed, reopened."
          type: string
          enum:
            - answered
            - category_changed
            - closed
            - created
            - deleted
            - edited
            - labeled
            - locked
            - pinned
            - reopened
            - transferred
            - unanswered
            - unlabeled
            - unlocked
            - unpinned
        discussion:
          $ref: '#/components/schemas/Discussion'
        answer:
          type: object
          description: |
            Present on answered action - the comment marked as answer. Richer than the
            Comment schema used for discussion_comment (has parent_id, child_comment_count,
            repository_url, discussion_id in addition to Comment's fields), so this is its
            own shape rather than a $ref to Comment.
          nullable: true
          properties:
            id:
              description: "The unique identifier of the resource."
              type: integer
            node_id:
              description: "The GraphQL node ID."
              type: string
            html_url:
              description: "The URL of the resource on GitHub.com."
              type: string
              format: uri
            parent_id:
              description: "The ID of the parent comment, if this is a reply; otherwise null."
              type: integer
              nullable: true
            child_comment_count:
              description: "The number of replies to this comment."
              type: integer
            repository_url:
              description: "The API URL of the repository this comment belongs to."
              type: string
              format: uri
            discussion_id:
              description: "The ID of the discussion this comment belongs to."
              type: integer
            author_association:
              type: string
              description: How the author is associated with the repository.
            user:
              $ref: '#/components/schemas/User'
            body:
              description: "The text of the comment."
              type: string
            created_at:
              description: "The date and time the resource was created, in ISO 8601 format."
              type: string
              format: date-time
            updated_at:
              description: "The date and time the resource was last updated, in ISO 8601 format."
              type: string
              format: date-time
        label:
          $ref: '#/components/schemas/Label'
        changes:
          type: object
          description: For edited/category_changed events, the changes made
          properties:
            title:
              type: object
              description: The discussion's previous title, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: string
            body:
              type: object
              description: The discussion's previous body text, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: string
            category:
              type: object
              description: Present on category_changed
              properties:
                from:
                  $ref: '#/components/schemas/Category'
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    DiscussionCommentPayload:
      type: object
      description: Payload for discussion_comment events
      required:
        - action
        - comment
        - discussion
      properties:
        action:
          description: "The action that was performed. Can be one of: created, edited, deleted."
          type: string
          enum:
            - created
            - deleted
            - edited
        comment:
          type: object
          description: The discussion comment
          properties:
            id:
              description: "The unique identifier of the resource."
              type: integer
            node_id:
              description: "The GraphQL node ID."
              type: string
            html_url:
              description: "The URL of the resource on GitHub.com."
              type: string
              format: uri
            body:
              description: "The text of the discussion comment."
              type: string
            user:
              $ref: '#/components/schemas/User'
            created_at:
              description: "The date and time the resource was created, in ISO 8601 format."
              type: string
              format: date-time
            updated_at:
              description: "The date and time the resource was last updated, in ISO 8601 format."
              type: string
              format: date-time
            author_association:
              type: string
              description: How the author is associated with the repository.
        discussion:
          $ref: '#/components/schemas/Discussion'
        changes:
          type: object
          description: For edited events, the changes to the comment
          properties:
            body:
              type: object
              description: The comment's previous body text, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: string
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    LabelPayload:
      type: object
      description: Payload for label events
      required:
        - action
        - label
        - sender
        - repository
      properties:
        action:
          description: "The action that was performed. Can be one of: created, edited, deleted."
          type: string
          enum:
            - created
            - deleted
            - edited
        label:
          $ref: '#/components/schemas/Label'
        changes:
          type: object
          description: For edited events, the changes to the label
          properties:
            color:
              type: object
              description: The label's previous color, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: string
            name:
              type: object
              description: The label's previous name, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: string
            description:
              type: object
              description: The label's previous description, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: string
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    MilestonePayload:
      type: object
      description: Payload for milestone events
      required:
        - action
        - milestone
        - sender
        - repository
      properties:
        action:
          description: "The action that was performed. Can be one of: created, edited, opened, deleted, closed."
          type: string
          enum:
            - closed
            - created
            - deleted
            - edited
            - opened
        milestone:
          $ref: '#/components/schemas/Milestone'
        changes:
          type: object
          description: For edited events, the changes to the milestone
          properties:
            description:
              type: object
              description: The milestone's previous description, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: string
            due_on:
              type: object
              description: The milestone's previous due date, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: string
                  format: date-time
                  nullable: true
            title:
              type: object
              description: The milestone's previous title, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: string
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    StatusPayload:
      type: object
      description: |
        Payload for status events. No action field — the state property carries
        the status (pending, success, failure, error).
      required:
        - id
        - sha
        - name
        - state
        - context
        - created_at
        - updated_at
        - commit
        - branches
        - sender
        - repository
      properties:
        id:
          type: integer
          description: The unique identifier of the status
        sha:
          type: string
          description: The commit SHA
        name:
          type: string
          description: The repository name
        state:
          type: string
          description: The new state of the commit status
          enum:
            - error
            - failure
            - pending
            - success
        context:
          type: string
          description: The status context identifier
        description:
          type: string
          nullable: true
          description: The optional human-readable description
        target_url:
          type: string
          nullable: true
          description: The optional link added to the status
        avatar_url:
          description: "The URL of the user's or organization's avatar image."
          type: string
          nullable: true
        commit:
          type: object
          description: The commit the status is associated with
          properties:
            sha:
              type: string
              description: The commit SHA.
            commit:
              type: object
              description: The raw git commit data (distinct from the GitHub account in the sibling author/committer fields)
              properties:
                author:
                  type: object
                  properties:
                    name:
                      type: string
                      description: Name of the git user.
                    email:
                      type: string
                      description: Git email address of the user.
                    date:
                      type: string
                      format: date-time
                      description: Timestamp of the commit.
                  description: Identifying information for the git-user.
                committer:
                  type: object
                  properties:
                    name:
                      type: string
                      description: Name of the git user.
                    email:
                      type: string
                      description: Git email address of the user.
                    date:
                      type: string
                      format: date-time
                      description: Timestamp of the commit.
                  description: Identifying information for the git-user.
                message:
                  type: string
                  description: Message describing the purpose of the commit.
                tree:
                  description: "A pointer to the Git tree object for a commit."
                  type: object
                  properties:
                    sha:
                      description: "The SHA of the Git tree object."
                      type: string
                    url:
                      description: "The API URL of the resource."
                      type: string
                      format: uri
                url:
                  description: "The API URL of the resource."
                  type: string
                  format: uri
                comment_count:
                  description: "The number of comments on the commit."
                  type: integer
                verification:
                  description: "GPG/SSH commit signature verification details."
                  type: object
                  properties:
                    verified:
                      description: "Whether the commit signature was successfully verified."
                      type: boolean
                    reason:
                      description: "The reason for the verification state, e.g. `valid`, `unsigned`, `bad_signature`."
                      type: string
                    signature:
                      description: "The signature that was extracted from the commit, or null if unsigned."
                      type: string
                      nullable: true
                    payload:
                      description: "The value that was signed, or null if unsigned."
                      type: string
                      nullable: true
            url:
              description: "The API URL of the resource."
              type: string
              format: uri
            html_url:
              description: "The URL of the resource on GitHub.com."
              type: string
              format: uri
            author:
              $ref: '#/components/schemas/User'
            committer:
              $ref: '#/components/schemas/User'
        branches:
          type: array
          description: Array of branches containing the status SHA (max 10)
          items:
            description: "A branch whose HEAD commit matches this status; at most 10 are included."
            type: object
            properties:
              name:
                description: "The name of the branch."
                type: string
              commit:
                description: "The commit at the tip of a branch (SHA and URL only)."
                type: object
                properties:
                  sha:
                    description: "The SHA of the commit."
                    type: string
                  url:
                    description: "The API URL of the resource."
                    type: string
                    format: uri
              protected:
                description: "Whether the branch has protection rules enabled."
                type: boolean
        created_at:
          description: "The date and time the resource was created, in ISO 8601 format."
          type: string
          format: date-time
        updated_at:
          description: "The date and time the resource was last updated, in ISO 8601 format."
          type: string
          format: date-time
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    WorkflowDispatchPayload:
      type: object
      description: Payload for workflow_dispatch events (manually triggered workflows)
      required:
        - ref
        - workflow
      properties:
        inputs:
          type: object
          nullable: true
          description: The inputs provided when manually triggering the workflow
          additionalProperties: true
        ref:
          type: string
          description: The branch or tag ref from which the workflow was triggered
        workflow:
          type: string
          description: The path to the workflow file (e.g. .github/workflows/main.yml)
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    PullRequestReviewThreadPayload:
      type: object
      description: Payload for pull_request_review_thread events
      required:
        - action
        - pull_request
        - thread
      properties:
        action:
          description: "The action that was performed. Can be one of: resolved, unresolved."
          type: string
          enum:
            - resolved
            - unresolved
        pull_request:
          $ref: '#/components/schemas/PullRequest'
        thread:
          type: object
          description: The review thread that was resolved or unresolved
          properties:
            node_id:
              description: "The GraphQL node ID."
              type: string
            comments:
              description: "The review comments in this conversation thread."
              type: array
              items:
                $ref: '#/components/schemas/PullRequestReviewComment'
        updated_at:
          description: "The date and time the resource was last updated, in ISO 8601 format."
          type: string
          format: date-time
          nullable: true
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    TeamPayload:
      type: object
      description: Payload for team events
      required:
        - action
        - team
      properties:
        action:
          description: "The action that was performed. Can be one of: created, deleted, edited, added_to_repository, removed_from_repository."
          type: string
          enum:
            - added_to_repository
            - created
            - deleted
            - edited
            - removed_from_repository
        team:
          $ref: '#/components/schemas/Team'
        changes:
          type: object
          description: For edited events, the changes to the team
          properties:
            description:
              type: object
              description: The team's previous description, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: string
            name:
              type: object
              description: The team's previous name, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: string
            privacy:
              type: object
              description: The team's previous privacy level, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: string
            notification_setting:
              type: object
              description: The team's previous notification setting, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: string
            repository:
              $ref: '#/components/schemas/Repository'
              description: For added_to_repository/removed_from_repository events
        repository:
          $ref: '#/components/schemas/Repository'
          description: Present for added_to_repository and removed_from_repository actions
        sender:
          $ref: '#/components/schemas/User'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    OrganizationPayload:
      type: object
      description: Payload for organization events
      required:
        - action
      properties:
        action:
          description: "The action that was performed. Can be one of: member_added, member_removed, member_invited, renamed, deleted."
          type: string
          enum:
            - deleted
            - member_added
            - member_invited
            - member_removed
            - renamed
        membership:
          type: object
          description: |
            The membership between the user and the organization.
            Not present when the action is member_invited.
          nullable: true
          properties:
            url:
              description: "The API URL of the resource."
              type: string
              format: uri
            state:
              type: string
              enum:
                - active
                - pending
              description: The state of the member in the organization. The `pending` state indicates the user has not yet accepted an invitation.
            role:
              type: string
              enum:
                - admin
                - member
                - billing_manager
              description: The user's membership type in the organization.
            organization_url:
              description: "The API URL of the organization."
              type: string
              format: uri
            user:
              $ref: '#/components/schemas/User'
        invitation:
          type: object
          nullable: true
          description: Present when action is member_invited
          required:
            - id
            - role
            - created_at
            - inviter
            - team_count
            - node_id
            - invitation_teams_url
          properties:
            id:
              description: "The unique identifier of the resource."
              type: integer
            login:
              description: "The username of the invited user, if known."
              type: string
              nullable: true
            email:
              description: "The email address of the invited user, if known."
              type: string
              nullable: true
            role:
              description: "The role assigned to the invited user, e.g. admin, direct_member, or billing_manager."
              type: string
            created_at:
              description: "The date and time the resource was created, in ISO 8601 format."
              type: string
              format: date-time
            failed_at:
              description: "The date and time the invitation failed, in ISO 8601 format, if applicable."
              type: string
              format: date-time
              nullable: true
            failed_reason:
              description: "The reason the invitation failed, if applicable."
              type: string
              nullable: true
            inviter:
              $ref: '#/components/schemas/User'
            team_count:
              description: "The number of teams the invitation grants membership to."
              type: integer
            node_id:
              description: "The GraphQL node ID."
              type: string
            invitation_teams_url:
              description: "The API URL listing the teams included in this invitation."
              type: string
              format: uri
            invitation_source:
              description: "The source through which the invitation was created, e.g. member or scim."
              type: string
              nullable: true
        changes:
          type: object
          nullable: true
          description: For renamed events, the old and new organization name
          properties:
            login:
              type: object
              description: The organization's previous login (name), before this rename.
              properties:
                from:
                  description: The previous value, before this change.
                  type: string
        sender:
          $ref: '#/components/schemas/User'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    DeploymentProtectionRulePayload:
      type: object
      description: Payload for deployment_protection_rule events (app webhooks only)
      properties:
        environment:
          type: string
          description: The name of the environment that has the deployment protection rule
        event:
          type: string
          description: The event that triggered the deployment protection rule
        sha:
          type: string
          description: The commit SHA that triggered the workflow
        ref:
          type: string
          description: The branch or tag ref that triggered the workflow
        deployment_callback_url:
          type: string
          format: uri
          description: The URL to call to approve or reject the deployment
        deployment:
          $ref: '#/components/schemas/Deployment'
          description: A request for a specific ref to be deployed
        pull_requests:
          type: array
          description: The pull requests associated with the deployment
          items:
            description: "A pull request associated with the deployment (number, URL, head and base refs)."
            type: object
            properties:
              number:
                description: "The pull request number."
                type: integer
              url:
                description: "The API URL of the resource."
                type: string
                format: uri
              head:
                $ref: '#/components/schemas/PullRequestRef'
              base:
                $ref: '#/components/schemas/PullRequestRef'
        sender:
          $ref: '#/components/schemas/User'
        installation:
          $ref: '#/components/schemas/Installation'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'

    # -----------------------------------------------------------------------
    # Batch 3 event payload schemas
    # -----------------------------------------------------------------------

    CommitCommentPayload:
      type: object
      description: Payload for commit_comment events
      required:
        - action
        - comment
      properties:
        action:
          description: "The action that was performed. Can be one of: created."
          type: string
          enum:
            - created
        comment:
          type: object
          description: The commit comment resource
          required:
            - id
            - body
          properties:
            id:
              description: "The unique identifier of the resource."
              type: integer
            node_id:
              description: "The GraphQL node ID."
              type: string
            url:
              description: "The API URL of the resource."
              type: string
              format: uri
            html_url:
              description: "The URL of the resource on GitHub.com."
              type: string
              format: uri
            body:
              description: "The contents of the comment."
              type: string
            path:
              type: string
              nullable: true
              description: The relative path of the file being commented on
            position:
              type: integer
              nullable: true
              description: The line index in the diff
            line:
              type: integer
              nullable: true
              description: The line of the blob the comment refers to
            commit_id:
              description: "The SHA of the commit the comment was made on."
              type: string
            user:
              $ref: '#/components/schemas/User'
            created_at:
              description: "The date and time the resource was created, in ISO 8601 format."
              type: string
              format: date-time
            updated_at:
              description: "The date and time the resource was last updated, in ISO 8601 format."
              type: string
              format: date-time
            author_association:
              type: string
              description: How the author is associated with the repository.
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    DependabotAlertPayload:
      type: object
      description: Payload for dependabot_alert events
      required:
        - action
        - alert
      properties:
        action:
          description: "The action that was performed. Can be one of: created, dismissed, reopened, reintroduced, fixed, auto_dismissed, auto_reopened, assignees_changed."
          type: string
          enum:
            - assignees_changed
            - auto_dismissed
            - auto_reopened
            - created
            - dismissed
            - fixed
            - reintroduced
            - reopened
        alert:
          type: object
          description: A Dependabot alert
          required:
            - number
            - state
          properties:
            number:
              type: integer
              description: The security alert number.
            state:
              type: string
              enum:
                - auto_dismissed
                - dismissed
                - fixed
                - open
              description: The state of the Dependabot alert.
            dependency:
              type: object
              properties:
                package:
                  $ref: '#/components/schemas/Package'
                manifest_path:
                  type: string
                  description: The full path to the dependency manifest file, relative to the root of the repository.
                scope:
                  type: string
                  enum:
                    - development
                    - runtime
                  nullable: true
                  description: The execution scope of the vulnerable dependency.
              description: Details for the vulnerable dependency.
            security_advisory:
              description: "Details for the GitHub Security Advisory linked to the alert."
              type: object
              properties:
                ghsa_id:
                  description: "The GitHub Security Advisory ID."
                  type: string
                cve_id:
                  description: "The CVE ID associated with the advisory, if any."
                  type: string
                  nullable: true
                summary:
                  description: "A short summary of the security advisory."
                  type: string
                description:
                  description: "A detailed description of the security advisory."
                  type: string
                severity:
                  description: "The severity of the advisory, e.g. low, medium, high, or critical."
                  type: string
                  enum:
                    - low
                    - medium
                    - high
                    - critical
                vulnerabilities:
                  description: "The list of package versions affected by this advisory."
                  type: array
                  items:
                    $ref: '#/components/schemas/SecurityVulnerability'
            security_vulnerability:
              $ref: '#/components/schemas/SecurityVulnerability'
            url:
              type: string
              format: uri
              description: The REST API URL of the alert resource.
            html_url:
              type: string
              format: uri
              description: The GitHub URL of the alert resource.
            created_at:
              type: string
              format: date-time
              description: "The time that the alert was created in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`."
            updated_at:
              type: string
              format: date-time
              description: "The time that the alert was last updated in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`."
            dismissed_at:
              type: string
              format: date-time
              nullable: true
              description: "The time that the alert was dismissed in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`."
            dismissed_by:
              $ref: '#/components/schemas/User'
              nullable: true
            dismissed_reason:
              type: string
              nullable: true
              description: The reason that the alert was dismissed.
            dismissed_comment:
              type: string
              nullable: true
              description: An optional comment associated with the alert's dismissal.
            fixed_at:
              type: string
              format: date-time
              nullable: true
              description: "The time that the alert was no longer detected and was considered fixed in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`."
            auto_dismissed_at:
              type: string
              format: date-time
              nullable: true
              description: "The time that the alert was auto-dismissed in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`."
            assignees:
              type: array
              items:
                $ref: '#/components/schemas/User'
              description: The users assigned to this alert.
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    GollumPayload:
      type: object
      description: Payload for gollum (wiki) events
      required:
        - pages
        - sender
        - repository
      properties:
        pages:
          type: array
          description: The pages that were updated
          items:
            description: "A wiki page that was created or updated."
            type: object
            required:
              - page_name
              - title
              - action
              - sha
              - html_url
            properties:
              page_name:
                type: string
                description: The name of the page
              title:
                type: string
                description: The current page title
              summary:
                type: string
                nullable: true
                description: A summary of the changes
              action:
                description: "The action performed on the page, either created or edited."
                type: string
                enum:
                  - created
                  - edited
              sha:
                type: string
                description: The latest commit SHA of the page
              html_url:
                description: "The URL of the resource on GitHub.com."
                type: string
                format: uri
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    InstallationPayload:
      type: object
      description: Payload for installation events
      required:
        - action
        - installation
      properties:
        action:
          description: "The action that was performed. Can be one of: created, deleted, suspend, unsuspend, new_permissions_accepted."
          type: string
          enum:
            - created
            - deleted
            - new_permissions_accepted
            - suspend
            - unsuspend
        installation:
          $ref: '#/components/schemas/Installation'
        repositories:
          type: array
          description: An array of repositories the installation can access
          items:
            description: "A minimal repository reference included in installation events."
            type: object
            properties:
              id:
                description: "The unique identifier of the resource."
                type: integer
              node_id:
                description: "The GraphQL node ID."
                type: string
              name:
                description: "The name of the repository."
                type: string
              full_name:
                description: "The full name of the repository, in the form `owner/repo`."
                type: string
              private:
                description: "Whether the repository is private."
                type: boolean
        requester:
          $ref: '#/components/schemas/User'
          nullable: true
        sender:
          $ref: '#/components/schemas/User'
        organization:
          $ref: '#/components/schemas/Organization'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    InstallationRepositoriesPayload:
      type: object
      description: Payload for installation_repositories events
      required:
        - action
        - repositories_added
        - repositories_removed
        - repository_selection
        - requester
      properties:
        action:
          description: "The action that was performed. Can be one of: added, removed."
          type: string
          enum:
            - added
            - removed
        repositories_added:
          type: array
          description: Repositories added to the installation
          items:
            description: "A repository added to a GitHub App installation."
            type: object
            properties:
              id:
                description: "The unique identifier of the resource."
                type: integer
              node_id:
                description: "The GraphQL node ID."
                type: string
              name:
                description: "The name of the repository."
                type: string
              full_name:
                description: "The full name of the repository, in owner/repo format."
                type: string
              private:
                description: "Whether the repository is private."
                type: boolean
        repositories_removed:
          type: array
          description: Repositories removed from the installation
          items:
            description: "A repository removed from a GitHub App installation."
            type: object
            properties:
              id:
                description: "The unique identifier of the resource."
                type: integer
              node_id:
                description: "The GraphQL node ID."
                type: string
              name:
                description: "The name of the repository."
                type: string
              full_name:
                description: "The full name of the repository, in owner/repo format."
                type: string
              private:
                description: "Whether the repository is private."
                type: boolean
        repository_selection:
          type: string
          description: Whether all repositories or a selection are accessible
          enum:
            - all
            - selected
        requester:
          $ref: '#/components/schemas/User'
          nullable: true
        installation:
          $ref: '#/components/schemas/Installation'
        sender:
          $ref: '#/components/schemas/User'
        organization:
          $ref: '#/components/schemas/Organization'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    MembershipPayload:
      type: object
      description: Payload for membership events (team member added/removed)
      required:
        - action
        - member
        - scope
        - team
      properties:
        action:
          description: "The action that was performed. Can be one of: added, removed."
          type: string
          enum:
            - added
            - removed
        member:
          $ref: '#/components/schemas/User'
          nullable: true
        scope:
          type: string
          description: The scope of the membership (currently always "team")
          enum:
            - team
        team:
          $ref: '#/components/schemas/Team'
        sender:
          $ref: '#/components/schemas/User'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    MergeGroupPayload:
      type: object
      description: Payload for merge_group events
      required:
        - action
        - merge_group
      properties:
        action:
          description: "The action that was performed. Can be one of: checks_requested, destroyed."
          type: string
          enum:
            - checks_requested
            - destroyed
        merge_group:
          type: object
          description: A group of pull requests grouped together by the merge queue
          required:
            - head_sha
            - head_ref
            - base_sha
            - base_ref
          properties:
            head_sha:
              type: string
              description: The SHA of the merge group's head commit
            head_ref:
              type: string
              description: The full ref of the merge group targeting branch
            base_sha:
              type: string
              description: The SHA of the merge group's base branch
            base_ref:
              type: string
              description: The full ref of the branch being merged into
            head_commit:
              $ref: '#/components/schemas/Commit'
        reason:
          type: string
          description: For destroyed action, the reason the merge group was destroyed
          nullable: true
          enum:
            - dequeued
            - invalidated
            - merged
            - rejected
            - 
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    PublicPayload:
      type: object
      description: |
        Payload for public events. Fired when a repository visibility changes
        from private to public. No action field.
      required:
        - sender
        - repository
      properties:
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    RepositoryDispatchPayload:
      type: object
      description: |
        Payload for repository_dispatch events. The action field matches the
        event_type provided in the POST /repos/{owner}/{repo}/dispatches request.
      required:
        - action
        - branch
        - client_payload
      properties:
        action:
          type: string
          description: The event_type specified in the dispatch request body
        branch:
          type: string
          description: The branch from which the dispatch was triggered
        client_payload:
          type: object
          nullable: true
          description: The client_payload from the dispatch request body
          additionalProperties: true
        installation:
          $ref: '#/components/schemas/Installation'
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    SecretScanningAlertPayload:
      type: object
      description: Payload for secret_scanning_alert events
      required:
        - action
        - alert
      properties:
        action:
          description: "The action that was performed. Can be one of: assigned, reopened, unassigned, created, publicly_leaked, validated, resolved."
          type: string
          enum:
            - assigned
            - created
            - publicly_leaked
            - reopened
            - resolved
            - unassigned
            - validated
        alert:
          type: object
          description: The secret scanning alert
          required:
            - number
            - state
          properties:
            number:
              type: integer
              description: The security alert number.
            created_at:
              type: string
              format: date-time
              description: "The time that the alert was created in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`."
            updated_at:
              type: string
              format: date-time
              nullable: true
              description: "The time that the alert was last updated in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`."
            url:
              type: string
              format: uri
              description: The REST API URL of the alert resource.
            html_url:
              type: string
              format: uri
              description: The GitHub URL of the alert resource.
            locations_url:
              type: string
              format: uri
              description: The REST API URL of the code locations for this alert.
            state:
              description: "The state of the secret scanning alert. Can be open or resolved."
              type: string
              enum:
                - open
                - resolved
            resolution:
              type: string
              nullable: true
              enum:
                - false_positive
                - 
                - pattern_deleted
                - pattern_edited
                - revoked
                - used_in_tests
                - wont_fix
              description: The reason for resolving the alert.
            resolved_at:
              type: string
              format: date-time
              nullable: true
              description: "The time that the alert was resolved in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`."
            resolved_by:
              $ref: '#/components/schemas/User'
              nullable: true
            resolution_comment:
              type: string
              nullable: true
              description: An optional comment to resolve an alert.
            secret_type:
              type: string
              description: The type of secret that was detected
            secret_type_display_name:
              type: string
              description: User-friendly name for the detected secret, matching the `secret_type`.
            validity:
              type: string
              enum:
                - active
                - inactive
                - unknown
              description: The token status as of the latest validity check.
            publicly_leaked:
              type: boolean
              description: Whether the detected secret was publicly leaked.
            multi_repo:
              type: boolean
              description: Whether the detected secret was found in multiple repositories under the same organization or enterprise.
            push_protection_bypassed:
              type: boolean
              nullable: true
              description: Whether push protection was bypassed for the detected secret.
            push_protection_bypassed_by:
              $ref: '#/components/schemas/User'
              nullable: true
            push_protection_bypassed_at:
              type: string
              format: date-time
              nullable: true
              description: "The time that push protection was bypassed in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`."
        assignee:
          $ref: '#/components/schemas/User'
          description: Present on assigned/unassigned actions
          nullable: true
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    # -----------------------------------------------------------------------
    # Batch 4 event payload schemas
    # -----------------------------------------------------------------------

    BranchProtectionRulePayload:
      type: object
      description: Payload for branch_protection_rule events
      required:
        - action
        - rule
      properties:
        action:
          description: "The action that was performed. Can be one of: created, edited, deleted."
          type: string
          enum:
            - created
            - deleted
            - edited
        rule:
          type: object
          description: |
            The branch protection rule. Includes name and all branch protection
            settings applied to matching branches. Binary settings are boolean;
            multi-level configs are off, non_admins, or everyone; actor and
            build lists are arrays of strings.
          properties:
            id:
              type: integer
              description: The branch protection rule's database ID.
            repository_id:
              type: integer
              description: The ID of the repository the rule applies to.
            name:
              type: string
              description: The name of the branch, or branch name pattern, the rule applies to.
            created_at:
              type: string
              format: date-time
              description: The time the branch protection rule was created, in ISO 8601 format.
            updated_at:
              type: string
              format: date-time
              description: The time the branch protection rule was last updated, in ISO 8601 format.
            pull_request_reviews_enforcement_level:
              type: string
              enum: ["off", non_admins, everyone]
              description: Enforcement level for requiring an approving pull request review before merging.
            required_approving_review_count:
              type: integer
              description: The number of approving reviews required on a pull request before merging.
            dismiss_stale_reviews_on_push:
              type: boolean
              description: Whether approving reviews are automatically dismissed when someone pushes a new commit.
            require_code_owner_review:
              type: boolean
              description: Whether pull requests are blocked from merging until code owners review them.
            authorized_dismissal_actors_only:
              type: boolean
              description: Whether only specific users, teams, or apps can dismiss pull request reviews.
            ignore_approvals_from_contributors:
              type: boolean
              description: Whether approvals from contributors who have also pushed to the branch are ignored when counting required approvals.
            require_last_push_approval:
              type: boolean
              description: Whether the most recent push to the pull request must be approved by someone other than whoever pushed it.
            required_status_checks:
              type: array
              items:
                type: string
              description: The list of status checks that must pass before merging into the branch.
            required_status_checks_enforcement_level:
              type: string
              enum: ["off", non_admins, everyone]
              description: Enforcement level for required status checks.
            strict_required_status_checks_policy:
              type: boolean
              description: Whether branches must be up to date with the base branch before merging.
            signature_requirement_enforcement_level:
              type: string
              enum: ["off", non_admins, everyone]
              description: Enforcement level for requiring signed commits.
            linear_history_requirement_enforcement_level:
              type: string
              enum: ["off", non_admins, everyone]
              description: Enforcement level for requiring a linear commit history.
            admin_enforced:
              type: boolean
              description: Whether the branch protection rule's restrictions are also enforced for repository administrators.
            allow_force_pushes_enforcement_level:
              type: string
              enum: ["off", non_admins, everyone]
              description: Enforcement level for permitting force pushes to matching branches.
            allow_deletions_enforcement_level:
              type: string
              enum: ["off", non_admins, everyone]
              description: Enforcement level for permitting deletion of matching branches.
            merge_queue_enforcement_level:
              type: string
              enum: ["off", non_admins, everyone]
              description: Enforcement level for requiring merges to go through a merge queue.
            required_deployments_enforcement_level:
              type: string
              enum: ["off", non_admins, everyone]
              description: Enforcement level for requiring successful deployments to specific environments before merging.
            required_conversation_resolution_level:
              type: string
              enum: ["off", non_admins, everyone]
              description: Enforcement level for requiring all pull request conversations to be resolved before merging.
            authorized_actors_only:
              type: boolean
              description: Whether only specific users, teams, or apps can push to the branch.
            authorized_actor_names:
              type: array
              items:
                type: string
              description: The names of the users, teams, or apps authorized to push to the branch.
        changes:
          type: object
          description: For edited events, the changes to the rule - one entry per changed setting, each wrapped in "from"
          properties:
            pull_request_reviews_enforcement_level:
              type: object
              description: The rule's previous pull request review enforcement level, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: string
            required_approving_review_count:
              type: object
              description: The rule's previous required approving review count, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: integer
            dismiss_stale_reviews_on_push:
              type: object
              description: The rule's previous dismiss-stale-reviews-on-push setting, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: boolean
            require_code_owner_review:
              type: object
              description: The rule's previous require-code-owner-review setting, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: boolean
            authorized_dismissal_actors_only:
              type: object
              description: The rule's previous authorized-dismissal-actors-only setting, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: boolean
            ignore_approvals_from_contributors:
              type: object
              description: The rule's previous ignore-approvals-from-contributors setting, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: boolean
            require_last_push_approval:
              type: object
              description: The rule's previous require-last-push-approval setting, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: boolean
            required_status_checks:
              type: object
              description: The rule's previous list of required status checks, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: array
                  items:
                    type: string
            required_status_checks_enforcement_level:
              type: object
              description: The rule's previous required status checks enforcement level, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: string
            strict_required_status_checks_policy:
              type: object
              description: The rule's previous strict required status checks policy setting, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: boolean
            signature_requirement_enforcement_level:
              type: object
              description: The rule's previous signature requirement enforcement level, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: string
            linear_history_requirement_enforcement_level:
              type: object
              description: The rule's previous linear history requirement enforcement level, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: string
            admin_enforced:
              type: object
              description: The rule's previous admin-enforced setting, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: boolean
            allow_force_pushes_enforcement_level:
              type: object
              description: The rule's previous allow-force-pushes enforcement level, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: string
            allow_deletions_enforcement_level:
              type: object
              description: The rule's previous allow-deletions enforcement level, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: string
            merge_queue_enforcement_level:
              type: object
              description: The rule's previous merge queue enforcement level, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: string
            required_deployments_enforcement_level:
              type: object
              description: The rule's previous required deployments enforcement level, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: string
            required_conversation_resolution_level:
              type: object
              description: The rule's previous required conversation resolution enforcement level, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: string
            authorized_actors_only:
              type: object
              description: The rule's previous authorized-actors-only setting, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: boolean
            authorized_actor_names:
              type: object
              description: The rule's previous list of authorized actor names, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: array
                  items:
                    type: string
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    CodeScanningAlertPayload:
      type: object
      description: Payload for code_scanning_alert events
      required:
        - action
        - alert
        - commit_oid
        - ref
      properties:
        action:
          description: "The action that was performed. Can be one of: appeared_in_branch, closed_by_user, created, fixed, reopened, reopened_by_user, updated_assignment."
          type: string
          enum:
            - appeared_in_branch
            - closed_by_user
            - created
            - fixed
            - reopened
            - reopened_by_user
            - updated_assignment
        alert:
          type: object
          description: The code scanning alert involved in the event
          required:
            - number
            - state
          properties:
            number:
              type: integer
              description: The security alert number.
            created_at:
              type: string
              format: date-time
              description: "The time that the alert was created in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`."
            updated_at:
              type: string
              format: date-time
              nullable: true
              description: "The time that the alert was last updated in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`."
            url:
              type: string
              format: uri
              description: The REST API URL of the alert resource.
            html_url:
              type: string
              format: uri
              description: The GitHub URL of the alert resource.
            state:
              type: string
              enum:
                - open
                - closed
                - dismissed
                - fixed
              description: State of a code scanning alert.
            fixed_at:
              type: string
              format: date-time
              nullable: true
              description: "The time that the alert was no longer detected and was considered fixed in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`."
            dismissed_by:
              $ref: '#/components/schemas/User'
              nullable: true
            dismissed_at:
              type: string
              format: date-time
              nullable: true
              description: "The time that the alert was dismissed in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`."
            dismissed_reason:
              type: string
              nullable: true
              enum:
                - false positive
                - used in tests
                - won't fix
                - 
              description: The reason for dismissing or closing the alert.
            dismissed_comment:
              type: string
              nullable: true
              description: The dismissal comment associated with the dismissal of the alert.
            rule:
              description: "The rule that triggered the code scanning alert."
              type: object
              properties:
                id:
                  description: "The unique identifier of the resource."
                  type: string
                severity:
                  description: "The severity of the alert, e.g. note, warning, or error."
                  type: string
                  enum: [error, warning, note, none]
                security_severity_level:
                  description: "The security severity level of the alert, e.g. low, medium, high, or critical."
                  type: string
                  nullable: true
                  enum: [critical, high, medium, low, "null"]
                description:
                  description: "A short description of the rule."
                  type: string
                name:
                  description: "The name of the rule."
                  type: string
                full_description:
                  description: "The full description of the rule."
                  type: string
                tags:
                  description: "Tags associated with the rule."
                  type: array
                  items:
                    type: string
                help:
                  description: "Help text describing how to fix the issue."
                  type: string
                  nullable: true
            tool:
              description: "The tool that generated the code scanning alert."
              type: object
              properties:
                name:
                  description: "The name of the tool."
                  type: string
                guid:
                  description: "The GUID of the tool."
                  type: string
                  nullable: true
                version:
                  description: "The version of the tool."
                  type: string
                  nullable: true
            most_recent_instance:
              description: "The most recent instance of the code scanning alert."
              type: object
              properties:
                ref:
                  description: "The full Git reference, formatted as `refs/heads/<branch name>`."
                  type: string
                analysis_key:
                  description: "Identifies the configuration under which the analysis was executed, e.g. workflow and job name."
                  type: string
                environment:
                  description: "Identifies the variable values of the environment the analysis was performed in, e.g. the language analyzed."
                  type: string
                state:
                  description: "The state of the alert instance. Can be one of: open, dismissed, fixed."
                  type: string
                commit_sha:
                  description: "The SHA of the commit this alert instance was found in."
                  type: string
                location:
                  type: object
                  description: |
                    Where in the code this alert instance was found - unrelated to, and a
                    different shape from, secret_scanning's own "location" field elsewhere in
                    this spec (that one identifies a commit/path, this one a line/column range).
                  properties:
                    path:
                      description: "The file path where the flagged code is located."
                      type: string
                    start_line:
                      description: "The line number where the flagged code range begins."
                      type: integer
                    end_line:
                      description: "The line number where the flagged code range ends."
                      type: integer
                    start_column:
                      description: "The column number where the flagged code range begins."
                      type: integer
                    end_column:
                      description: "The column number where the flagged code range ends."
                      type: integer
        commit_oid:
          type: string
          description: |
            The commit SHA of the alert. Empty when action is reopened_by_user
            or closed_by_user.
        ref:
          type: string
          description: |
            The git ref of the alert. Empty when action is reopened_by_user
            or closed_by_user.
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    DeployKeyPayload:
      type: object
      description: Payload for deploy_key events
      required:
        - action
        - key
      properties:
        action:
          description: "The action that was performed. Can be one of: created, deleted."
          type: string
          enum:
            - created
            - deleted
        key:
          type: object
          description: The deploy key resource
          required:
            - id
            - key
          properties:
            id:
              description: "The unique identifier of the resource."
              type: integer
            key:
              type: string
              description: The public key
            url:
              description: "The API URL of the resource."
              type: string
              format: uri
            title:
              description: "The user-supplied label for the deploy key."
              type: string
            verified:
              description: "Whether GitHub has verified the deploy key."
              type: boolean
            created_at:
              description: "The date and time the resource was created, in ISO 8601 format."
              type: string
              format: date-time
            read_only:
              description: "Whether the deploy key has read-only (not read-write) access."
              type: boolean
            added_by:
              description: "The username of the user who added the deploy key, if known."
              type: string
              nullable: true
            last_used:
              description: "The date and time the deploy key was last used, in ISO 8601 format."
              type: string
              format: date-time
              nullable: true
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    GithubAppAuthorizationPayload:
      type: object
      description: Payload for github_app_authorization events (app webhooks only)
      required:
        - action
      properties:
        action:
          type: string
          enum:
            - revoked
          description: Always revoked — a user revoked their GitHub App authorization
        sender:
          $ref: '#/components/schemas/User'
        installation:
          $ref: '#/components/schemas/Installation'

    MetaPayload:
      type: object
      description: Payload for meta events (webhook lifecycle)
      required:
        - action
        - hook_id
        - hook
      properties:
        action:
          type: string
          enum:
            - deleted
          description: Always deleted — the webhook that triggered this event was deleted
        hook_id:
          type: integer
          description: The id of the modified webhook
        hook:
          type: object
          description: |
            The deleted webhook. Fields vary by webhook type (repository,
            organization, business, app, or GitHub Marketplace).
          required:
            - type
            - id
            - name
            - active
          properties:
            type:
              description: "The type of resource the webhook is attached to, e.g. Repository, Organization, App, Business."
              type: string
              enum:
                - Repository
                - Organization
                - Business
                - App
                - Marketplace
            id:
              type: integer
              description: Unique identifier of the webhook.
            name:
              type: string
              description: The name of a valid service, use 'web' for a webhook.
            active:
              type: boolean
              description: Determines whether the hook is actually triggered on pushes.
            events:
              type: array
              items:
                type: string
              description: "Determines what events the hook is triggered for. Default: ['push']."
            config:
              description: "The webhook's delivery configuration (URL, content type, SSL verification)."
              type: object
              properties:
                content_type:
                  description: "The media type used to serialize the payloads, e.g. `json` or `form`."
                  type: string
                  enum: [json, form]
                insecure_ssl:
                  description: "Whether SSL certificate verification is skipped when delivering payloads."
                  type: string
                url:
                  description: "The URL to which the payloads will be delivered."
                  type: string
                  format: uri
                secret:
                  type: string
                  description: Omitted from payloads for security
            updated_at:
              description: "The date and time the resource was last updated, in ISO 8601 format."
              type: string
              format: date-time
            created_at:
              description: "The date and time the resource was created, in ISO 8601 format."
              type: string
              format: date-time
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    OrgBlockPayload:
      type: object
      description: Payload for org_block events
      required:
        - action
        - blocked_user
      properties:
        action:
          description: "The action that was performed. Can be one of: blocked, unblocked."
          type: string
          enum:
            - blocked
            - unblocked
        blocked_user:
          $ref: '#/components/schemas/User'
          nullable: true
        sender:
          $ref: '#/components/schemas/User'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    PackagePayload:
      type: object
      description: Payload for package events (GitHub Packages)
      required:
        - action
        - package
      properties:
        action:
          description: "The action that was performed. Can be one of: published, updated."
          type: string
          enum:
            - published
            - updated
        package:
          type: object
          description: Information about the package
          required:
            - id
            - name
          properties:
            id:
              type: integer
              description: Unique identifier of the package.
            name:
              type: string
              description: The name of the package.
            namespace:
              description: "The namespace of the package, e.g. npm scope or repository owner."
              type: string
            description:
              description: "A short description of the package."
              type: string
              nullable: true
            ecosystem:
              description: "The package ecosystem (e.g. npm, docker, maven, nuget, rubygems)."
              type: string
            package_type:
              type: string
              enum:
                - container
                - docker
                - maven
                - npm
                - nuget
                - rubygems
              description: "The package registry type, e.g. maven, container, docker, npm, nuget, or rubygems."
            html_url:
              description: "The URL of the resource on GitHub.com."
              type: string
              format: uri
            created_at:
              description: "The date and time the resource was created, in ISO 8601 format."
              type: string
              format: date-time
            updated_at:
              description: "The date and time the resource was last updated, in ISO 8601 format."
              type: string
              format: date-time
            owner:
              $ref: '#/components/schemas/User'
            package_version:
              description: "The published version of a package, including registry-specific metadata."
              type: object
              nullable: true
              properties:
                id:
                  description: "The unique identifier of the resource."
                  type: integer
                version:
                  description: "The version string of this package version."
                  type: string
                summary:
                  description: "A short summary of this package version."
                  type: string
                  nullable: true
                name:
                  description: "The name of the package this version belongs to."
                  type: string
                description:
                  description: "The description of this package version."
                  type: string
                  nullable: true
                body:
                  description: "The release notes body for this package version, in Markdown."
                  type: string
                  nullable: true
                body_html:
                  description: "The release notes body for this package version, rendered as HTML."
                  type: string
                  nullable: true
                release:
                  type: object
                  nullable: true
                  description: |
                    A reduced Release shape as delivered on a package version, not the full
                    Release resource (missing node_id, assets_url, upload_url, body, assets) -
                    must not $ref Release.
                  properties:
                    url:
                      description: "The API URL of the resource."
                      type: string
                      format: uri
                    html_url:
                      description: "The URL of the resource on GitHub.com."
                      type: string
                      format: uri
                    id:
                      description: "The unique identifier of the resource."
                      type: integer
                    tag_name:
                      type: string
                      description: The name of the tag.
                    target_commitish:
                      type: string
                      description: Specifies the commitish value that determines where the Git tag is created from.
                    name:
                      description: "The name of the release."
                      type: string
                      nullable: true
                    draft:
                      type: boolean
                      description: Whether the release is a draft or published.
                    author:
                      $ref: '#/components/schemas/User'
                    prerelease:
                      type: boolean
                      description: Whether the release is identified as a prerelease or a full release.
                    created_at:
                      description: "The date and time the resource was created, in ISO 8601 format."
                      type: string
                      format: date-time
                    published_at:
                      description: "The date and time the release was published, in ISO 8601 format."
                      type: string
                      format: date-time
                      nullable: true
                manifest:
                  description: "The raw package manifest file content for this version."
                  type: string
                  nullable: true
                html_url:
                  description: "The URL of the resource on GitHub.com."
                  type: string
                  format: uri
                tag_name:
                  description: "The name of the Git tag associated with this package version."
                  type: string
                  nullable: true
                target_commitish:
                  description: "The branch or commit the associated Git tag targets."
                  type: string
                target_oid:
                  description: "The resolved Git commit SHA that the associated tag targets."
                  type: string
                draft:
                  description: "Whether this package version is a draft."
                  type: boolean
                prerelease:
                  description: "Whether this package version is a prerelease."
                  type: boolean
                created_at:
                  description: "The date and time the resource was created, in ISO 8601 format."
                  type: string
                  format: date-time
                updated_at:
                  description: "The date and time the resource was last updated, in ISO 8601 format."
                  type: string
                  format: date-time
                metadata:
                  type: array
                  description: |
                    Genuinely arbitrary per GitHub's own webhook schema - each entry declares no
                    fixed shape (additionalProperties: true, no properties), confirmed via
                    github/rest-api-description's webhook-package-published schema.
                  items:
                    type: object
                    additionalProperties: true
                container_metadata:
                  type: object
                  nullable: true
                  description: Metadata for a container (Docker) package version
                  properties:
                    tag:
                      description: "The digest and name of a container image tag."
                      type: object
                      properties:
                        digest:
                          description: "The digest of the container image tag."
                          type: string
                        name:
                          description: "The name of the container image tag."
                          type: string
                    labels:
                      type: object
                      nullable: true
                      description: Untyped even in GitHub's own webhook schema - genuinely open, not incomplete.
                      additionalProperties: true
                    manifest:
                      type: object
                      nullable: true
                      description: Untyped even in GitHub's own webhook schema - genuinely open, not incomplete.
                      additionalProperties: true
                npm_metadata:
                  description: "Metadata for an npm package version."
                  type: object
                  nullable: true
                  properties:
                    name:
                      description: "The name of the npm package."
                      type: string
                    version:
                      description: "The version of the npm package."
                      type: string
                    npm_user:
                      description: "The npm username of the publisher."
                      type: string
                    author:
                      description: "The author of the npm package, as recorded in package.json."
                      type: object
                      nullable: true
                      additionalProperties: true
                    bugs:
                      description: "The bug tracker information from package.json."
                      type: object
                      nullable: true
                      additionalProperties: true
                    dependencies:
                      description: "The package's runtime dependencies, as recorded in package.json."
                      type: object
                      additionalProperties: true
                    dev_dependencies:
                      description: "The development dependencies declared in the package's package.json."
                      type: object
                      additionalProperties: true
                    peer_dependencies:
                      description: "The peer dependencies declared in the package's package.json."
                      type: object
                      additionalProperties: true
                    optional_dependencies:
                      description: "The optional dependencies declared in the package's package.json."
                      type: object
                      additionalProperties: true
                    description:
                      description: "The description declared in the package's package.json."
                      type: string
                    dist:
                      description: "The npm registry distribution metadata for this version (e.g. tarball URL, checksum)."
                      type: object
                      nullable: true
                      additionalProperties: true
                    git_head:
                      description: "The Git commit SHA the package was published from, if recorded."
                      type: string
                    homepage:
                      description: "The homepage URL declared in the package's package.json."
                      type: string
                    license:
                      description: "The license identifier declared in the package's package.json."
                      type: string
                    main:
                      description: "The path to the package's main entry file, as declared in package.json."
                      type: string
                    repository:
                      description: "The source repository declared in the package's package.json."
                      type: object
                      nullable: true
                      additionalProperties: true
                    scripts:
                      description: "The npm scripts declared in the package's package.json."
                      type: object
                      additionalProperties: true
                    id:
                      description: "The unique identifier of the resource."
                      type: string
                    node_version:
                      description: "The Node.js version used to publish this package version."
                      type: string
                    npm_version:
                      description: "The npm client version used to publish this package version."
                      type: string
                    has_shrinkwrap:
                      description: "Whether the package version includes an npm-shrinkwrap.json file."
                      type: boolean
                    maintainers:
                      description: "The list of maintainers declared for this package on the npm registry."
                      type: array
                      items:
                        type: object
                        additionalProperties: true
                    contributors:
                      description: "The list of contributors declared in the package's package.json."
                      type: array
                      items:
                        type: object
                        additionalProperties: true
                    engines:
                      description: "The Node.js/npm version constraints declared in the package's package.json."
                      type: object
                      additionalProperties: true
                    keywords:
                      description: "The keywords declared in the package's package.json."
                      type: array
                      items:
                        type: string
                    files:
                      description: "The file patterns to include when publishing, as declared in package.json."
                      type: array
                      items:
                        type: string
                    bin:
                      description: "The executable scripts declared in the package's package.json."
                      type: object
                      additionalProperties: true
                    man:
                      description: "The man page files declared in the package's package.json."
                      type: object
                      additionalProperties: true
                    directories:
                      description: "The directory layout hints declared in the package's package.json."
                      type: object
                      nullable: true
                      additionalProperties: true
                    os:
                      description: "The operating systems this package version supports, as declared in package.json."
                      type: array
                      items:
                        type: string
                    cpu:
                      description: "The CPU architectures this package version supports, as declared in package.json."
                      type: array
                      items:
                        type: string
                    readme:
                      description: "The contents of the package's README file."
                      type: string
                    installation_command:
                      description: "The command used to install this package version."
                      type: string
                    release_id:
                      description: "The identifier of the GitHub release associated with this package version."
                      type: integer
                    commit_oid:
                      description: "The Git commit SHA associated with this package version."
                      type: string
                    published_via_actions:
                      description: "Whether this package version was published via a GitHub Actions workflow."
                      type: boolean
                    deleted_by_id:
                      description: "The identifier of the user who deleted this package version, if applicable."
                      type: integer
                nuget_metadata:
                  description: "NuGet-specific metadata for this package version."
                  type: array
                  nullable: true
                  items:
                    description: "A single metadata entry for a NuGet package version."
                    type: object
                    properties:
                      id:
                        type: string
                        description: Real payloads may send this as either an integer or a string
                      name:
                        description: "The name of this metadata attribute, as defined in the package's .nuspec file."
                        type: string
                      value:
                        type: string
                        description: |
                          Real payloads may send this as a boolean, string, integer, or an object
                          with url/branch/commit/type fields - kept as string here since AsyncAPI
                          schemas have no first-class "any of these primitive types" construct;
                          the object variant's fields are lost by this simplification.
                rubygems_metadata:
                  description: "RubyGems-specific metadata for this package version."
                  type: array
                  nullable: true
                  items:
                    description: "Metadata for a RubyGems package version."
                    type: object
                    properties:
                      name:
                        description: "The name of this RubyGems package."
                        type: string
                      description:
                        description: "The description of this RubyGems package."
                        type: string
                      readme:
                        description: "The contents of this RubyGems package's README file."
                        type: string
                      homepage:
                        description: "The homepage URL of this RubyGems package."
                        type: string
                      version_info:
                        description: "Version information for a RubyGems package."
                        type: object
                        properties:
                          version:
                            description: "The version string of this RubyGems package."
                            type: string
                      platform:
                        description: "The target platform of this RubyGems package (e.g. ruby, java)."
                        type: string
                      metadata:
                        description: "Additional key-value metadata declared in the package's gemspec."
                        type: object
                        additionalProperties:
                          type: string
                      repo:
                        description: "The source repository URL of this RubyGems package."
                        type: string
                      dependencies:
                        description: "The runtime dependencies declared in the package's gemspec."
                        type: array
                        items:
                          type: object
                          additionalProperties:
                            type: string
                      commit_oid:
                        description: "The Git commit SHA associated with this package version."
                        type: string
                package_files:
                  description: "The downloadable files included in this package version."
                  type: array
                  items:
                    type: object
                    description: A single downloadable file within a published package version
                    properties:
                      download_url:
                        description: "The URL to download this package file."
                        type: string
                        format: uri
                      id:
                        description: "The unique identifier of the resource."
                        type: integer
                      name:
                        description: "The file name of this package file."
                        type: string
                      sha256:
                        description: "The SHA-256 checksum of this package file."
                        type: string
                      sha1:
                        description: "The SHA-1 checksum of this package file."
                        type: string
                      md5:
                        description: "The MD5 checksum of this package file."
                        type: string
                      content_type:
                        description: "The MIME type of this package file."
                        type: string
                      state:
                        description: "The upload state of this package file (e.g. uploaded)."
                        type: string
                      size:
                        description: "The size of this package file, in bytes."
                        type: integer
                      created_at:
                        description: "The date and time the resource was created, in ISO 8601 format."
                        type: string
                        format: date-time
                      updated_at:
                        description: "The date and time the resource was last updated, in ISO 8601 format."
                        type: string
                        format: date-time
                package_url:
                  description: "The URL to view this package version."
                  type: string
                  nullable: true
                author:
                  $ref: '#/components/schemas/User'
                source_url:
                  description: "The URL of the source repository for this package version."
                  type: string
                  nullable: true
                installation_command:
                  description: "The command used to install this package version."
                  type: string
                  nullable: true
            registry:
              description: "The package registry a package version was published to."
              type: object
              nullable: true
              properties:
                about_url:
                  description: "The URL with more information about this registry."
                  type: string
                  format: uri
                name:
                  description: "The name of the registry."
                  type: string
                type:
                  description: "The type of the registry."
                  type: string
                url:
                  description: "The API URL of the resource."
                  type: string
                  format: uri
                vendor:
                  description: "The vendor that operates this registry."
                  type: string
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    ProjectsV2Payload:
      type: object
      description: Payload for projects_v2 events (organization-level Projects)
      required:
        - action
        - projects_v2
      properties:
        action:
          description: "The action that was performed. Can be one of: created, edited, closed, reopened, deleted."
          type: string
          enum:
            - closed
            - created
            - deleted
            - edited
            - reopened
        projects_v2:
          type: object
          description: A Projects v2 project
          required:
            - id
            - node_id
            - owner
            - title
          properties:
            id:
              type: integer
              description: The unique identifier of the project.
            node_id:
              type: string
              description: The node ID of the project.
            owner:
              $ref: '#/components/schemas/User'
            creator:
              $ref: '#/components/schemas/User'
            title:
              type: string
              description: The project title.
            description:
              type: string
              nullable: true
              description: A short description of the project.
            public:
              type: boolean
              description: Whether the project is visible to anyone with access to the owner.
            closed_at:
              type: string
              format: date-time
              nullable: true
              description: The time when the project was closed.
            created_at:
              type: string
              format: date-time
              description: The time when the project was created.
            updated_at:
              type: string
              format: date-time
              description: The time when the project was last updated.
            deleted_at:
              type: string
              format: date-time
              nullable: true
              description: The time when the project was deleted.
            deleted_by:
              $ref: '#/components/schemas/User'
              nullable: true
            number:
              type: integer
              description: The project number.
            short_description:
              type: string
              nullable: true
              description: A concise summary of the project.
            status:
              description: "The project's status; undocumented field, exact semantics unconfirmed."
              type: string
              nullable: true
        sender:
          $ref: '#/components/schemas/User'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'

    RepositoryAdvisoryPayload:
      type: object
      description: Payload for repository_advisory events
      required:
        - action
        - repository_advisory
      properties:
        action:
          description: "The action that was performed. Can be one of: reported, published."
          type: string
          enum:
            - published
            - reported
        repository_advisory:
          type: object
          description: A repository security advisory
          required:
            - ghsa_id
            - cve_id
            - summary
            - severity
            - state
          properties:
            ghsa_id:
              type: string
              description: The GitHub Security Advisory identifier
            cve_id:
              type: string
              nullable: true
              description: The Common Vulnerabilities and Exposures (CVE) ID.
            url:
              type: string
              format: uri
              description: The API URL for the advisory.
            html_url:
              type: string
              format: uri
              description: The URL for the advisory.
            summary:
              type: string
              description: A short summary of the advisory.
            description:
              type: string
              nullable: true
              description: A detailed description of what the advisory entails.
            severity:
              type: string
              enum:
                - critical
                - high
                - medium
                - low
                - unknown
              description: The severity of the advisory.
            author:
              $ref: '#/components/schemas/User'
            publisher:
              $ref: '#/components/schemas/User'
              nullable: true
            identifiers:
              description: "The CVE and/or GHSA identifiers for the advisory."
              type: array
              items:
                description: "A CVE or GHSA identifier for the advisory."
                type: object
                properties:
                  type:
                    description: "The type of identifier, e.g. CVE or GHSA."
                    type: string
                    enum: [CVE, GHSA]
                  value:
                    description: "The identifier value."
                    type: string
            state:
              type: string
              enum:
                - draft
                - published
                - triage
                - withdrawn
              description: The state of the advisory.
            created_at:
              type: string
              format: date-time
              description: The date and time of when the advisory was created, in ISO 8601 format.
            updated_at:
              type: string
              format: date-time
              description: The date and time of when the advisory was last updated, in ISO 8601 format.
            published_at:
              type: string
              format: date-time
              nullable: true
              description: The date and time of when the advisory was published, in ISO 8601 format.
            withdrawn_at:
              type: string
              format: date-time
              nullable: true
              description: The date and time of when the advisory was withdrawn, in ISO 8601 format.
            submission:
              type: object
              nullable: true
              description: The accept/reject decision on a repository security advisory publish request
              properties:
                accepted:
                  type: boolean
                  description: Whether a private vulnerability report was accepted by the repository's administrators.
            vulnerabilities:
              type: array
              description: The products affected by the vulnerability detailed in the advisory.
              items:
                description: "One package version range affected by the advisory."
                type: object
                properties:
                  package:
                    description: "The package affected by the vulnerability."
                    type: object
                    properties:
                      ecosystem:
                        description: "The package ecosystem the affected package belongs to, e.g. npm."
                        type: string
                      name:
                        description: "The name of the affected package."
                        type: string
                  vulnerable_version_range:
                    description: "The range of versions affected by the vulnerability."
                    type: string
                    nullable: true
                  patched_versions:
                    description: "The versions that contain a patch for the vulnerability."
                    type: string
                    nullable: true
                  vulnerable_functions:
                    description: "The functions affected by the vulnerability."
                    type: array
                    items:
                      type: string
            cvss:
              description: "The CVSS score and vector for the advisory."
              type: object
              nullable: true
              properties:
                vector_string:
                  description: "The CVSS vector string describing the vulnerability's characteristics."
                  type: string
                  nullable: true
                score:
                  description: "The CVSS score of the vulnerability."
                  type: number
                  nullable: true
            cwes:
              description: "The CWE classifications for the advisory."
              type: array
              nullable: true
              items:
                description: "A CWE (Common Weakness Enumeration) classification for the advisory."
                type: object
                properties:
                  cwe_id:
                    description: "The CWE identifier, e.g. CWE-79."
                    type: string
                  name:
                    description: "The name of the CWE weakness."
                    type: string
            credits:
              description: "The users credited for reporting or fixing the advisory."
              type: array
              nullable: true
              items:
                description: "A user credited for reporting or fixing the advisory."
                type: object
                properties:
                  user:
                    $ref: '#/components/schemas/User'
                  type:
                    description: "The type of credit given to the user, e.g. reporter."
                    type: string
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    SecurityAdvisoryPayload:
      type: object
      description: Payload for security_advisory events (GitHub-reviewed global advisories)
      required:
        - action
        - security_advisory
      properties:
        action:
          description: "The action that was performed. Can be one of: withdrawn, published, updated."
          type: string
          enum:
            - published
            - updated
            - withdrawn
        security_advisory:
          type: object
          description: |
            The details of the global security advisory, including summary,
            description, severity, and affected packages.
          required:
            - ghsa_id
            - summary
            - severity
          properties:
            schema_version:
              description: "The schema version of the security advisory payload, when present."
              type: string
            ghsa_id:
              type: string
              description: The GitHub Security Advisory ID.
            cve_id:
              type: string
              nullable: true
              description: The Common Vulnerabilities and Exposures (CVE) ID.
            url:
              type: string
              format: uri
              description: The API URL for the advisory.
            html_url:
              type: string
              format: uri
              description: The URL for the advisory.
            summary:
              type: string
              description: A short summary of the advisory.
            description:
              type: string
              description: A detailed description of what the advisory entails.
            severity:
              type: string
              enum:
                - critical
                - high
                - medium
                - low
                - unknown
              description: The severity of the advisory.
            identifiers:
              description: "The advisory's CVE and GHSA identifiers."
              type: array
              items:
                description: "An identifier (CVE or GHSA) for the security advisory."
                type: object
                properties:
                  value:
                    description: "The identifier value."
                    type: string
                  type:
                    description: "The type of identifier. Can be one of: CVE, GHSA."
                    type: string
                    enum: [CVE, GHSA]
            references:
              description: "URLs with more information about the security advisory."
              type: array
              items:
                description: "A URL providing more information about the security advisory."
                type: object
                properties:
                  url:
                    description: "A URL with more information about the security advisory."
                    type: string
                    format: uri
            published_at:
              type: string
              format: date-time
              description: The date and time of when the advisory was published, in ISO 8601 format.
            updated_at:
              type: string
              format: date-time
              description: The date and time of when the advisory was last updated, in ISO 8601 format.
            withdrawn_at:
              type: string
              format: date-time
              nullable: true
              description: The date and time of when the advisory was withdrawn, in ISO 8601 format.
            vulnerabilities:
              type: array
              items:
                description: "A package and version range affected by the advisory."
                type: object
                properties:
                  package:
                    description: "The vulnerable package affected by the advisory."
                    type: object
                    properties:
                      ecosystem:
                        description: "The package's language or package management ecosystem."
                        type: string
                      name:
                        description: "The unique package name within its ecosystem."
                        type: string
                  severity:
                    description: "The severity of the vulnerability, e.g. `low`, `moderate`, `high`, `critical`."
                    type: string
                    enum: [critical, high, medium, low, unknown]
                  vulnerable_version_range:
                    description: "The range of the package versions affected by the vulnerability."
                    type: string
                  first_patched_version:
                    description: "The first package version containing a fix for the vulnerability."
                    type: object
                    nullable: true
                    properties:
                      identifier:
                        description: "The package version that resolves the vulnerability."
                        type: string
              description: The products and respective version ranges affected by the advisory.
            cvss:
              description: "The CVSS score and vector string of the security advisory."
              type: object
              properties:
                vector_string:
                  description: "The CVSS vector string, or null if not scored."
                  type: string
                  nullable: true
                score:
                  description: "The CVSS score of the vulnerability."
                  type: number
            cwes:
              description: "The CWE weaknesses associated with the advisory."
              type: array
              items:
                description: "A CWE weakness classification associated with the advisory."
                type: object
                properties:
                  cwe_id:
                    description: "The Common Weakness Enumeration (CWE) identifier."
                    type: string
                  name:
                    description: "The name of the CWE weakness."
                    type: string
        installation:
          $ref: '#/components/schemas/Installation'

    # -----------------------------------------------------------------------
    # Batch 5 event payload schemas
    # -----------------------------------------------------------------------

    BranchProtectionConfigurationPayload:
      type: object
      description: Payload for branch_protection_configuration events
      required:
        - action
      properties:
        action:
          type: string
          enum:
            - disabled
            - enabled
          description: disabled — all branch protections were disabled. enabled — all were enabled.
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    CustomPropertyPayload:
      type: object
      description: Payload for custom_property events
      required:
        - action
        - definition
      properties:
        action:
          description: "The action that was performed. Can be one of: updated, deleted, promote_to_enterprise, created."
          type: string
          enum:
            - created
            - deleted
            - promote_to_enterprise
            - updated
        definition:
          type: object
          description: Custom property defined on an organization
          required:
            - property_name
            - value_type
          properties:
            property_name:
              type: string
              description: The name of the property.
            value_type:
              type: string
              enum:
                - string
                - single_select
                - multi_select
                - true_false
              description: The type of the value for the property.
            required:
              type: string
              nullable: true
              enum:
                - optional
                - required
                - all_repositories
                - new_repositories
              description: Whether the property is required.
            default_value:
              nullable: true
              description: Default value (string or array of strings)
            description:
              type: string
              nullable: true
              description: Short description of the property.
            allowed_values:
              type: array
              nullable: true
              items:
                type: string
              description: An ordered list of the allowed values of the property. The property can have up to 200 allowed values.
        sender:
          $ref: '#/components/schemas/User'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    CustomPropertyValuesPayload:
      type: object
      description: Payload for custom_property_values events
      required:
        - action
        - new_property_values
        - old_property_values
      properties:
        action:
          description: "The action that was performed. Always updated."
          type: string
          enum:
            - updated
        new_property_values:
          type: array
          description: The new custom property values for the repository
          items:
            description: "The new value of a custom property on the repository."
            type: object
            required:
              - property_name
            properties:
              property_name:
                description: "The name of the custom property."
                type: string
              value:
                nullable: true
                description: String or array of strings
        old_property_values:
          type: array
          description: The old custom property values for the repository
          items:
            description: "The previous value of a custom property on the repository."
            type: object
            required:
              - property_name
            properties:
              property_name:
                description: "The name of the custom property."
                type: string
              value:
                nullable: true
                description: String or array of strings
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    ProjectsV2ItemPayload:
      type: object
      description: Payload for projects_v2_item events
      required:
        - action
        - projects_v2_item
        - changes
      properties:
        action:
          description: "The action that was performed. Can be one of: edited, created, archived, deleted, restored, reordered, converted."
          type: string
          enum:
            - archived
            - converted
            - created
            - deleted
            - edited
            - reordered
            - restored
        projects_v2_item:
          type: object
          description: An item belonging to a Projects v2 project
          required:
            - id
            - node_id
            - project_node_id
            - content_node_id
            - content_type
          properties:
            id:
              type: integer
              description: The unique identifier of the project item.
            node_id:
              type: string
              description: The node ID of the project item.
            project_node_id:
              type: string
              description: The node ID of the project that contains this item.
            content_node_id:
              type: string
              description: The node ID of the content represented by this item.
            content_type:
              type: string
              description: The type of content tracked in the project item.
              enum:
                - DraftIssue
                - Issue
                - PullRequest
            created_at:
              type: string
              format: date-time
              description: The time when the item was created.
            updated_at:
              type: string
              format: date-time
              description: The time when the item was last updated.
            archived_at:
              type: string
              format: date-time
              nullable: true
              description: The time when the item was archived.
            creator:
              $ref: '#/components/schemas/User'
        changes:
          type: object
          description: The changes made to the item (for edited events)
          properties:
            field_value:
              type: object
              description: Which project field's value changed on this item.
              properties:
                field_node_id:
                  type: string
                  description: The node ID of the field whose value changed.
                field_type:
                  type: string
                  description: The type of the field whose value changed.
        sender:
          $ref: '#/components/schemas/User'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'

    ProjectsV2StatusUpdatePayload:
      type: object
      description: Payload for projects_v2_status_update events
      required:
        - action
        - projects_v2_status_update
      properties:
        action:
          description: "The action that was performed. Can be one of: created, edited, deleted."
          type: string
          enum:
            - created
            - deleted
            - edited
        projects_v2_status_update:
          type: object
          description: A status update belonging to a Projects v2 project
          required:
            - id
            - node_id
            - project_node_id
          properties:
            id:
              type: integer
              description: The unique identifier of the status update.
            node_id:
              type: string
              description: The node ID of the status update.
            project_node_id:
              type: string
              description: The node ID of the project that this status update belongs to.
            status:
              type: string
              nullable: true
              enum:
                - INACTIVE
                - ON_TRACK
                - AT_RISK
                - OFF_TRACK
                - COMPLETE
              description: The current status.
            body:
              type: string
              nullable: true
              description: Body of the status update.
            created_at:
              type: string
              format: date-time
              description: The time when the status update was created.
            updated_at:
              type: string
              format: date-time
              description: The time when the status update was last updated.
            start_date:
              type: string
              format: date
              nullable: true
              description: The start date of the period covered by the update.
            target_date:
              type: string
              format: date
              nullable: true
              description: The target date associated with the update.
            creator:
              $ref: '#/components/schemas/User'
        sender:
          $ref: '#/components/schemas/User'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'

    RegistryPackagePayload:
      type: object
      description: Payload for registry_package events (legacy GitHub Packages event)
      required:
        - action
        - registry_package
      properties:
        action:
          description: "The action that was performed. Can be one of: published, updated."
          type: string
          enum:
            - published
            - updated
        registry_package:
          type: object
          description: The registry package object
          required:
            - id
            - name
            - package_type
          properties:
            id:
              type: integer
              description: Unique identifier of the package.
            name:
              type: string
              description: The name of the package.
            namespace:
              description: "The namespace of the package, e.g. npm scope or repository owner."
              type: string
            description:
              description: "A short description of the package."
              type: string
              nullable: true
            ecosystem:
              description: "The package ecosystem (e.g. npm, docker, maven, nuget, rubygems)."
              type: string
            package_type:
              type: string
              enum:
                - CONTAINER
                - DOCKER
                - MAVEN
                - NPM
                - NUGET
                - RUBYGEMS
              description: "The package registry type, e.g. maven, container, docker, npm, nuget, or rubygems."
            html_url:
              description: "The URL of the resource on GitHub.com."
              type: string
              format: uri
            created_at:
              description: "The date and time the resource was created, in ISO 8601 format."
              type: string
              format: date-time
            updated_at:
              description: "The date and time the resource was last updated, in ISO 8601 format."
              type: string
              format: date-time
            owner:
              $ref: '#/components/schemas/User'
            package_version:
              description: "A specific published version of a package."
              type: object
              nullable: true
              properties:
                id:
                  description: "The unique identifier of the resource."
                  type: integer
                version:
                  description: "The version string of the package."
                  type: string
                summary:
                  description: "A short summary describing the package version."
                  type: string
                html_url:
                  description: "The URL of the resource on GitHub.com."
                  type: string
                  format: uri
                target_commitish:
                  description: "The branch or commit associated with this package version."
                  type: string
                target_oid:
                  description: "The SHA of the commit associated with this package version."
                  type: string
                draft:
                  description: "Whether this package version is a draft."
                  type: boolean
                prerelease:
                  description: "Whether this package version is a prerelease."
                  type: boolean
                created_at:
                  description: "The date and time the resource was created, in ISO 8601 format."
                  type: string
                  format: date-time
                updated_at:
                  description: "The date and time the resource was last updated, in ISO 8601 format."
                  type: string
                  format: date-time
                package_files:
                  description: "The list of downloadable files for this package version."
                  type: array
                  items:
                    description: "A single downloadable file within a package version."
                    type: object
                    properties:
                      download_url:
                        description: "The URL to download this package file."
                        type: string
                        format: uri
                      id:
                        description: "The unique identifier of the resource."
                        type: integer
                      name:
                        description: "The file name of the package file."
                        type: string
                      sha256:
                        description: "The SHA-256 checksum of the package file."
                        type: string
                      content_type:
                        description: "The MIME content type of the package file."
                        type: string
                      size:
                        description: "The size of the package file, in bytes."
                        type: integer
                      created_at:
                        description: "The date and time the resource was created, in ISO 8601 format."
                        type: string
                        format: date-time
                      updated_at:
                        description: "The date and time the resource was last updated, in ISO 8601 format."
                        type: string
                        format: date-time
                author:
                  $ref: '#/components/schemas/User'
                installation_command:
                  description: "The command used to install this package version."
                  type: string
                  nullable: true
            registry:
              description: "The package registry a package version was published to."
              type: object
              nullable: true
              properties:
                about_url:
                  description: "The URL with more information about the registry."
                  type: string
                  format: uri
                name:
                  description: "The name of the package registry."
                  type: string
                type:
                  description: "The type of the package registry, e.g. npm or docker."
                  type: string
                url:
                  description: "The API URL of the resource."
                  type: string
                  format: uri
                vendor:
                  description: "The vendor of the package registry."
                  type: string
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    RepositoryRulesetPayload:
      type: object
      description: Payload for repository_ruleset events
      required:
        - action
        - repository_ruleset
      properties:
        action:
          description: "The action that was performed. Can be one of: created, edited, deleted."
          type: string
          enum:
            - created
            - deleted
            - edited
        repository_ruleset:
          type: object
          description: A set of rules to apply when specified conditions are met
          required:
            - id
            - name
            - enforcement
          properties:
            id:
              type: integer
              description: The ID of the ruleset.
            name:
              type: string
              description: The name of the ruleset.
            target:
              type: string
              nullable: true
              enum:
                - branch
                - tag
                - push
              description: The target of the ruleset.
            source_type:
              type: string
              nullable: true
              enum:
                - Repository
                - Organization
              description: The type of the source of the ruleset.
            source:
              type: string
              description: The name of the source.
            enforcement:
              type: string
              enum:
                - disabled
                - active
                - evaluate
              description: The enforcement level of the ruleset. `evaluate` allows admins to test rules before enforcing them and view insights on the Rule Insights page (Enterprise only).
            conditions:
              type: object
              nullable: true
              description: Which refs this ruleset applies to
              properties:
                ref_name:
                  type: object
                  properties:
                    include:
                      type: array
                      items:
                        type: string
                      description: Array of ref names or patterns to include. One of these patterns must match for the condition to pass. Also accepts `~DEFAULT_BRANCH` to include the default branch or `~ALL` to include all branches.
                    exclude:
                      type: array
                      items:
                        type: string
                      description: Array of ref names or patterns to exclude. The condition will not pass if any of these patterns match.
                  description: Parameters for a repository ruleset ref name condition.
            rules:
              type: array
              description: |
                Each entry's real shape depends on its "type" - 20 real rule types confirmed
                against github/rest-api-description's authoritative OpenAPI spec (the actual
                source powering docs.github.com and every GitHub SDK), not guessed from field
                names or a third-party summary.
              items:
                description: "One rule in a ruleset; shape depends on its `type` discriminator."
                oneOf:
                  - type: object
                    description: Only allow users with bypass permission to create matching refs.
                    required: [type]
                    properties:
                      type:
                        type: string
                        enum: [creation]
                        description: The rule type - always `creation`.
                  - type: object
                    description: Only allow users with bypass permission to update matching refs.
                    required: [type]
                    properties:
                      type:
                        type: string
                        enum: [update]
                        description: The rule type - always `update`.
                      parameters:
                        type: object
                        description: Parameters for the `update` rule.
                        required: [update_allows_fetch_and_merge]
                        properties:
                          update_allows_fetch_and_merge:
                            type: boolean
                            description: Branch can pull changes from its upstream repository
                  - type: object
                    description: Only allow users with bypass permissions to delete matching refs.
                    required: [type]
                    properties:
                      type:
                        type: string
                        enum: [deletion]
                        description: The rule type - always `deletion`.
                  - type: object
                    description: Prevent merge commits from being pushed to matching refs.
                    required: [type]
                    properties:
                      type:
                        type: string
                        enum: [required_linear_history]
                        description: The rule type - always `required_linear_history`.
                  - type: object
                    description: Merges must be performed via a merge queue.
                    required: [type]
                    properties:
                      type:
                        type: string
                        enum: [merge_queue]
                        description: The rule type - always `merge_queue`.
                      parameters:
                        type: object
                        description: Parameters for the `merge_queue` rule.
                        required:
                          - check_response_timeout_minutes
                          - grouping_strategy
                          - max_entries_to_build
                          - max_entries_to_merge
                          - merge_method
                          - min_entries_to_merge
                          - min_entries_to_merge_wait_minutes
                        properties:
                          check_response_timeout_minutes:
                            type: integer
                            description: Maximum time (minutes) for a required status check to report a conclusion. After this much time has elapsed, checks that have not reported a conclusion will be assumed to have failed.
                          grouping_strategy:
                            type: string
                            enum: [ALLGREEN, HEADGREEN]
                            description: When set to ALLGREEN, the merge commit created by merge queue for each PR in the group must pass all required checks to merge. When set to HEADGREEN, only the commit at the head of the merge group must pass its required checks to merge.
                          max_entries_to_build:
                            type: integer
                            description: Limit the number of queued pull requests requesting checks and workflow runs at the same time.
                          max_entries_to_merge:
                            type: integer
                            description: The maximum number of PRs that will be merged together in a group.
                          merge_method:
                            type: string
                            enum: [MERGE, SQUASH, REBASE]
                            description: Method to use when merging changes from queued pull requests.
                          min_entries_to_merge:
                            type: integer
                            description: The minimum number of PRs that will be merged together in a group.
                          min_entries_to_merge_wait_minutes:
                            type: integer
                            description: The time merge queue should wait after the first PR is added to the queue for the minimum group size to be met, before merging a smaller group.
                  - type: object
                    description: Choose which environments must be successfully deployed to before refs can be pushed.
                    required: [type]
                    properties:
                      type:
                        type: string
                        enum: [required_deployments]
                        description: The rule type - always `required_deployments`.
                      parameters:
                        type: object
                        description: Parameters for the `required_deployments` rule.
                        required: [required_deployment_environments]
                        properties:
                          required_deployment_environments:
                            type: array
                            items:
                              type: string
                            description: Environments that must be successfully deployed to before branches can be merged.
                  - type: object
                    description: Commits pushed to matching refs must have verified signatures.
                    required: [type]
                    properties:
                      type:
                        type: string
                        enum: [required_signatures]
                        description: The rule type - always `required_signatures`.
                  - type: object
                    description: Require commits be submitted via a pull request before they can be merged.
                    required: [type]
                    properties:
                      type:
                        type: string
                        enum: [pull_request]
                        description: The rule type - always `pull_request`.
                      parameters:
                        type: object
                        description: Parameters for the `pull_request` rule.
                        required:
                          - dismiss_stale_reviews_on_push
                          - require_code_owner_review
                          - require_last_push_approval
                          - required_approving_review_count
                          - required_review_thread_resolution
                        properties:
                          allowed_merge_methods:
                            type: array
                            items:
                              type: string
                              enum: [merge, squash, rebase]
                            description: Array of allowed merge methods. At least one option must be enabled.
                          dismiss_stale_reviews_on_push:
                            type: boolean
                            description: New, reviewable commits pushed will dismiss previous pull request review approvals.
                          dismissal_restriction:
                            $ref: '#/components/schemas/RuleDismissalRestriction'
                          require_code_owner_review:
                            type: boolean
                            description: Require an approving review in pull requests that modify files that have a designated code owner.
                          require_last_push_approval:
                            type: boolean
                            description: Whether the most recent reviewable push must be approved by someone other than the person who pushed it.
                          required_approving_review_count:
                            type: integer
                            description: The number of approving reviews that are required before a pull request can be merged.
                          required_review_thread_resolution:
                            type: boolean
                            description: All conversations on code must be resolved before a pull request can be merged.
                          required_reviewers:
                            type: array
                            items:
                              $ref: '#/components/schemas/RuleRequiredReviewerConfiguration'
                            description: A collection of reviewers and associated file patterns. Each reviewer has a list of file patterns which determine the files that reviewer is required to review.
                  - type: object
                    description: Choose which status checks must pass before the ref is updated.
                    required: [type]
                    properties:
                      type:
                        type: string
                        enum: [required_status_checks]
                        description: The rule type - always `required_status_checks`.
                      parameters:
                        type: object
                        description: Parameters for the `required_status_checks` rule.
                        required:
                          - required_status_checks
                          - strict_required_status_checks_policy
                        properties:
                          do_not_enforce_on_create:
                            type: boolean
                            description: Allow repositories and branches to be created if a check would otherwise prohibit it.
                          required_status_checks:
                            type: array
                            items:
                              $ref: '#/components/schemas/RuleStatusCheckConfiguration'
                            description: Status checks that are required.
                          strict_required_status_checks_policy:
                            type: boolean
                            description: Whether pull requests targeting a matching branch must be tested with the latest code. Has no effect unless at least one status check is enabled.
                  - type: object
                    description: Prevent users with push access from force pushing to refs.
                    required: [type]
                    properties:
                      type:
                        type: string
                        enum: [non_fast_forward]
                        description: The rule type - always `non_fast_forward`.
                  - type: object
                    description: Restrict commit messages matching a pattern.
                    required: [type]
                    properties:
                      type:
                        type: string
                        enum: [commit_message_pattern]
                        description: The rule type - always `commit_message_pattern`.
                      parameters:
                        $ref: '#/components/schemas/RulePatternParameters'
                  - type: object
                    description: Restrict commit author emails matching a pattern.
                    required: [type]
                    properties:
                      type:
                        type: string
                        enum: [commit_author_email_pattern]
                        description: The rule type - always `commit_author_email_pattern`.
                      parameters:
                        $ref: '#/components/schemas/RulePatternParameters'
                  - type: object
                    description: Restrict committer emails matching a pattern.
                    required: [type]
                    properties:
                      type:
                        type: string
                        enum: [committer_email_pattern]
                        description: The rule type - always `committer_email_pattern`.
                      parameters:
                        $ref: '#/components/schemas/RulePatternParameters'
                  - type: object
                    description: Restrict branch names matching a pattern.
                    required: [type]
                    properties:
                      type:
                        type: string
                        enum: [branch_name_pattern]
                        description: The rule type - always `branch_name_pattern`.
                      parameters:
                        $ref: '#/components/schemas/RulePatternParameters'
                  - type: object
                    description: Restrict tag names matching a pattern.
                    required: [type]
                    properties:
                      type:
                        type: string
                        enum: [tag_name_pattern]
                        description: The rule type - always `tag_name_pattern`.
                      parameters:
                        $ref: '#/components/schemas/RulePatternParameters'
                  - type: object
                    description: Restrict file and folder paths from being pushed.
                    required: [type]
                    properties:
                      type:
                        type: string
                        enum: [file_path_restriction]
                        description: The rule type - always `file_path_restriction`.
                      parameters:
                        type: object
                        description: Parameters for the `file_path_restriction` rule.
                        required: [restricted_file_paths]
                        properties:
                          restricted_file_paths:
                            type: array
                            items:
                              type: string
                            description: File paths that are restricted from being pushed to the commit graph.
                  - type: object
                    description: Restrict file paths exceeding a character limit.
                    required: [type]
                    properties:
                      type:
                        type: string
                        enum: [max_file_path_length]
                        description: The rule type - always `max_file_path_length`.
                      parameters:
                        type: object
                        description: Parameters for the `max_file_path_length` rule.
                        required: [max_file_path_length]
                        properties:
                          max_file_path_length:
                            type: integer
                            description: The maximum amount of characters allowed in file paths.
                  - type: object
                    description: Restrict files with specified file extensions.
                    required: [type]
                    properties:
                      type:
                        type: string
                        enum: [file_extension_restriction]
                        description: The rule type - always `file_extension_restriction`.
                      parameters:
                        type: object
                        description: Parameters for the `file_extension_restriction` rule.
                        required: [restricted_file_extensions]
                        properties:
                          restricted_file_extensions:
                            type: array
                            items:
                              type: string
                            description: File extensions that are restricted from being pushed to the commit graph.
                  - type: object
                    description: Restrict individual files exceeding a size limit (MB).
                    required: [type]
                    properties:
                      type:
                        type: string
                        enum: [max_file_size]
                        description: The rule type - always `max_file_size`.
                      parameters:
                        type: object
                        description: Parameters for the `max_file_size` rule.
                        required: [max_file_size]
                        properties:
                          max_file_size:
                            type: integer
                            description: The maximum file size allowed in megabytes.
                  - type: object
                    description: Require specified workflows to pass before changes can be merged.
                    required: [type]
                    properties:
                      type:
                        type: string
                        enum: [workflows]
                        description: The rule type - always `workflows`.
                      parameters:
                        type: object
                        description: Parameters for the `workflows` rule.
                        required: [workflows]
                        properties:
                          do_not_enforce_on_create:
                            type: boolean
                            description: Allow repositories and branches to be created if a check would otherwise prohibit it.
                          workflows:
                            type: array
                            items:
                              $ref: '#/components/schemas/RuleWorkflowFileReference'
                            description: Workflows that must pass for this rule to pass.
                  - type: object
                    description: Choose which code scanning tools must provide results before the reference is updated.
                    required: [type]
                    properties:
                      type:
                        type: string
                        enum: [code_scanning]
                        description: The rule type - always `code_scanning`.
                      parameters:
                        type: object
                        description: Parameters for the `code_scanning` rule.
                        required: [code_scanning_tools]
                        properties:
                          code_scanning_tools:
                            type: array
                            items:
                              $ref: '#/components/schemas/RuleCodeScanningTool'
                            description: Tools that must provide code scanning results for this rule to pass.
            bypass_actors:
              type: array
              items:
                description: "An actor permitted to bypass this ruleset's rules."
                type: object
                properties:
                  actor_id:
                    description: "The ID of the actor that can bypass this ruleset."
                    type: integer
                    nullable: true
                  actor_type:
                    description: "The type of actor that can bypass this ruleset, e.g. Team or OrganizationAdmin."
                    type: string
                    enum:
                      - DeployKey
                      - Integration
                      - OrganizationAdmin
                      - RepositoryRole
                      - Team
                  bypass_mode:
                    description: "When the actor can bypass this ruleset, e.g. always or pull_request."
                    type: string
                    enum:
                      - always
                      - pull_request
              description: The actors that can bypass the rules in this ruleset.
            created_at:
              description: "The date and time the resource was created, in ISO 8601 format."
              type: string
              format: date-time
            updated_at:
              description: "The date and time the resource was last updated, in ISO 8601 format."
              type: string
              format: date-time
        changes:
          type: object
          nullable: true
          description: |
            For edited events, the changes made to the ruleset. conditions/rules are each a
            genuine added/deleted/updated diff (entries added or removed wholesale, or an
            existing entry's own fields changed), not a flat "field changed from X" map like
            most other changes payloads - bypass_actors is never part of this diff at all.
          properties:
            name:
              type: object
              description: The ruleset's previous name, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: string
            enforcement:
              type: object
              description: The ruleset's previous enforcement status, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: string
            conditions:
              type: object
              description: The ref-targeting conditions added, deleted, or updated on the ruleset.
              properties:
                added:
                  type: array
                  items:
                    type: object
                    properties:
                      ref_name:
                        type: object
                        properties:
                          include:
                            type: array
                            items:
                              type: string
                            description: Array of ref names or patterns to include. One of these patterns must match for the condition to pass. Also accepts `~DEFAULT_BRANCH` to include the default branch or `~ALL` to include all branches.
                          exclude:
                            type: array
                            items:
                              type: string
                            description: Array of ref names or patterns to exclude. The condition will not pass if any of these patterns match.
                        description: Parameters for a repository ruleset ref name condition.
                  description: Conditions newly added to the ruleset.
                deleted:
                  type: array
                  items:
                    type: object
                    properties:
                      ref_name:
                        type: object
                        properties:
                          include:
                            type: array
                            items:
                              type: string
                            description: Array of ref names or patterns to include. One of these patterns must match for the condition to pass. Also accepts `~DEFAULT_BRANCH` to include the default branch or `~ALL` to include all branches.
                          exclude:
                            type: array
                            items:
                              type: string
                            description: Array of ref names or patterns to exclude. The condition will not pass if any of these patterns match.
                        description: Parameters for a repository ruleset ref name condition.
                  description: Conditions removed from the ruleset.
                updated:
                  type: array
                  description: Conditions whose own fields changed on the ruleset.
                  items:
                    description: "A ruleset condition whose fields changed in this edit, with its current state and the diff."
                    type: object
                    properties:
                      condition:
                        type: object
                        description: The current state of the updated condition.
                        properties:
                          ref_name:
                            type: object
                            properties:
                              include:
                                type: array
                                items:
                                  type: string
                                description: Array of ref names or patterns to include. One of these patterns must match for the condition to pass. Also accepts `~DEFAULT_BRANCH` to include the default branch or `~ALL` to include all branches.
                              exclude:
                                type: array
                                items:
                                  type: string
                                description: Array of ref names or patterns to exclude. The condition will not pass if any of these patterns match.
                            description: Parameters for a repository ruleset ref name condition.
                      changes:
                        type: object
                        description: The fields of the condition that changed.
                        properties:
                          condition_type:
                            type: object
                            description: The condition's previous type, before this edit.
                            properties:
                              from:
                                description: The previous value, before this change.
                                type: string
                          target:
                            type: object
                            description: The condition's previous target, before this edit.
                            properties:
                              from:
                                description: The previous value, before this change.
                                type: string
                          include:
                            type: object
                            description: The condition's previous list of included ref patterns, before this edit.
                            properties:
                              from:
                                description: The previous value, before this change.
                                type: array
                                items:
                                  type: string
                          exclude:
                            type: object
                            description: The condition's previous list of excluded ref patterns, before this edit.
                            properties:
                              from:
                                description: The previous value, before this change.
                                type: array
                                items:
                                  type: string
            rules:
              type: object
              description: The rules added, deleted, or updated on the ruleset.
              properties:
                added:
                  type: array
                  description: Rules newly added to the ruleset.
                  items:
                    description: "A rule added to the ruleset by this edit."
                    type: object
                    properties:
                      type:
                        type: string
                        description: The type of rule that was added.
                      parameters:
                        type: object
                        description: The parameters of the rule that was added.
                        oneOf:
                          - type: object
                            description: Parameters for the `update` rule.
                            properties:
                              update_allows_fetch_and_merge:
                                type: boolean
                                description: Branch can pull changes from its upstream repository
                          - type: object
                            description: Parameters for the `merge_queue` rule.
                            properties:
                              check_response_timeout_minutes:
                                type: integer
                                description: Maximum time (minutes) for a required status check to report a conclusion. After this much time has elapsed, checks that have not reported a conclusion will be assumed to have failed.
                              grouping_strategy:
                                type: string
                                description: When set to ALLGREEN, the merge commit created by merge queue for each PR in the group must pass all required checks to merge. When set to HEADGREEN, only the commit at the head of the merge group must pass its required checks to merge.
                              max_entries_to_build:
                                type: integer
                                description: Limit the number of queued pull requests requesting checks and workflow runs at the same time.
                              max_entries_to_merge:
                                type: integer
                                description: The maximum number of PRs that will be merged together in a group.
                              merge_method:
                                type: string
                                description: Method to use when merging changes from queued pull requests.
                              min_entries_to_merge:
                                type: integer
                                description: The minimum number of PRs that will be merged together in a group.
                              min_entries_to_merge_wait_minutes:
                                type: integer
                                description: The time merge queue should wait after the first PR is added to the queue for the minimum group size to be met, before merging a smaller group.
                          - type: object
                            description: Parameters for the `required_deployments` rule.
                            properties:
                              required_deployment_environments:
                                type: array
                                items:
                                  type: string
                                description: Environments that must be successfully deployed to before branches can be merged.
                          - type: object
                            description: Parameters for the `pull_request` rule.
                            properties:
                              allowed_merge_methods:
                                type: array
                                items:
                                  type: string
                                description: Array of allowed merge methods. At least one option must be enabled.
                              dismiss_stale_reviews_on_push:
                                type: boolean
                                description: New, reviewable commits pushed will dismiss previous pull request review approvals.
                              dismissal_restriction:
                                $ref: '#/components/schemas/RuleDismissalRestriction'
                              require_code_owner_review:
                                type: boolean
                                description: Require an approving review in pull requests that modify files that have a designated code owner.
                              require_last_push_approval:
                                type: boolean
                                description: Whether the most recent reviewable push must be approved by someone other than the person who pushed it.
                              required_approving_review_count:
                                type: integer
                                description: The number of approving reviews that are required before a pull request can be merged.
                              required_review_thread_resolution:
                                type: boolean
                                description: All conversations on code must be resolved before a pull request can be merged.
                              required_reviewers:
                                type: array
                                items:
                                  $ref: '#/components/schemas/RuleRequiredReviewerConfiguration'
                                description: A collection of reviewers and associated file patterns. Each reviewer has a list of file patterns which determine the files that reviewer is required to review.
                          - type: object
                            description: Parameters for the `required_status_checks` rule.
                            properties:
                              do_not_enforce_on_create:
                                type: boolean
                                description: Allow repositories and branches to be created if a check would otherwise prohibit it.
                              required_status_checks:
                                type: array
                                items:
                                  $ref: '#/components/schemas/RuleStatusCheckConfiguration'
                                description: Status checks that are required.
                              strict_required_status_checks_policy:
                                type: boolean
                                description: Whether pull requests targeting a matching branch must be tested with the latest code. Has no effect unless at least one status check is enabled.
                          - type: object
                            description: |
                              Shared by the 5 pattern-matching ruleset rules (commit_message_pattern,
                              commit_author_email_pattern, committer_email_pattern, branch_name_pattern,
                              tag_name_pattern) - identical parameters shape for all five per GitHub's own schema.
                            properties:
                              name:
                                type: string
                                description: How this rule appears when configuring it.
                              negate:
                                type: boolean
                                description: If true, the rule will fail if the pattern matches.
                              operator:
                                type: string
                                description: The operator to use for matching.
                              pattern:
                                type: string
                                description: The pattern to match with.
                          - type: object
                            description: Parameters for the `file_path_restriction` rule.
                            properties:
                              restricted_file_paths:
                                type: array
                                items:
                                  type: string
                                description: File paths that are restricted from being pushed to the commit graph.
                          - type: object
                            description: Parameters for the `max_file_path_length` rule.
                            properties:
                              max_file_path_length:
                                type: integer
                                description: The maximum amount of characters allowed in file paths.
                          - type: object
                            description: Parameters for the `file_extension_restriction` rule.
                            properties:
                              restricted_file_extensions:
                                type: array
                                items:
                                  type: string
                                description: File extensions that are restricted from being pushed to the commit graph.
                          - type: object
                            description: Parameters for the `max_file_size` rule.
                            properties:
                              max_file_size:
                                type: integer
                                description: The maximum file size allowed in megabytes.
                          - type: object
                            description: Parameters for the `workflows` rule.
                            properties:
                              do_not_enforce_on_create:
                                type: boolean
                                description: Allow repositories and branches to be created if a check would otherwise prohibit it.
                              workflows:
                                type: array
                                items:
                                  $ref: '#/components/schemas/RuleWorkflowFileReference'
                                description: Workflows that must pass for this rule to pass.
                          - type: object
                            description: Parameters for the `code_scanning` rule.
                            properties:
                              code_scanning_tools:
                                type: array
                                items:
                                  $ref: '#/components/schemas/RuleCodeScanningTool'
                                description: Tools that must provide code scanning results for this rule to pass.
                deleted:
                  type: array
                  description: Rules removed from the ruleset.
                  items:
                    description: "A rule removed from the ruleset by this edit."
                    type: object
                    properties:
                      type:
                        type: string
                        description: The type of rule that was removed.
                      parameters:
                        type: object
                        description: The parameters of the rule that was removed.
                        oneOf:
                          - type: object
                            description: Parameters for the `update` rule.
                            properties:
                              update_allows_fetch_and_merge:
                                type: boolean
                                description: Branch can pull changes from its upstream repository
                          - type: object
                            description: Parameters for the `merge_queue` rule.
                            properties:
                              check_response_timeout_minutes:
                                type: integer
                                description: Maximum time (minutes) for a required status check to report a conclusion. After this much time has elapsed, checks that have not reported a conclusion will be assumed to have failed.
                              grouping_strategy:
                                type: string
                                description: When set to ALLGREEN, the merge commit created by merge queue for each PR in the group must pass all required checks to merge. When set to HEADGREEN, only the commit at the head of the merge group must pass its required checks to merge.
                              max_entries_to_build:
                                type: integer
                                description: Limit the number of queued pull requests requesting checks and workflow runs at the same time.
                              max_entries_to_merge:
                                type: integer
                                description: The maximum number of PRs that will be merged together in a group.
                              merge_method:
                                type: string
                                description: Method to use when merging changes from queued pull requests.
                              min_entries_to_merge:
                                type: integer
                                description: The minimum number of PRs that will be merged together in a group.
                              min_entries_to_merge_wait_minutes:
                                type: integer
                                description: The time merge queue should wait after the first PR is added to the queue for the minimum group size to be met, before merging a smaller group.
                          - type: object
                            description: Parameters for the `required_deployments` rule.
                            properties:
                              required_deployment_environments:
                                type: array
                                items:
                                  type: string
                                description: Environments that must be successfully deployed to before branches can be merged.
                          - type: object
                            description: Parameters for the `pull_request` rule.
                            properties:
                              allowed_merge_methods:
                                type: array
                                items:
                                  type: string
                                description: Array of allowed merge methods. At least one option must be enabled.
                              dismiss_stale_reviews_on_push:
                                type: boolean
                                description: New, reviewable commits pushed will dismiss previous pull request review approvals.
                              dismissal_restriction:
                                $ref: '#/components/schemas/RuleDismissalRestriction'
                              require_code_owner_review:
                                type: boolean
                                description: Require an approving review in pull requests that modify files that have a designated code owner.
                              require_last_push_approval:
                                type: boolean
                                description: Whether the most recent reviewable push must be approved by someone other than the person who pushed it.
                              required_approving_review_count:
                                type: integer
                                description: The number of approving reviews that are required before a pull request can be merged.
                              required_review_thread_resolution:
                                type: boolean
                                description: All conversations on code must be resolved before a pull request can be merged.
                              required_reviewers:
                                type: array
                                items:
                                  $ref: '#/components/schemas/RuleRequiredReviewerConfiguration'
                                description: A collection of reviewers and associated file patterns. Each reviewer has a list of file patterns which determine the files that reviewer is required to review.
                          - type: object
                            description: Parameters for the `required_status_checks` rule.
                            properties:
                              do_not_enforce_on_create:
                                type: boolean
                                description: Allow repositories and branches to be created if a check would otherwise prohibit it.
                              required_status_checks:
                                type: array
                                items:
                                  $ref: '#/components/schemas/RuleStatusCheckConfiguration'
                                description: Status checks that are required.
                              strict_required_status_checks_policy:
                                type: boolean
                                description: Whether pull requests targeting a matching branch must be tested with the latest code. Has no effect unless at least one status check is enabled.
                          - type: object
                            description: |
                              Shared by the 5 pattern-matching ruleset rules (commit_message_pattern,
                              commit_author_email_pattern, committer_email_pattern, branch_name_pattern,
                              tag_name_pattern) - identical parameters shape for all five per GitHub's own schema.
                            properties:
                              name:
                                type: string
                                description: How this rule appears when configuring it.
                              negate:
                                type: boolean
                                description: If true, the rule will fail if the pattern matches.
                              operator:
                                type: string
                                description: The operator to use for matching.
                              pattern:
                                type: string
                                description: The pattern to match with.
                          - type: object
                            description: Parameters for the `file_path_restriction` rule.
                            properties:
                              restricted_file_paths:
                                type: array
                                items:
                                  type: string
                                description: File paths that are restricted from being pushed to the commit graph.
                          - type: object
                            description: Parameters for the `max_file_path_length` rule.
                            properties:
                              max_file_path_length:
                                type: integer
                                description: The maximum amount of characters allowed in file paths.
                          - type: object
                            description: Parameters for the `file_extension_restriction` rule.
                            properties:
                              restricted_file_extensions:
                                type: array
                                items:
                                  type: string
                                description: File extensions that are restricted from being pushed to the commit graph.
                          - type: object
                            description: Parameters for the `max_file_size` rule.
                            properties:
                              max_file_size:
                                type: integer
                                description: The maximum file size allowed in megabytes.
                          - type: object
                            description: Parameters for the `workflows` rule.
                            properties:
                              do_not_enforce_on_create:
                                type: boolean
                                description: Allow repositories and branches to be created if a check would otherwise prohibit it.
                              workflows:
                                type: array
                                items:
                                  $ref: '#/components/schemas/RuleWorkflowFileReference'
                                description: Workflows that must pass for this rule to pass.
                          - type: object
                            description: Parameters for the `code_scanning` rule.
                            properties:
                              code_scanning_tools:
                                type: array
                                items:
                                  $ref: '#/components/schemas/RuleCodeScanningTool'
                                description: Tools that must provide code scanning results for this rule to pass.
                updated:
                  type: array
                  description: Rules whose own fields changed on the ruleset.
                  items:
                    description: "A ruleset rule whose fields changed in this edit, with its current state and the diff."
                    type: object
                    properties:
                      rule:
                        type: object
                        description: The current state of the updated rule.
                        properties:
                          type:
                            type: string
                            description: The type of the updated rule.
                          parameters:
                            type: object
                            description: The parameters of the updated rule.
                            oneOf:
                              - type: object
                                description: Parameters for the `update` rule.
                                properties:
                                  update_allows_fetch_and_merge:
                                    type: boolean
                                    description: Branch can pull changes from its upstream repository
                              - type: object
                                description: Parameters for the `merge_queue` rule.
                                properties:
                                  check_response_timeout_minutes:
                                    type: integer
                                    description: Maximum time (minutes) for a required status check to report a conclusion. After this much time has elapsed, checks that have not reported a conclusion will be assumed to have failed.
                                  grouping_strategy:
                                    type: string
                                    description: When set to ALLGREEN, the merge commit created by merge queue for each PR in the group must pass all required checks to merge. When set to HEADGREEN, only the commit at the head of the merge group must pass its required checks to merge.
                                  max_entries_to_build:
                                    type: integer
                                    description: Limit the number of queued pull requests requesting checks and workflow runs at the same time.
                                  max_entries_to_merge:
                                    type: integer
                                    description: The maximum number of PRs that will be merged together in a group.
                                  merge_method:
                                    type: string
                                    description: Method to use when merging changes from queued pull requests.
                                  min_entries_to_merge:
                                    type: integer
                                    description: The minimum number of PRs that will be merged together in a group.
                                  min_entries_to_merge_wait_minutes:
                                    type: integer
                                    description: The time merge queue should wait after the first PR is added to the queue for the minimum group size to be met, before merging a smaller group.
                              - type: object
                                description: Parameters for the `required_deployments` rule.
                                properties:
                                  required_deployment_environments:
                                    type: array
                                    items:
                                      type: string
                                    description: Environments that must be successfully deployed to before branches can be merged.
                              - type: object
                                description: Parameters for the `pull_request` rule.
                                properties:
                                  allowed_merge_methods:
                                    type: array
                                    items:
                                      type: string
                                    description: Array of allowed merge methods. At least one option must be enabled.
                                  dismiss_stale_reviews_on_push:
                                    type: boolean
                                    description: New, reviewable commits pushed will dismiss previous pull request review approvals.
                                  dismissal_restriction:
                                    $ref: '#/components/schemas/RuleDismissalRestriction'
                                  require_code_owner_review:
                                    type: boolean
                                    description: Require an approving review in pull requests that modify files that have a designated code owner.
                                  require_last_push_approval:
                                    type: boolean
                                    description: Whether the most recent reviewable push must be approved by someone other than the person who pushed it.
                                  required_approving_review_count:
                                    type: integer
                                    description: The number of approving reviews that are required before a pull request can be merged.
                                  required_review_thread_resolution:
                                    type: boolean
                                    description: All conversations on code must be resolved before a pull request can be merged.
                                  required_reviewers:
                                    type: array
                                    items:
                                      $ref: '#/components/schemas/RuleRequiredReviewerConfiguration'
                                    description: A collection of reviewers and associated file patterns. Each reviewer has a list of file patterns which determine the files that reviewer is required to review.
                              - type: object
                                description: Parameters for the `required_status_checks` rule.
                                properties:
                                  do_not_enforce_on_create:
                                    type: boolean
                                    description: Allow repositories and branches to be created if a check would otherwise prohibit it.
                                  required_status_checks:
                                    type: array
                                    items:
                                      $ref: '#/components/schemas/RuleStatusCheckConfiguration'
                                    description: Status checks that are required.
                                  strict_required_status_checks_policy:
                                    type: boolean
                                    description: Whether pull requests targeting a matching branch must be tested with the latest code. Has no effect unless at least one status check is enabled.
                              - type: object
                                description: |
                                  Shared by the 5 pattern-matching ruleset rules (commit_message_pattern,
                                  commit_author_email_pattern, committer_email_pattern, branch_name_pattern,
                                  tag_name_pattern) - identical parameters shape for all five per GitHub's own schema.
                                properties:
                                  name:
                                    type: string
                                    description: How this rule appears when configuring it.
                                  negate:
                                    type: boolean
                                    description: If true, the rule will fail if the pattern matches.
                                  operator:
                                    type: string
                                    description: The operator to use for matching.
                                  pattern:
                                    type: string
                                    description: The pattern to match with.
                              - type: object
                                description: Parameters for the `file_path_restriction` rule.
                                properties:
                                  restricted_file_paths:
                                    type: array
                                    items:
                                      type: string
                                    description: File paths that are restricted from being pushed to the commit graph.
                              - type: object
                                description: Parameters for the `max_file_path_length` rule.
                                properties:
                                  max_file_path_length:
                                    type: integer
                                    description: The maximum amount of characters allowed in file paths.
                              - type: object
                                description: Parameters for the `file_extension_restriction` rule.
                                properties:
                                  restricted_file_extensions:
                                    type: array
                                    items:
                                      type: string
                                    description: File extensions that are restricted from being pushed to the commit graph.
                              - type: object
                                description: Parameters for the `max_file_size` rule.
                                properties:
                                  max_file_size:
                                    type: integer
                                    description: The maximum file size allowed in megabytes.
                              - type: object
                                description: Parameters for the `workflows` rule.
                                properties:
                                  do_not_enforce_on_create:
                                    type: boolean
                                    description: Allow repositories and branches to be created if a check would otherwise prohibit it.
                                  workflows:
                                    type: array
                                    items:
                                      $ref: '#/components/schemas/RuleWorkflowFileReference'
                                    description: Workflows that must pass for this rule to pass.
                              - type: object
                                description: Parameters for the `code_scanning` rule.
                                properties:
                                  code_scanning_tools:
                                    type: array
                                    items:
                                      $ref: '#/components/schemas/RuleCodeScanningTool'
                                    description: Tools that must provide code scanning results for this rule to pass.
                      changes:
                        type: object
                        description: The fields of the rule that changed.
                        properties:
                          configuration:
                            type: object
                            description: The rule's previous configuration, before this edit.
                            properties:
                              from:
                                description: The previous value, before this change.
                                type: string
                          rule_type:
                            type: object
                            description: The rule's previous type, before this edit.
                            properties:
                              from:
                                description: The previous value, before this change.
                                type: string
                          pattern:
                            type: object
                            description: The rule's previous pattern, before this edit.
                            properties:
                              from:
                                description: The previous value, before this change.
                                type: string
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    RepositoryVulnerabilityAlertPayload:
      type: object
      description: Payload for repository_vulnerability_alert events (closing down — use dependabot_alert)
      required:
        - action
        - alert
      properties:
        action:
          description: "The action that was performed. Can be one of: create, dismiss, reopen, resolve."
          type: string
          enum:
            - create
            - dismiss
            - reopen
            - resolve
        alert:
          type: object
          description: The security alert of the vulnerable dependency
          required:
            - id
            - affected_package_name
            - affected_range
            - severity
          properties:
            id:
              description: "The unique identifier of the resource."
              type: integer
            affected_package_name:
              description: "The name of the package affected by the vulnerability."
              type: string
            affected_range:
              description: "The range of package versions affected by the vulnerability."
              type: string
            fixed_in:
              description: "The package version that resolves the vulnerability."
              type: string
              nullable: true
            severity:
              description: "The severity of the vulnerability."
              type: string
              enum:
                - low
                - moderate
                - high
                - critical
            ghsa_id:
              description: "The GitHub Security Advisory identifier for the vulnerability."
              type: string
            external_identifier:
              description: "The vulnerability's identifier in an external database, e.g. a CVE ID."
              type: string
            external_reference:
              description: "A URL to the vulnerability's external reference source."
              type: string
              format: uri
              nullable: true
            created_at:
              description: "The date and time the resource was created, in ISO 8601 format."
              type: string
              format: date-time
            auto_dismissed_at:
              description: "The date and time the alert was automatically dismissed, in ISO 8601 format."
              type: string
              format: date-time
              nullable: true
            dismiss_reason:
              description: "The reason the alert was dismissed."
              type: string
              nullable: true
            dismissed_at:
              description: "The date and time the alert was dismissed, in ISO 8601 format."
              type: string
              format: date-time
              nullable: true
            dismissed_by:
              $ref: '#/components/schemas/User'
              nullable: true
            number:
              description: "The alert number, unique within the repository."
              type: integer
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'

    SecretScanningAlertLocationPayload:
      type: object
      description: Payload for secret_scanning_alert_location events
      required:
        - alert
        - location
      properties:
        alert:
          type: object
          description: The existing secret scanning alert the location was added to
          required:
            - number
            - secret_type
          properties:
            number:
              type: integer
              description: The security alert number.
            secret_type:
              type: string
              description: The type of secret that secret scanning detected.
        location:
          type: object
          description: The location where the secret was found
          required:
            - type
          properties:
            type:
              type: string
              enum:
                - commit
                - wiki_commit
                - issue_title
                - issue_body
                - issue_comment
                - discussion_title
                - discussion_body
                - discussion_comment
                - pull_request_title
                - pull_request_body
                - pull_request_comment
                - pull_request_review
                - pull_request_review_comment
              description: The location type. Because secrets may be found in different types of resources (ie. code, comments, issues, pull requests, discussions), this field identifies the type of resource where the secret was found.
            details:
              type: object
              description: Location details; shape varies by type
              properties:
                path:
                  type: string
                  description: The file path in the repository.
                start_line:
                  type: integer
                  description: Line number at which the secret starts in the file.
                end_line:
                  type: integer
                  description: Line number at which the secret ends in the file.
                start_column:
                  type: integer
                  description: The column at which the secret starts within the start line when the file is interpreted as 8BIT ASCII.
                end_column:
                  type: integer
                  description: The column at which the secret ends within the end line when the file is interpreted as 8BIT ASCII.
                blob_sha:
                  type: string
                  description: SHA-1 hash ID of the associated blob.
                blob_url:
                  type: string
                  format: uri
                  description: The API URL to get the associated blob resource.
                commit_sha:
                  type: string
                  description: SHA-1 hash ID of the associated commit.
                commit_url:
                  type: string
                  format: uri
                  description: The API URL to get the associated commit resource.
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    SecretScanningScanPayload:
      type: object
      description: Payload for secret_scanning_scan events. No action field.
      required:
        - type
        - source
        - started_at
        - completed_at
      properties:
        type:
          type: string
          description: What type of scan was completed
          enum:
            - backfill
            - custom-pattern
            - pattern-update
        source:
          type: string
          description: What type of content was scanned
          enum:
            - git
            - issues
            - pull_requests
            - wiki
        started_at:
          type: string
          format: date-time
          description: ISO 8601 timestamp when the scan started
        completed_at:
          type: string
          format: date-time
          description: ISO 8601 timestamp when the scan completed
        secret_types:
          type: array
          nullable: true
          description: Patterns updated. Empty for normal backfill or custom pattern scans.
          items:
            type: string
        custom_pattern_name:
          type: string
          nullable: true
          description: If triggered by a custom pattern update, the name of that pattern
        custom_pattern_scope:
          type: string
          nullable: true
          description: If triggered by a custom pattern update, the scope of that pattern
          enum:
            - global
            - organization
            - repository
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    # -----------------------------------------------------------------------
    # Batch 6 — Final 4 event payload schemas
    # -----------------------------------------------------------------------

    PageBuildPayload:
      type: object
      description: Payload for page_build events. No action field.
      required:
        - id
        - build
      properties:
        id:
          type: integer
          description: The unique identifier of the page build
        build:
          type: object
          description: The GitHub Pages build object
          required:
            - url
            - status
            - error
            - pusher
            - commit
            - duration
            - created_at
            - updated_at
          properties:
            url:
              description: "The API URL of the resource."
              type: string
              format: uri
            status:
              type: string
              description: Current build status
              enum:
                - queued
                - building
                - built
                - errored
            error:
              type: object
              description: Error information if the build failed
              required:
                - message
              properties:
                message:
                  description: "The error message describing why the Pages build failed."
                  type: string
                  nullable: true
            pusher:
              $ref: '#/components/schemas/User'
            commit:
              type: string
              description: The SHA of the commit that triggered the build
            duration:
              type: integer
              description: Duration of the build in milliseconds
            created_at:
              description: "The date and time the resource was created, in ISO 8601 format."
              type: string
              format: date-time
            updated_at:
              description: "The date and time the resource was last updated, in ISO 8601 format."
              type: string
              format: date-time
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    TeamAddPayload:
      type: object
      description: |
        Payload for team_add events. Fired when a repository is added to a team.
        No action field.
      required:
        - team
      properties:
        team:
          type: object
          description: The team that was granted access to the repository
          required:
            - id
            - node_id
            - name
            - slug
          properties:
            id:
              type: integer
              description: Unique identifier of the team.
            node_id:
              description: "The GraphQL node ID."
              type: string
            url:
              type: string
              format: uri
              description: URL for the team.
            html_url:
              description: "The URL of the resource on GitHub.com."
              type: string
              format: uri
            name:
              type: string
              description: Name of the team.
            slug:
              description: "The URL-friendly identifier of the resource."
              type: string
            description:
              type: string
              nullable: true
              description: Description of the team.
            privacy:
              type: string
              enum:
                - closed
                - secret
              description: The level of privacy this team should have.
            notification_setting:
              type: string
              enum:
                - notifications_enabled
                - notifications_disabled
              description: The notification setting the team has set.
            permission:
              type: string
              enum:
                - pull
                - push
                - admin
                - maintain
                - triage
              description: Permission that the team will have for its repositories.
            members_url:
              description: "The API URL listing the members of this team."
              type: string
            repositories_url:
              description: "The API URL listing the repositories associated with this resource."
              type: string
              format: uri
            parent:
              type: object
              nullable: true
              description: The parent team, if this is a child team
              properties:
                id:
                  type: integer
                  description: Unique identifier of the team.
                node_id:
                  description: "The GraphQL node ID."
                  type: string
                name:
                  type: string
                  description: Name of the team.
                slug:
                  description: "The URL-friendly identifier of the resource."
                  type: string
                description:
                  type: string
                  nullable: true
                  description: Description of the team.
                privacy:
                  type: string
                  description: The level of privacy this team should have.
                permission:
                  type: string
                  description: Permission that the team will have for its repositories.
                members_url:
                  description: "The API URL listing the members of this team."
                  type: string
                repositories_url:
                  description: "The API URL listing the repositories associated with this resource."
                  type: string
                  format: uri
                html_url:
                  description: "The URL of the resource on GitHub.com."
                  type: string
                  format: uri
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    SponsorshipPayload:
      type: object
      description: Payload for sponsorship events
      required:
        - action
        - sponsorship
      properties:
        action:
          description: "The action that was performed. Can be one of: created, cancelled, edited, tier_changed, pending_cancellation, pending_tier_change."
          type: string
          enum:
            - cancelled
            - created
            - edited
            - pending_cancellation
            - pending_tier_change
            - tier_changed
        sponsorship:
          type: object
          description: The sponsorship object
          required:
            - node_id
            - created_at
            - privacy_level
            - tier
            - sponsor
            - sponsorable
          properties:
            node_id:
              description: "The GraphQL node ID."
              type: string
            created_at:
              description: "The date and time the resource was created, in ISO 8601 format."
              type: string
              format: date-time
            privacy_level:
              description: "Whether the sponsorship is publicly visible (public or private)."
              type: string
              enum:
                - public
                - private
            tier:
              $ref: '#/components/schemas/Tier'
            sponsor:
              $ref: '#/components/schemas/User'
            sponsorable:
              $ref: '#/components/schemas/User'
        changes:
          type: object
          nullable: true
          description: For edited, tier_changed, and pending_tier_change events
          properties:
            tier:
              type: object
              description: The sponsorship's previous tier, before this change.
              properties:
                from:
                  $ref: '#/components/schemas/Tier'
            privacy_level:
              type: object
              description: The sponsorship's previous privacy level, before this change.
              properties:
                from:
                  description: The previous value, before this change.
                  type: string
        effective_date:
          type: string
          format: date
          nullable: true
          description: |
            For pending_cancellation and pending_tier_change, the date the
            change takes effect (ISO 8601 date).
        sender:
          $ref: '#/components/schemas/User'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'

    SecurityAndAnalysisPayload:
      type: object
      description: |
        Payload for security_and_analysis events. Fired when code security and
        analysis features are enabled or disabled for a repository. No action field.
      required:
        - changes
      properties:
        changes:
          type: object
          description: The security and analysis settings that changed
          properties:
            advanced_security:
              type: object
              description: Change to GitHub Advanced Security enablement
              properties:
                from:
                  description: The previous value, before this change.
                  type: string
                  enum:
                    - enabled
                    - disabled
                to:
                  description: "The new value, after this change."
                  type: string
                  enum:
                    - enabled
                    - disabled
            dependabot_alerts:
              type: object
              description: Change to Dependabot alerts enablement
              properties:
                from:
                  description: The previous value, before this change.
                  type: string
                  enum:
                    - enabled
                    - disabled
                to:
                  description: "The new value, after this change."
                  type: string
                  enum:
                    - enabled
                    - disabled
            dependabot_security_updates:
              type: object
              description: Change to Dependabot security updates enablement
              properties:
                from:
                  description: The previous value, before this change.
                  type: string
                  enum:
                    - enabled
                    - disabled
                to:
                  description: "The new value, after this change."
                  type: string
                  enum:
                    - enabled
                    - disabled
            secret_scanning:
              type: object
              description: Change to secret scanning enablement
              properties:
                from:
                  description: The previous value, before this change.
                  type: string
                  enum:
                    - enabled
                    - disabled
                to:
                  description: "The new value, after this change."
                  type: string
                  enum:
                    - enabled
                    - disabled
            secret_scanning_push_protection:
              type: object
              description: Change to secret scanning push protection enablement
              properties:
                from:
                  description: The previous value, before this change.
                  type: string
                  enum:
                    - enabled
                    - disabled
                to:
                  description: "The new value, after this change."
                  type: string
                  enum:
                    - enabled
                    - disabled
            secret_scanning_non_provider_patterns:
              type: object
              description: Change to non-provider pattern scanning enablement
              properties:
                from:
                  description: The previous value, before this change.
                  type: string
                  enum:
                    - enabled
                    - disabled
                to:
                  description: "The new value, after this change."
                  type: string
                  enum:
                    - enabled
                    - disabled
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    # -----------------------------------------------------------------------
    # Batch 7 — Final 10 event payload schemas
    # -----------------------------------------------------------------------

    DeploymentReviewPayload:
      type: object
      description: Payload for deployment_review events
      required:
        - action
        - environment
        - workflow_run
        - since
      properties:
        action:
          description: "The action that was performed. Can be one of: requested, approved, rejected."
          type: string
          enum:
            - approved
            - rejected
            - requested
        environment:
          type: string
          description: The name of the environment that was approved or rejected
        comment:
          type: string
          nullable: true
          description: The reviewer's comment (for approved/rejected)
        since:
          type: string
          format: date-time
          nullable: true
          description: ISO 8601 date of when the review was requested
        reviewers:
          type: array
          description: The reviewers who were requested or who reviewed
          items:
            description: "A user or team requested as, or acting as, a deployment reviewer."
            type: object
            properties:
              type:
                description: "The type of the reviewer, either User or Team."
                type: string
                enum:
                  - Team
                  - User
              reviewer:
                description: A User or Team object depending on type
                oneOf:
                  - $ref: '#/components/schemas/User'
                  - type: object
                    description: Team reviewer
                    properties:
                      id:
                        type: integer
                        description: Unique identifier of the team.
                      node_id:
                        type: string
                        description: The node ID of the team.
                      name:
                        type: string
                        description: Name of the team.
                      slug:
                        type: string
                        description: The URL-friendly slug of the team's name.
                      description:
                        type: string
                        nullable: true
                        description: Description of the team.
                      privacy:
                        type: string
                        description: The level of privacy this team should have.
                      permission:
                        type: string
                        description: Permission that the team will have for its repositories.
                      html_url:
                        type: string
                        format: uri
                        description: URL for the team.
        workflow_run:
          type: object
          description: The workflow run associated with the deployment
          required:
            - id
            - name
          properties:
            id:
              type: integer
              description: The ID of the workflow run.
            name:
              type: string
              nullable: true
              description: The name of the workflow run.
            head_sha:
              type: string
              description: The SHA of the head commit that points to the version of the workflow being run.
            head_branch:
              type: string
              nullable: true
              description: The branch that triggered the workflow run.
            run_number:
              type: integer
              description: The auto incrementing run number for the workflow run.
            status:
              type: string
              description: The status of the workflow run.
            conclusion:
              type: string
              nullable: true
              description: The result of the completed workflow run.
            html_url:
              type: string
              format: uri
              description: The URL to view the workflow run on GitHub.
            pull_requests:
              type: array
              items:
                $ref: '#/components/schemas/PullRequestMinimal'
              description: Pull requests that are open with a `head_sha` or `head_branch` that matches the workflow run. The returned pull requests do not necessarily indicate pull requests that triggered the run.
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    InstallationTargetPayload:
      type: object
      description: Payload for installation_target events (GitHub App installation account renamed)
      required:
        - action
        - account
        - target_type
        - changes
      properties:
        action:
          description: "The action that was performed. Always: renamed."
          type: string
          enum:
            - renamed
        account:
          type: object
          description: The account (user or organization) where the app is installed
          required:
            - id
            - login
            - type
          properties:
            id:
              description: "The unique identifier of the resource."
              type: integer
            node_id:
              description: "The GraphQL node ID."
              type: string
            login:
              description: "The username or organization login of the account."
              type: string
            type:
              description: "The account type. Can be one of: Bot, User, Organization."
              type: string
              enum:
                - User
                - Organization
            avatar_url:
              description: "The URL of the user's or organization's avatar image."
              type: string
              format: uri
            html_url:
              description: "The URL of the resource on GitHub.com."
              type: string
              format: uri
            site_admin:
              description: "Whether the user is a GitHub site administrator."
              type: boolean
        target_type:
          description: "The type of account the app is installed on: user or organization."
          type: string
          enum:
            - User
            - Organization
        changes:
          type: object
          description: The changes made to the account
          properties:
            login:
              type: object
              description: The account's previous login, before this rename.
              properties:
                from:
                  description: The previous value, before this change.
                  type: string
            slug:
              type: object
              description: The account's previous slug, before this rename.
              properties:
                from:
                  description: The previous value, before this change.
                  type: string
        installation:
          $ref: '#/components/schemas/Installation'

    IssueDependenciesPayload:
      type: object
      description: Payload for issue_dependencies events
      required:
        - action
      properties:
        action:
          description: "The action that was performed. Can be one of: blocking_removed, blocked_by_removed, blocking_added, blocked_by_added."
          type: string
          enum:
            - blocked_by_added
            - blocked_by_removed
            - blocking_added
            - blocking_removed
        blocked_issue_id:
          type: integer
          description: The ID of the blocked issue.
        blocked_issue:
          $ref: '#/components/schemas/Issue'
        blocking_issue_id:
          type: integer
          description: The ID of the blocking issue.
        blocking_issue:
          $ref: '#/components/schemas/Issue'
        blocking_issue_repo:
          $ref: '#/components/schemas/Repository'
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    MarketplacePurchasePayload:
      type: object
      description: Payload for marketplace_purchase events
      required:
        - action
        - marketplace_purchase
        - sender
        - effective_date
      properties:
        action:
          description: "The action that was performed. Can be one of: purchased, cancelled, pending_change, pending_change_cancelled, changed."
          type: string
          enum:
            - cancelled
            - changed
            - pending_change
            - pending_change_cancelled
            - purchased
        marketplace_purchase:
          $ref: '#/components/schemas/MarketplacePurchase'
        previous_marketplace_purchase:
          $ref: '#/components/schemas/MarketplacePurchase'
          nullable: true
          description: The previous purchase state (for changed/pending_change events)
        effective_date:
          type: string
          format: date-time
          description: ISO 8601 date when the change takes effect
        sender:
          $ref: '#/components/schemas/User'
        installation:
          $ref: '#/components/schemas/Installation'

    PersonalAccessTokenRequestPayload:
      type: object
      description: Payload for personal_access_token_request events
      required:
        - action
        - personal_access_token_request
      properties:
        action:
          description: "The action that was performed. Can be one of: created, approved, denied, cancelled."
          type: string
          enum:
            - approved
            - cancelled
            - created
            - denied
        personal_access_token_request:
          type: object
          description: A fine-grained personal access token request
          required:
            - id
            - owner
          properties:
            id:
              type: integer
              description: Unique identifier of the request for access via fine-grained personal access token. Used as the `pat_request_id` parameter in the list and review API calls.
            owner:
              $ref: '#/components/schemas/User'
            permissions_added:
              type: object
              description: |
                Permissions added by the request - each of organization/repository/other is a
                map of permission name to access level (e.g. "contents": "read"), NOT a
                reference to the Organization/Repository entity schemas despite the field names.
              properties:
                organization:
                  description: "Organization-level permissions requested, mapped from permission name to access level."
                  type: object
                  nullable: true
                  additionalProperties:
                    type: string
                repository:
                  description: "Repository-level permissions requested, mapped from permission name to access level."
                  type: object
                  nullable: true
                  additionalProperties:
                    type: string
                other:
                  description: "Other account-level permissions requested, mapped from permission name to access level."
                  type: object
                  nullable: true
                  additionalProperties:
                    type: string
            permissions_upgraded:
              type: object
              description: |
                Permissions upgraded from existing token - same permission-name-to-access-level
                map shape as permissions_added, not an entity reference.
              properties:
                organization:
                  description: "Organization-level permissions being upgraded, mapped from permission name to access level."
                  type: object
                  nullable: true
                  additionalProperties:
                    type: string
                repository:
                  description: "Repository-level permissions being upgraded, mapped from permission name to access level."
                  type: object
                  nullable: true
                  additionalProperties:
                    type: string
                other:
                  description: "Other account-level permissions being upgraded, mapped from permission name to access level."
                  type: object
                  nullable: true
                  additionalProperties:
                    type: string
            permissions_result:
              type: object
              description: |
                The resulting full set of permissions if approved - same permission-name-to-
                access-level map shape as permissions_added, not an entity reference.
              properties:
                organization:
                  description: "The resulting organization-level permissions if the request is approved, mapped from permission name to access level."
                  type: object
                  nullable: true
                  additionalProperties:
                    type: string
                repository:
                  description: "The resulting repository-level permissions if the request is approved, mapped from permission name to access level."
                  type: object
                  nullable: true
                  additionalProperties:
                    type: string
                other:
                  description: "The resulting other account-level permissions if the request is approved, mapped from permission name to access level."
                  type: object
                  nullable: true
                  additionalProperties:
                    type: string
            repository_selection:
              type: string
              enum:
                - none
                - all
                - subset
              description: Type of repository selection requested.
            repositories_url:
              description: "The API URL listing the repositories associated with this resource."
              type: string
              format: uri
              nullable: true
            repositories:
              type: array
              nullable: true
              items:
                $ref: '#/components/schemas/Repository'
              description: An array of repository objects the token is requesting access to. This field is only populated when `repository_selection` is `subset`.
            token_expired:
              type: boolean
              description: Whether the associated fine-grained personal access token has expired.
            token_expires_at:
              type: string
              format: date-time
              nullable: true
              description: Date and time when the associated fine-grained personal access token expires.
            token_last_used_at:
              type: string
              format: date-time
              nullable: true
              description: Date and time when the associated fine-grained personal access token was last used for authentication.
            created_at:
              type: string
              format: date-time
              description: Date and time when the request for access was created.
        sender:
          $ref: '#/components/schemas/User'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    ProjectPayload:
      type: object
      description: |
        Payload for project events (classic project boards).
        Note: classic Projects are deprecated; use projects_v2 instead.
      required:
        - action
        - project
      properties:
        action:
          description: "The action that was performed. Can be one of: deleted, created, closed, reopened, edited."
          type: string
          enum:
            - closed
            - created
            - deleted
            - edited
            - reopened
        project:
          type: object
          description: A classic project board
          required:
            - id
            - node_id
            - name
            - number
            - state
          properties:
            id:
              description: "The unique identifier of the resource."
              type: integer
            node_id:
              description: "The GraphQL node ID."
              type: string
            url:
              description: "The API URL of the resource."
              type: string
              format: uri
            html_url:
              description: "The URL of the resource on GitHub.com."
              type: string
              format: uri
            columns_url:
              description: "The API URL listing the project's columns."
              type: string
              format: uri
            name:
              type: string
              description: Name of the project.
            body:
              type: string
              nullable: true
              description: Body of the project.
            number:
              description: "The project's number, unique within its owner."
              type: integer
            state:
              type: string
              enum:
                - open
                - closed
              description: State of the project; either 'open' or 'closed'.
            creator:
              $ref: '#/components/schemas/User'
            created_at:
              description: "The date and time the resource was created, in ISO 8601 format."
              type: string
              format: date-time
            updated_at:
              description: "The date and time the resource was last updated, in ISO 8601 format."
              type: string
              format: date-time
        changes:
          type: object
          nullable: true
          description: For edited events, the changes made to the project
          properties:
            name:
              type: object
              description: The project's previous name, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: string
            body:
              type: object
              description: The project's previous body text, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: string
                  nullable: true
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    ProjectCardPayload:
      type: object
      description: |
        Payload for project_card events (classic project cards).
        Note: classic Projects are deprecated; use projects_v2_item instead.
      required:
        - action
        - project_card
      properties:
        action:
          description: "The action that was performed. Can be one of: created, edited, moved, converted, deleted."
          type: string
          enum:
            - converted
            - created
            - deleted
            - edited
            - moved
        project_card:
          type: object
          description: A card on a classic project board
          required:
            - id
            - node_id
            - column_id
          properties:
            id:
              type: integer
              description: The project card's ID.
            node_id:
              description: "The GraphQL node ID."
              type: string
            url:
              description: "The API URL of the resource."
              type: string
              format: uri
            column_id:
              description: "The ID of the project column the card belongs to."
              type: integer
            column_url:
              description: "The API URL of the project column the card belongs to."
              type: string
              format: uri
            project_url:
              description: "The API URL of the classic project the card belongs to."
              type: string
              format: uri
            note:
              description: "The card's note text; null when the card links to an issue or pull request."
              type: string
              nullable: true
            content_url:
              type: string
              format: uri
              nullable: true
              description: Link to the issue or PR if the card is content-based
            after_id:
              type: integer
              nullable: true
              description: The ID of the card this card was moved after
            creator:
              $ref: '#/components/schemas/User'
            created_at:
              description: "The date and time the resource was created, in ISO 8601 format."
              type: string
              format: date-time
            updated_at:
              description: "The date and time the resource was last updated, in ISO 8601 format."
              type: string
              format: date-time
        changes:
          type: object
          nullable: true
          description: For edited/moved events, the changes made
          properties:
            note:
              type: object
              description: The card's previous note text, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: string
                  nullable: true
            column_id:
              type: object
              description: The ID of the column the card was previously in, before this move.
              properties:
                from:
                  description: The previous value, before this change.
                  type: integer
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    ProjectColumnPayload:
      type: object
      description: |
        Payload for project_column events (classic project columns).
        Note: classic Projects are deprecated; use projects_v2 instead.
      required:
        - action
        - project_column
      properties:
        action:
          description: "The action that was performed. Can be one of: created, edited, moved, deleted."
          type: string
          enum:
            - created
            - deleted
            - edited
            - moved
        project_column:
          type: object
          description: A column in a classic project board
          required:
            - id
            - node_id
            - name
          properties:
            id:
              type: integer
              description: The unique identifier of the project column.
            node_id:
              description: "The GraphQL node ID."
              type: string
            url:
              description: "The API URL of the resource."
              type: string
              format: uri
            project_url:
              description: "The API URL of the project this column belongs to."
              type: string
              format: uri
            cards_url:
              description: "The API URL listing the cards in this column."
              type: string
              format: uri
            name:
              type: string
              description: Name of the project column.
            after_id:
              type: integer
              nullable: true
              description: The ID of the column this column was moved after
            created_at:
              description: "The date and time the resource was created, in ISO 8601 format."
              type: string
              format: date-time
            updated_at:
              description: "The date and time the resource was last updated, in ISO 8601 format."
              type: string
              format: date-time
        changes:
          type: object
          nullable: true
          description: For edited events, the changes made to the column
          properties:
            name:
              type: object
              description: The column's previous name, before this edit.
              properties:
                from:
                  description: The previous value, before this change.
                  type: string
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'

    RepositoryImportPayload:
      type: object
      description: |
        Payload for repository_import events. Fired when a repository import
        finishes on GitHub.com. No action field.
      required:
        - status
      properties:
        status:
          type: string
          description: The final status of the import
          enum:
            - success
            - cancelled
            - failure
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'

    SubIssuesPayload:
      type: object
      description: Payload for sub_issues events
      required:
        - action
        - parent_issue_id
        - parent_issue
        - parent_issue_repo
        - sub_issue_id
        - sub_issue
      properties:
        action:
          description: "The action that was performed. Can be one of: sub_issue_added, sub_issue_removed, parent_issue_added, parent_issue_removed."
          type: string
          enum:
            - parent_issue_added
            - parent_issue_removed
            - sub_issue_added
            - sub_issue_removed
        parent_issue_id:
          type: integer
          description: The ID of the parent issue.
        parent_issue:
          $ref: '#/components/schemas/Issue'
          description: The parent issue.
        parent_issue_repo:
          $ref: '#/components/schemas/Repository'
          description: The repository of the parent issue.
        sub_issue_id:
          type: integer
          description: The ID of the sub-issue.
        sub_issue:
          $ref: '#/components/schemas/Issue'
          description: The sub-issue.
        sender:
          $ref: '#/components/schemas/User'
        repository:
          $ref: '#/components/schemas/Repository'
        organization:
          $ref: '#/components/schemas/Organization'
        installation:
          $ref: '#/components/schemas/Installation'
        enterprise:
          $ref: '#/components/schemas/Enterprise'
